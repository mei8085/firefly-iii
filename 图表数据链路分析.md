# Firefly III 图表数据链路分析

## 一、系统架构概览

Firefly III 存在两套图表系统：

| 版本 | 技术栈 | 路由前缀 | 主要用途 |
|------|--------|----------|----------|
| v1   | jQuery + Chart.js + Twig 模板 | `/chart/*` | 传统管理界面图表 |
| v2   | Vue + Chart.js + Axios | `/api/v1/chart/*` | 新版仪表盘图表 |

---

## 二、后端数据结构契约

### 2.1 v1 传统格式（ChartJsGenerator 输出）

#### 2.1.1 多数据集格式（multiSet）

由 [ChartJsGenerator::multiSet](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Generator/Chart/Basic/ChartJsGenerator.php#L96-L137) 生成：

```json
{
  "count": 2,
  "labels": ["2024-01", "2024-02", "2024-03"],
  "datasets": [
    {
      "label": "支出 (CNY)",
      "type": "bar",
      "data": ["1200.50", "890.00", "1560.75"],
      "backgroundColor": "rgba(219, 68, 55, 0.5)",
      "currency_symbol": "¥"
    },
    {
      "label": "收入 (CNY)",
      "type": "bar",
      "data": ["5000.00", "5200.00", "4800.00"],
      "backgroundColor": "rgba(0, 141, 76, 0.5)",
      "currency_symbol": "¥"
    }
  ]
}
```

**字段说明**：
- `count`: 数据集数量
- `labels`: X 轴标签数组（时间点/分类名称）
- `datasets`: 数据集数组
  - `label`: 数据集名称（含货币信息）
  - `type`: 图表类型（bar/line）
  - `data`: 数值数组（字符串格式，避免浮点数精度问题）
  - `backgroundColor`: 背景色（RGBA 格式）
  - `currency_symbol`: 货币符号
  - `yAxisID`（可选）: Y 轴 ID（双 Y 轴图表使用）
  - `fill`（可选）: 折线图是否填充

#### 2.1.2 饼图格式（pieChart / multiCurrencyPieChart）

由 [ChartJsGenerator::multiCurrencyPieChart](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Generator/Chart/Basic/ChartJsGenerator.php#L40-L64) 生成：

```json
{
  "datasets": [
    {
      "data": ["1200.50", "890.00", "1560.75"],
      "backgroundColor": ["rgba(53, 124, 165, 0.5)", "rgba(0, 141, 76, 0.5)", "rgba(219, 139, 11, 0.5)"],
      "currency_symbol": ["¥", "¥", "¥"]
    }
  ],
  "labels": ["餐饮", "交通", "购物"]
}
```

### 2.2 v2 API 格式（面向前端数据处理）

由 [Api\V1\Controllers\Chart\CategoryController::overview](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/CategoryController.php#L82-L187) 生成：

```json
[
  {
    "label": "餐饮",
    "currency_id": "1",
    "currency_name": "Chinese Yuan",
    "currency_code": "CNY",
    "currency_symbol": "¥",
    "currency_decimal_places": 2,
    "primary_currency_id": "1",
    "primary_currency_name": "Chinese Yuan",
    "primary_currency_code": "CNY",
    "primary_currency_symbol": "¥",
    "primary_currency_decimal_places": 2,
    "period": null,
    "start_date": "2024-01-01T00:00:00+08:00",
    "end_date": "2024-01-31T23:59:59+08:00",
    "yAxisID": 0,
    "type": "bar",
    "entries": {
      "spent": "1200.50",
      "earned": "0.00"
    },
    "pc_entries": {
      "spent": "1200.50",
      "earned": "0.00"
    }
  }
]
```

**关键字段说明**：
- `entries`: 原始货币金额（spent=支出, earned=收入）
- `pc_entries`: 转换为主货币后的金额（Primary Currency entries）
- `currency_*`: 原始交易货币信息
- `primary_currency_*`: 用户主货币信息（用于多货币统一显示）

---

## 三、后端数据生成链路

### 3.1 核心类层次结构

```
Controller (接收请求)
    ↓
ChartGenerator (业务逻辑)
    ↓
Repository (数据查询)
    ↓
GeneratorInterface (格式转换)
    ↓
JsonResponse (返回)
```

### 3.2 v1 传统链路（以分类图表为例）

#### 控制器入口：[CategoryController::frontPage](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/CategoryController.php#L105-L127)

```php
public function frontPage(): JsonResponse
{
    // 1. 从 Session 获取时间区间
    $start = session('start', today()->startOfMonth());
    $end   = session('end', today()->endOfMonth());
    
    // 2. 缓存检查
    $cache = new CacheProperties();
    $cache->addProperty($start);
    $cache->addProperty($end);
    $cache->addProperty('chart.category.frontpage');
    if ($cache->has()) {
        return response()->json($cache->get());
    }
    
    // 3. 调用业务生成器
    $frontpageGenerator = new FrontpageChartGenerator($start, $end);
    $chartData = $frontpageGenerator->generate();
    
    // 4. 格式转换为 Chart.js 兼容格式
    $data = $this->generator->multiSet($chartData);
    $cache->store($data);
    
    return response()->json($data);
}
```

#### 业务生成器：[FrontpageChartGenerator::generate](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Chart/Category/FrontpageChartGenerator.php#L66-L90)

```php
public function generate(): array
{
    // 1. 获取所有分类
    $categories = $this->repository->getCategories();
    
    // 2. 获取资产账户
    $accounts = $this->accountRepos->getAccountsByType([...]);
    
    // 3. 收集各分类支出
    $collection = $this->collectExpensesAll($categories, $accounts);
    
    // 4. 收集未分类支出
    $noCategory = $this->collectNoCatExpenses($accounts);
    
    // 5. 按金额排序
    $amounts = array_column($collection, 'sum_float');
    array_multisort($amounts, SORT_ASC, $collection);
    
    // 6. 按货币分组
    $currencyData = $this->createCurrencyGroups($collection);
    
    // 7. 填充数值
    return $this->insertValues($currencyData, $collection);
}
```

#### 原始数据格式（生成器输出）：

```php
// 输入给 GeneratorInterface::multiSet 的格式
[
    'spent-1' => [
        'label'           => '支出 (人民币)',
        'type'            => 'bar',
        'currency_symbol' => '¥',
        'entries'         => [
            '餐饮' => 1200.50,
            '交通' => 890.00,
        ]
    ],
    'earned-1' => [
        'label'           => '收入 (人民币)',
        'type'            => 'bar',
        'currency_symbol' => '¥',
        'entries'         => [
            '工资' => 5000.00,
        ]
    ]
]
```

### 3.3 v2 API 链路（以分类概览为例）

控制器入口：[Api\V1\Controllers\Chart\CategoryController::overview](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/CategoryController.php#L82-L187)

核心特点：
- 使用 `DateRangeRequest` 校验时间参数
- 使用 `GroupCollectorInterface` 批量收集交易数据
- 支持主货币转换（`ExchangeRateConverter`）
- 返回结构化数据，由前端负责组装成 Chart.js 格式

---

## 四、时间区间处理机制

### 4.1 时间粒度自动计算

[WholePeriodChartGenerator::calculateStep](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Chart/Category/WholePeriodChartGenerator.php#L124-L139)

```php
protected function calculateStep(Carbon $start, Carbon $end): string
{
    $step   = '1D';  // 默认按天
    $months = $start->diffInMonths($end, true);
    
    if ($months > 3)    $step = '1W';  // 超过3个月按周
    if ($months > 24)   $step = '1M';  // 超过2年按月
    if ($months > 100)  return '1Y';   // 超过8年按年
    
    return $step;
}
```

### 4.2 时间区间列表生成

[Navigation::listOfPeriods](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Navigation.php#L446-L478)

```php
public function listOfPeriods(Carbon $start, Carbon $end): array
{
    $diff = $start->diffInMonths($end, true);
    
    // 根据时间跨度决定增量单位
    if ($diff < 1) {
        $increment = 'addDay';           // <1个月: 按天
        $displayFormat = 'MM-DD';
    } elseif ($diff < 12) {
        $increment = 'addMonthsNoOverflow'; // 1-12个月: 按月
        $displayFormat = 'YYYY-MM';
    } else {
        $increment = 'addYear';           // >12个月: 按年
        $displayFormat = 'YYYY';
    }
    
    // 生成区间列表
    $entries = [];
    while ($begin < $end) {
        $entries[$begin->format('Y-m-d')] = $begin->isoFormat($displayFormat);
        $begin->{$increment}();
    }
    
    return $entries;
}
```

### 4.3 时间区间变化时的重新计算流程

**触发时机**：
1. 用户切换视图范围（月/季/年/自定义）
2. 前端 `window.store.observe('end')` 监听到变化
3. 调用对应 API 重新获取数据

**完整流程**：

```
前端时间选择器变化
    ↓
window.store 更新 'start' / 'end'
    ↓
观察者触发（categories.js line 211-217）
    ↓
chartData = null  // 清除本地缓存
    ↓
getFreshData() 调用 API
    ↓
URL 参数更新: ?start=2024-01-01&end=2024-03-31
    ↓
后端接收请求
    ↓
CacheProperties 哈希变化（时间参数参与哈希计算）
    ↓
缓存未命中，重新生成数据
    ├─ 重新计算时间粒度（calculateStep）
    ├─ 重新生成时间区间（listOfPeriods）
    └─ 按新区间重新查询聚合数据
    ↓
返回新图表数据
    ↓
前端调用 chart.update() 更新渲染
```

### 4.4 时间参数参与缓存键计算

[CacheProperties::hash](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/CacheProperties.php#L92-L104)

```php
private function hash(): void
{
    $content = '';
    foreach ($this->properties as $property) {
        // $start 和 $end 作为属性参与 JSON 编码
        $content .= json_encode($property);
    }
    // 生成 16 位 SHA256 哈希作为缓存键
    $this->hash = substr(hash('sha256', $content), 0, 16);
}
```

**缓存键包含参数**：
- 用户 ID
- 最后活动时间（数据变更时失效）
- `start` 开始时间
- `end` 结束时间
- 分类 ID / 账户 ID 等维度参数
- `convertToPrimary` 主货币转换标志

---

## 五、分类维度变化时的重新计算

### 5.1 分类维度数据结构

以分类报表图表为例：[CategoryController::reportPeriodChart](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/CategoryController.php#L216-L290)

```php
private function reportPeriodChart($accounts, $start, $end, ?Category $category): array
{
    // 1. 根据分类是否存在，选择不同 Repository
    if (!$category) {
        // 未分类交易
        $expenses = $this->noCatRepository->listExpenses($start, $end, $accounts);
        $income   = $this->noCatRepository->listIncome($start, $end, $accounts);
    } else {
        // 指定分类
        $collection = new Collection()->push($category);
        $expenses = $this->opsRepository->listExpenses($start, $end, $accounts, $collection);
        $income   = $this->opsRepository->listIncome($start, $end, $accounts, $collection);
    }
    
    // 2. 获取涉及的所有货币
    $currencies = array_unique(array_merge(array_keys($income), array_keys($expenses)));
    
    // 3. 获取时间区间列表
    $periods = Navigation::listOfPeriods($start, $end);
    
    // 4. 为每种货币创建收支两个数据集
    foreach ($currencies as $currencyId) {
        // 初始化空数据
        foreach (array_keys($periods) as $period) {
            $chartData[$outKey]['entries'][$label] = '0';
            $chartData[$inKey]['entries'][$label]  = '0';
        }
        
        // 填充支出数据
        foreach ($outSet['transaction_journals'] as $journal) {
            $date = $journal['date']->isoFormat($format);
            $chartData[$outKey]['entries'][$date] = bcadd($amount, $chartData[$outKey]['entries'][$date]);
        }
        
        // 填充收入数据
        foreach ($inSet['transaction_journals'] as $journal) {
            $date = $journal['date']->isoFormat($format);
            $chartData[$inKey]['entries'][$date] = bcadd($amount, $chartData[$inKey]['entries'][$date]);
        }
    }
    
    return $this->generator->multiSet($chartData, array_values($periods));
}
```

### 5.2 多分类维度聚合（首页预算图表）

[Budget\FrontpageChartGenerator::generate](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Chart/Budget/FrontpageChartGenerator.php#L67-L85)

```php
public function generate(): array
{
    $budgets = $this->budgetRepository->getActiveBudgets();
    $data = [
        ['label' => '预算内支出', 'entries' => [], 'type' => 'bar'],
        ['label' => '剩余预算',   'entries' => [], 'type' => 'bar'],
        ['label' => '超支',       'entries' => [], 'type' => 'bar'],
    ];
    
    // 遍历所有预算分类
    foreach ($budgets as $budget) {
        $data = $this->processBudget($data, $budget);
    }
    
    return $data;
}
```

### 5.3 分类切换时的重新计算流程

```
用户点击不同分类
    ↓
前端更新 URL 参数: /chart/category/123
或 API 参数: ?category_id=123
    ↓
后端接收分类参数
    ↓
CacheProperties 哈希变化（分类 ID 参与哈希）
    ↓
缓存未命中，重新查询
    ├─ 过滤条件增加: where category_id = ?
    ├─ 或 Repository 方法调用参数变化
    └─ 重新聚合计算
    ↓
返回新数据
    ↓
前端更新图表
```

---

## 六、前端数据消费链路

### 6.1 v1 传统前端

入口：[charts.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/public/v1/js/ff/charts.js)

```javascript
// 模板中调用
columnChart('{{ route('chart.category.frontpage') }}', 'category_chart');

// 核心渲染函数
function drawAChart(URL, container, chartType, options, colorData) {
    // 1. AJAX 请求后端数据
    $.getJSON(URL).done(function(data) {
        // 2. 空数据处理
        if (0 === data.labels.length) {
            // 显示"无数据"提示
            return;
        }
        
        // 3. 自动着色（如后端未提供颜色）
        if (colorData) {
            data = colorizeData(data);
        }
        
        // 4. 已有图表则更新数据，否则新建
        if (allCharts[container]) {
            allCharts[container].data.datasets = data.datasets;
            allCharts[container].data.labels = data.labels;
            allCharts[container].update();
        } else {
            allCharts[container] = new Chart(ctx, {
                type: chartType,
                data: data,
                options: options
            });
        }
    });
}
```

### 6.2 v2 现代前端（Alpine.js + Chart.js）

入口：[categories.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/categories.js)

```javascript
// 1. 初始化时监听全局状态变化
init() {
    // 监听时间区间变化
    window.store.observe('end', () => {
        this.chartData = null;  // 清除缓存
        this.loadChart();       // 重新加载
    });
    
    // 监听主货币转换开关
    window.store.observe('convert_to_primary', (newValue) => {
        this.convertToPrimary = newValue;
        this.loadChart();
    });
}

// 2. 获取数据
getFreshData() {
    const start = new Date(window.store.get('start'));
    const end = new Date(window.store.get('end'));
    
    const dashboard = new Dashboard();
    dashboard.dashboard(start, end, null).then((response) => {
        chartData = response.data;
        this.drawChart(this.generateOptions(chartData));
    });
}

// 3. 前端数据转换（后端返回结构化数据，前端组装成 Chart.js 格式）
generateOptions(data) {
    let options = getDefaultChartSettings('column');
    
    // 按货币和收支类型分组
    let series = {};
    for (const i in data) {
        let current = data[i];
        let code = current.currency_code;
        
        // 创建系列
        for(const type of ['spent', 'earned']) {
            let typeCode = code + '_' + type;
            if (!series[typeCode]) {
                series[typeCode] = {
                    name: typeCode,
                    code: code,
                    type: type,
                    data: {},
                };
            }
        }
        
        // 填充数据
        for(const type of ['spent', 'earned']) {
            let typeCode = code + '_' + type;
            let amount = parseFloat(current.entries[type]);
            
            // 主货币转换时使用 pc_entries
            if(this.convertToPrimary) {
                amount = parseFloat(current.entries.pc_entries[type]);
            }
            
            series[typeCode].data[current.label] = amount;
        }
        
        // X 轴标签
        if (!options.data.labels.includes(current.label)) {
            options.data.labels.push(current.label);
        }
    }
    
    // 4. 组装 Chart.js datasets
    for (const i in series) {
        let dataset = {
            label: i,
            currency_code: currencyCode,
            yAxisID: yAxisID,
            data: [],
        };
        for (const ii in series[i].data) {
            dataset.data.push(series[i].data[ii]);
        }
        options.data.datasets.push(dataset);
    }
    
    return options;
}

// 5. 渲染图表
drawChart(options) {
    if (chart) {
        chart.options = options.options;
        chart.data = options.data;
        chart.update();  // 更新现有图表
        return;
    }
    chart = new Chart(document.querySelector("#category-chart"), options);
}
```

---

## 七、缓存机制详解

### 7.1 CacheProperties 工作流程

[CacheProperties](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/CacheProperties.php)

```
┌─────────────────────────────────────────────────┐
│ 1. 初始化时自动添加                           │
│    - auth()->user()->id                       │
│    - Preferences::lastActivity()              │
│    - Steam::anonymous()                       │
└───────────────────┬─────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────┐
│ 2. 手动添加业务参数                             │
│    $cache->addProperty($start);                │
│    $cache->addProperty($end);                  │
│    $cache->addProperty($category->id);         │
│    $cache->addProperty('chart.category.all');  │
└───────────────────┬─────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────┐
│ 3. 检查缓存                                     │
│    $cache->has()  // 生成哈希并检查             │
└───────────────────┬─────────────────────────────┘
                    │
           ┌────────┴────────┐
           │ 命中            │ 未命中
           ▼                  ▼
    返回缓存数据         执行业务逻辑生成数据
           │                  │
           │      ┌───────────▼───────────┐
           │      │ 4. 存储缓存            │
           │      │    $cache->store($data)│
           │      └───────────┬───────────┘
           │                  │
           └──────────────────┘
                    │
                    ▼
           返回 JSON 响应
```

### 7.2 缓存失效机制

**自动失效**：
- `Preferences::lastActivity()` 记录用户最后活动时间
- 数据变更（创建/更新/删除交易）时更新该时间戳
- 时间戳变化导致缓存哈希变化，原缓存自动失效

**手动失效**：
- 测试环境（`app.env = testing`）直接返回 `false`，跳过缓存
- `Cache::flush()` 清空全部缓存

---

## 八、完整数据流图

### 8.1 v1 分类首页图表完整流程

```
用户访问 / (首页)
    │
    ├─ 页面渲染
    │   └─ Twig 模板包含 <canvas id="category_chart">
    │   └─ JS 调用 columnChart('/chart/category/frontpage', 'category_chart')
    │
    └─ AJAX 请求 GET /chart/category/frontpage
        │
        ▼
[CategoryController::frontPage]
        │
        ├─ 读取 session('start'), session('end')
        │
        ├─ new CacheProperties()
        │   └─ addProperty($start, $end, 'chart.category.frontpage')
        │   └─ has()? → 若命中直接返回缓存
        │
        ├─ new FrontpageChartGenerator($start, $end)
        │   │
        │   ├─ getCategories() → 所有分类
        │   ├─ getAccountsByType([ASSET, DEBT...]) → 相关账户
        │   │
        │   ├─ collectExpensesAll()
        │   │   └─ opsRepos->collectExpenses($start, $end, $accounts, $categories)
        │   │       └─ SQL: SUM(amount) GROUP BY category_id, currency_id
        │   │
        │   ├─ collectNoCatExpenses()
        │   │   └─ noCatRepos->sumExpenses($start, $end, $accounts)
        │   │
        │   └─ generate() 返回分类维度聚合数据
        │
        ├─ $this->generator->multiSet($chartData)
        │   └─ 转换为 Chart.js 兼容格式
        │
        ├─ $cache->store($data) → 永久缓存
        │
        └─ return response()->json($data)
            │
            ▼
[前端 charts.js]
        │
        ├─ colorizeData(data) → 添加颜色
        ├─ new Chart(ctx, { type: 'bar', data: data })
        │
        └─ 图表渲染完成
```

### 8.2 v2 仪表盘分类图表完整流程

```
用户进入 v2 仪表盘
    │
    ├─ Alpine 组件 init()
    │   ├─ 监听 'end' 变化
    │   ├─ 监听 'convert_to_primary' 变化
    │   └─ 调用 loadChart()
    │
    └─ API 请求 GET /api/v1/chart/category/overview?start=2024-01-01&end=2024-01-31
        │
        ▼
[Api\V1\Controllers\Chart\CategoryController::overview]
        │
        ├─ DateRangeRequest 校验 start/end 参数
        │
        ├─ GroupCollectorInterface
        │   └─ setRange($start, $end)
        │   └─ setTypes([WITHDRAWAL, DEPOSIT])
        │   └─ withCategoryInformation()
        │   └─ getExtractedJournals() → 所有符合条件的交易
        │
        ├─ 遍历交易，按分类+货币聚合
        │   ├─ 计算 spent / earned
        │   └─ 如需主货币转换，调用 ExchangeRateConverter
        │
        ├─ $this->clean($return) → 格式化字段
        │
        └─ return response()->json($return)
            │
            ▼
[前端 categories.js]
        │
        ├─ generateOptions(data)
        │   ├─ 按货币+收支类型分组
        │   ├─ 组装 X 轴 labels
        │   └─ 构建 Chart.js datasets（支持双 Y 轴）
        │
        ├─ drawChart(options)
        │   └─ chart.update() 或 new Chart()
        │
        └─ 图表渲染完成
```

---

## 九、关键设计模式与最佳实践

### 9.1 分层架构

| 层级 | 职责 | 代表类 |
|------|------|--------|
| Controller | 参数接收、缓存检查、响应返回 | CategoryController |
| Generator | 业务逻辑编排、数据聚合 | FrontpageChartGenerator |
| Repository | 数据查询封装 | OperationsRepositoryInterface |
| GeneratorInterface | 格式转换（业务格式 → Chart.js 格式） | ChartJsGenerator |

### 9.2 货币处理

1. **字符串存储**：所有金额使用字符串（`bcmath` 函数计算），避免浮点数精度问题
2. **多货币支持**：每种货币独立数据集，前端按货币分组显示
3. **主货币转换**：`ExchangeRateConverter` 实时汇率转换，`pc_entries` 字段存储转换后金额
4. **精度控制**：`Steam::bcround()` 统一处理小数位数

### 9.3 时间处理

1. **粒度自适应**：根据时间跨度自动选择天/周/月/年粒度
2. **时区安全**：所有时间使用 Carbon 对象，统一时区处理
3. **区间对齐**：`startOfPeriod()` / `endOfPeriod()` 确保区间边界正确

### 9.4 性能优化

1. **缓存前置**：控制器入口先检查缓存，未命中才执行业务逻辑
2. **批量查询**：使用 `GroupCollectorInterface` 一次查询所有数据，避免 N+1
3. **前端缓存**：`window.store.set(cacheKey, chartData)` 减少重复请求

---

## 十、代码引用索引

### 后端核心文件

| 文件 | 核心功能 |
|------|----------|
| [CategoryController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/CategoryController.php) | v1 分类图表控制器 |
| [CategoryController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/CategoryController.php) | v2 API 分类图表控制器 |
| [ChartJsGenerator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Generator/Chart/Basic/ChartJsGenerator.php) | Chart.js 格式转换器 |
| [GeneratorInterface.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Generator/Chart/Basic/GeneratorInterface.php) | 图表生成器接口 |
| [WholePeriodChartGenerator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Chart/Category/WholePeriodChartGenerator.php) | 全周期分类图表生成器 |
| [FrontpageChartGenerator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Chart/Category/FrontpageChartGenerator.php) | 首页分类图表生成器 |
| [FrontpageChartGenerator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Chart/Budget/FrontpageChartGenerator.php) | 首页预算图表生成器 |
| [Navigation.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Navigation.php) | 时间区间计算服务 |
| [CacheProperties.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/CacheProperties.php) | 缓存属性管理 |
| [ChartGeneration.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Http/Controllers/ChartGeneration.php) | 图表生成 Trait |

### 前端核心文件

| 文件 | 核心功能 |
|------|----------|
| [charts.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/public/v1/js/ff/charts.js) | v1 图表渲染通用函数 |
| [categories.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/categories.js) | v2 分类图表组件 |
| [dashboard.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/api/v1/chart/category/dashboard.js) | v2 分类图表 API 封装 |

---

## 十一、各控制器图表数据契约差异分析

### 11.1 总览：生成器方法选用对比

各控制器虽然共享 `GeneratorInterface` 的四个输出方法，但选用的方法组合和原始数据格式各不相同：

| 控制器 | `multiSet` | `multiCurrencyPieChart` | `singleSet` | 典型场景 |
|--------|:----------:|:-----------------------:|:-----------:|----------|
| **AccountController** | ✅ expenseAccounts / revenueAccounts / frontpage / period | ✅ expenseBudget / expenseCategory / incomeCategory | ❌ | 账户余额趋势 + 分类/预算饼图 |
| **BillController** | ✅ single（多数据集折线+柱状） | ✅ frontpage（已付/未付饼图） | ❌ | 账单追踪 |
| **BudgetController** | ✅ budget / frontpage / period | ✅ expenseAsset / expenseCategory / expenseExpense | ✅ budgetLimit / periodNoBudget | 预算概览最复杂 |
| **CategoryController** | ✅ all / frontPage / reportPeriod / specificPeriod | ❌ | ❌ | 纯柱状/折线趋势 |
| **TransactionController** | ❌ | ✅ budgets / categories / destinationAccounts / sourceAccounts | ❌ | 纯饼图分组 |
| **ReportController** | ✅ netWorth / operations | ❌ | ❌ | 净资产趋势 + 收支操作 |
| **BudgetReportController** | ✅ mainChart | ✅ budgetExpense / categoryExpense / destinationAccountExpense / sourceAccountExpense | ❌ | 报表下的预算子图 |

### 11.2 各控制器输出格式差异

#### AccountController — 最复杂的混合型

[AccountController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/AccountController.php) 同时使用了三种 `GeneratorInterface` 方法，数据契约差异最大：

**1) multiSet 产出（expenseAccounts / revenueAccounts）**

按货币分数据集，X 轴为账户名称：

```php
// 生成器输入格式（与 CategoryController 不同！）
[
    $currencyId => [
        'label'           => '支出',     // 不含货币名称
        'type'            => 'bar',
        'currency_symbol' => '¥',
        'currency_code'   => 'CNY',       // 比 Category 多了 currency_code
        'entries'         => [
            '支付宝' => 1200.50,
            '微信'   => 890.00,
        ]
    ]
]
```

与 CategoryController 的关键差异：
- **entries 的 key 是账户名而非时间段**，属于"分类维度柱状图"
- `currency_code` 字段在 CategoryController 的 v1 格式中**不传递**（只有 `currency_symbol`）
- `label` 不包含货币名称（CategoryController 的 label 是 `"支出 (人民币)"` 含货币信息）

**2) multiCurrencyPieChart 产出（expenseBudget / expenseCategory / incomeCategory）**

原始数据格式与 TransactionController 完全一致：

```php
$chartData[$label] = [
    'amount'          => $row['total'],
    'currency_symbol' => $row['currency_symbol'],
    'currency_code'   => $row['currency_code'],
];
```

**3) period 方法产出的 multiSet（账户余额趋势）**

这是整个系统中最特殊的图表，原始数据结构有三种 key：

```php
// key 可能是 'balance'、'pc_balance' 或货币代码（如 'USD'）
$return = [
    'balance'    => ['entries' => [...], 'label' => '账户名 (¥)'],
    'pc_balance' => ['entries' => [...], 'label' => '账户名 (合计) (¥)'],
    'USD'        => ['entries' => [...], 'label' => '账户名 ($)'],  // 外币账户
];
```

其他控制器没有这种动态 key 结构。

#### BillController — 唯一的多类型混搭

[BillController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BillController.php) 的 `single` 方法是整个项目中**唯一同时混合 line + bar 类型**的图表：

```php
$chartData = [
    [
        'type'  => 'line',      // 最低金额线
        'label' => '最低金额',
        'entries' => [...],
    ],
    [
        'type'  => 'line',      // 最高金额线
        'label' => '最高金额',
        'entries' => [...],
    ],
    [
        'type'  => 'bar',       // 实际金额柱
        'label' => '实际金额',
        'entries' => [...],
    ],
];
```

其他所有控制器的 `multiSet` 图表中，数据集要么全是 `bar`（Category/Budget 首页），要么全是 `line`（Account 余额），只有 Bill 存在混合类型。

#### BudgetController — 唯一使用 singleSet 的控制器

[BudgetController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BudgetController.php) 的 `budgetLimit` 方法使用了 `singleSet`，这是所有控制器中**唯一**的用例：

```php
$data = $this->generator->singleSet('剩余', $entries);
// 额外手动追加 currency_symbol（singleSet 原生不支持）
$data['datasets'][0]['currency_symbol'] = $currency->symbol;
$data['datasets'][0]['currency_code']   = $currency->code;
```

`periodNoBudget` 方法同样使用 `singleSet`，也没有携带货币信息的原生支持。

`period` 方法中 `entries` 的 key 格式由 `preferredCarbonLocalizedFormat` 决定（与 Category 的 `periodShow` 不同）：

```php
// Budget: 使用 isoFormat
$title = $currentStart->isoFormat($titleFormat);

// Category: 使用 periodShow
$label = Navigation::periodShow($current, $step);
```

#### TransactionController — 纯饼图控制器

[TransactionController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/TransactionController.php) **完全不使用 `multiSet`**，所有方法都输出 `multiCurrencyPieChart`。这是唯一一个不产出时间序列数据的图表控制器。

特殊之处：通过 `resolveJournalAmountAndCurrency` 统一处理货币解析逻辑（其他控制器各自内联处理）。

#### ReportController — 净资产的时间步进差异

[ReportController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/ReportController.php) 的 `netWorth` 方法使用**固定 7 天步进**：

```php
while ($current < $end) {
    // ...
    $current->addDays(7);  // 硬编码 7 天
}
```

而 `operations` 方法使用 `preferredRangeFormat` 动态计算步进。这是唯一一个不使用 `calculateStep` 或 `preferredRangeFormat` 的时间序列图表。

### 11.3 v1 与 v2 API 数据契约根本差异

| 特征 | v1（ChartJsGenerator 输出） | v2（CleansChartData 输出） |
|------|---------------------------|--------------------------|
| **格式转换层** | 后端 `GeneratorInterface` 完成 | 后端输出原始数据，前端自行组装 |
| **输出结构** | `{ count, labels, datasets }` | `[{ label, entries, pc_entries, ... }]` |
| **货币表示** | 数据集级别的 `currency_symbol` | 每行携带完整 `currency_*` + `primary_currency_*` |
| **主货币转换** | 后端直接替换原值 | 双轨制：`entries` + `pc_entries` 并存 |
| **强制字段** | 无校验 | `CleansChartData::clean()` 校验 9 个必填字段 |
| **ID 类型** | 整数 | 字符串（`currency_id` 强制转为 string） |
| **时间标签** | 后端格式化后的本地化字符串 | ISO 8601 Atom 格式（`2024-01-01T00:00:00+08:00`） |
| **Y 轴** | 后端不处理 | 前端按 `currency_code + type` 动态创建 |
| **缓存** | `CacheProperties` + SHA256 | 无服务端缓存（仅前端 `window.store`） |

### 11.4 v2 API 各控制器 entries 差异

| API 控制器 | `entries` 键 | `pc_entries` 键 | 特殊字段 |
|------------|-------------|-----------------|----------|
| [CategoryController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/CategoryController.php) | `{ spent, earned }` | `{ spent, earned }` | - |
| [BudgetController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/BudgetController.php) | `{ budgeted, spent, left, overspent }` | `{ budgeted, spent, left, overspent }` | 4 个维度（最复杂） |
| [AccountController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/AccountController.php) | `{ "2024-01-01T...": balance, ... }` | `{ "2024-01-01T...": pc_balance, ... }` | entries key 是时间点 |
| [BalanceController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/BalanceController.php) | `{ "2024-01-01T...": earned, ... }` 等 | 同左（已按主货币聚合） | 使用 `AccountBalanceGrouped` |

**关键发现**：Category 和 Budget 的 `entries` 是**固定 key 的对象**（spent/earned/budgeted/left/overspent），而 Account 和 Balance 的 `entries` 是**动态时间 key 的对象**。前端 [categories.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/categories.js) 中 `parseFloat(current.entries[type])` 的写法仅适用于固定 key 格式。

---

## 十二、时间区间格式与阈值核实

### 12.1 calculateStep 阈值核实

代码中存在**两份 `calculateStep` 实现**，阈值一致但位置不同：

**实现一**：[DateCalculation Trait](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Http/Controllers/DateCalculation.php#L70-L85)

```php
protected function calculateStep(Carbon $start, Carbon $end): string
{
    $step   = '1D';
    $months = $start->diffInMonths($end, true);
    if ($months > 3)    $step = '1W';
    if ($months > 24)   $step = '1M';
    if ($months > 100)  return '1Y';
    return $step;
}
```

**实现二**：[WholePeriodChartGenerator](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Chart/Category/WholePeriodChartGenerator.php#L124-L139)

```php
protected function calculateStep(Carbon $start, Carbon $end): string
{
    $step   = '1D';
    $months = $start->diffInMonths($end, true);
    if ($months > 3)    $step = '1W';
    if ($months > 24)   $step = '1M';
    if ($months > 100)  return '1Y';
    return $step;
}
```

两份代码完全一致。**但上一版文档中描述的"超过 8 年按年"不准确**——`$months > 100` 对应约 8 年 4 个月，而非恰好 8 年。更精确的说法是"超过 100 个月按年"。

### 12.2 listOfPeriods 与 preferredRangeFormat 阈值差异

系统中有**两套时间区间计算逻辑**，阈值不同：

| 方法 | 来源 | <1 个月 | 1~12 个月 | >12 个月 | 特殊阈值 |
|------|------|---------|-----------|----------|---------|
| [listOfPeriods](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Navigation.php#L446-L478) | v1 | 天 (`addDay`) | 月 (`addMonthsNoOverflow`) | 年 (`addYear`) | `diff >= 1.0001 && < 12.001` |
| [preferredRangeFormat](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Navigation.php#L619-L631) | v1/v2 | `1D` | `1M` | `1Y` | `diff >= 1.001 && < 12.001` |
| `calculateStep` | v1 | `1D` | `1W` (3~24月) | `1M` (24~100月) | `> 3`, `> 24`, `> 100` |

**关键差异**：

1. `calculateStep` 在 3~24 个月区间使用 **`1W`（周）**，而 `preferredRangeFormat` 在 1~12 个月直接使用 **`1M`（月）**。这意味着同一时间跨度，不同图表可能展示不同粒度。
2. `listOfPeriods` 的阈值使用 `>= 1.0001`（浮点容差），而 `preferredRangeFormat` 使用 `>= 1.001`。这导致约 30~31 天的跨度在 `listOfPeriods` 中按月显示，但在 `preferredRangeFormat` 中仍按天显示。
3. `calculateStep` 没有"周"到"年"的直接跳变——在 100 个月之前都是 `1M`，而 `preferredRangeFormat` 在 12 个月时就跳到 `1Y`。

### 12.3 preferredCarbonFormat 格式输出核实

[preferredCarbonFormat](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Navigation.php#L544-L560) 的返回值用于 `$journal['date']->format($format)`：

| 跨度 | 返回值 | 说明 |
|------|--------|------|
| < 1.001 个月 | `'Y-m-d'` | 如 `2024-01-15` |
| 1.001~12.001 个月 | `'Y-m'` | 如 `2024-01` |
| > 12.001 个月 | `'Y'` | 如 `2024` |

[preferredCarbonLocalizedFormat](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Navigation.php#L582-L595) 用于 `$journal['date']->isoFormat($format)`，返回 i18n 格式字符串。

[preferredCarbonFormatByPeriod](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Navigation.php#L565-L575) 按周期字符串映射，v2 API 使用：

| 周期 | 格式 | 示例 |
|------|------|------|
| `1D` | `Y-m-d` | `2024-01-15` |
| `1W` | `\WW,Y` | `W03,2024` |
| `1M` | `Y-m` | `2024-01` |
| `3M`/`6M` | `\QQ,Y` | `Q1,2024` |
| `1Y` | `Y` | `2024` |

### 12.4 ReportController::netWorth 的特殊步进

[netWorth](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/ReportController.php#L69-L136) 使用硬编码 `$current->addDays(7)`，不经过任何阈值计算。这是唯一一个不受上述三套阈值体系约束的图表方法。

---

## 十三、两套图表共存的迁移策略分析

### 13.1 架构演进路径

```
v1 (jQuery/Twig)                    v2 (Alpine/Vue)
┌──────────────┐                   ┌──────────────┐
│  Twig 模板   │                   │  Blade 模板  │
│  + JS 调用   │                   │  + Alpine.js │
├──────────────┤                   ├──────────────┤
│  $.getJSON   │                   │  Axios API   │
│  → /chart/*  │                   │  → /api/v1/* │
├──────────────┤                   ├──────────────┤
│  ChartJsGene │                   │  CleansChart │
│  rator 格式化 │                   │  Data 校验   │
│  → 直接渲染  │                   │  → 前端组装  │
└──────────────┘                   └──────────────┘
       ↕ 共存                            ↕
┌──────────────────────────────────────────────┐
│           共享 Repository 层                 │
│  OperationsRepository / AccountRepository    │
└──────────────────────────────────────────────┘
```

**迁移策略**体现在代码中的以下特征：

1. **渐进式替换**：v2 API 控制器与 v1 控制器并存，但使用不同的路由命名空间（`/chart/*` vs `/api/v1/chart/*`），互不干扰。

2. **v2 代码中大量 `TODO` 标记迁移意图**：
   - [BudgetController::period](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BudgetController.php#L491) 注释：`"Suppress warning because this method will be replaced by API calls."`
   - [CategoryController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/CategoryController.php) 多处 `TODO test me, for category refactor`
   - [AccountController::report](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/AccountController.php#L627) 注释：`TODO this chart is not multi currency aware.`

3. **v2 不使用 `GeneratorInterface`**：v2 API 控制器直接输出结构化数组，由 [CleansChartData](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Http/Api/CleansChartData.php) 校验后返回 JSON，格式化逻辑移到前端。这避免了 `ChartJsGenerator` 的耦合。

4. **v2 不使用 `CacheProperties`**：v2 API 无服务端缓存，完全依赖前端 `window.store` 缓存。这简化了后端逻辑但增加了 API 调用频率。

### 13.2 两套系统共存时的风险

| 风险 | 说明 |
|------|------|
| **数据不一致** | 同一业务逻辑在 v1/v2 中可能存在不同实现（如 Budget 概览在 v1 用 `FrontpageChartGenerator`，v2 用内联 `processBudget`），两份数据可能不同步 |
| **`calculateStep` 重复** | 同一逻辑在 `DateCalculation` trait 和 `WholePeriodChartGenerator` 中各有一份，维护时容易只改一处 |
| **货币转换差异** | v1 在后端完成转换后直接替换原值，v2 保留 `entries` + `pc_entries` 双轨，前端按 `convertToPrimary` 开关选择使用哪个 |
| **缓存策略割裂** | v1 用服务端永久缓存（`Cache::forever`），v2 无服务端缓存；同一用户切换 v1/v2 界面时缓存行为完全不同 |

---

## 十四、缓存键设计分析

### 14.1 缓存键构成模式对比

各控制器 `CacheProperties` 添加的属性存在系统性差异：

| 控制器 | 缓存键属性 | 是否包含 `convertToPrimary` |
|--------|-----------|:------------------------:|
| CategoryController::frontPage | `$start`, `$end`, `convertToPrimary`, 标识符 | ✅ |
| CategoryController::all | 标识符, `$category->id`, `convertToPrimary` | ✅ |
| CategoryController::reportPeriod | `$start`, `$end`, 标识符, `$accounts->pluck('id')`, `$category`, `convertToPrimary` | ✅ |
| AccountController::expenseAccounts | `$start`, `$end`, `convertToPrimary`, 标识符 | ✅ |
| BillController::frontpage | `$start`, `$end`, 标识符 | ❌ |
| BillController::single | 标识符, `$bill->id`, `convertToPrimary` | ✅ |
| BudgetController::budget | `$start`, `$end`, `convertToPrimary`, 标识符, `$budget->id` | ✅ |
| BudgetController::period | `$start`, `$end`, `$accounts`, `$budget->id`, `$currency->id`, 标识符 | ❌ |
| ReportController::operations | 标识符, `$start`, `$accounts`, `$end`, `convertToPrimary` | ✅ |
| ReportController::netWorth | 标识符, `$start`, `accounts IDs`, `$end` | ❌ |

**设计缺陷**：

1. **BillController::frontpage 不含 `convertToPrimary`**：[代码第 59-63 行](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BillController.php#L59-L63) 只添加了 `$start`、`$end`、标识符，当用户切换主货币转换开关时，缓存不会失效，返回旧数据。

2. **BudgetController::period 不含 `convertToPrimary`**：[代码第 494-501 行](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BudgetController.php#L494-L501) 缺失该属性，但方法内部确实使用了 `$this->convertToPrimary`。

3. **ReportController::netWorth 不含 `convertToPrimary`**：[代码第 72-77 行](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/ReportController.php#L72-L77) 同样缺失。

4. **Account ID 传入方式不一致**：`CategoryController::reportPeriod` 使用 `$accounts->pluck('id')->toArray()`（Collection），而 `ReportController::netWorth` 使用 `implode(',', $accounts->pluck('id')->toArray())`（字符串），两者 JSON 编码结果不同但语义等价。

### 14.2 自动失效保障

[CacheProperties](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/CacheProperties.php#L43-L51) 构造时自动添加：

```php
$this->addProperty(auth()->user()->id);
$this->addProperty(Preferences::lastActivity());
$this->addProperty(Steam::anonymous());
```

`Preferences::lastActivity()` 在用户执行任何数据变更操作时更新，导致**所有图表缓存批量失效**。这是一种"宁可重算也不给错数据"的粗粒度策略——即使只修改了一笔交易，所有图表缓存都会失效。

---

## 十五、货币转换层设计分析

### 15.1 v1 与 v2 的货币转换架构差异

```
v1 后端转换（替换式）                v2 后端转换（双轨式）
┌──────────────────┐               ┌──────────────────┐
│  原始金额        │               │  entries         │
│  (原始货币)      │               │  (原始货币金额)  │
│       ↓          │               │       +          │
│  ExchangeRate    │               │  pc_entries      │
│  Converter       │               │  (主货币金额)    │
│       ↓          │               └──────────────────┘
│  转换后金额      │                      ↓
│  (主货币)        │               前端按开关选择
│       ↓          │               entries 或 pc_entries
│  替换原值        │
│  (原值丢失)      │
└──────────────────┘
```

### 15.2 ExchangeRateConverter 的使用模式

`ExchangeRateConverter` 在整个图表系统中存在两种使用模式：

**模式 A：后端即时转换**（v1 和 v2 均使用）

```php
$converter = new ExchangeRateConverter();
$converted = $converter->convert($fromCurrency, $toCurrency, $date, $amount);
```

调用点统计：

| 控制器 | 调用位置 | 用途 |
|--------|---------|------|
| AccountController::period | 内联 | 余额主货币转换 |
| ChartGeneration::accountBalanceChart | Trait | 账户余额趋势 |
| Api\AccountController::overview | 不使用 converter | 直接从 `Steam::finalAccountBalanceInRange` 获取 `pc_balance` |
| Api\BudgetController::overview | 内联 `filterLimit` | 预算限额转换 |

**模式 B：GroupCollector 预计算**（v2 API 主要方式）

```php
// GroupCollector 在查询时已经计算了 pc_amount
$journal['pc_amount']  // 主货币等值金额
$journal['pc_entries'] // 主货币等值条目
```

[GroupCollectorInterface](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Helpers/Collector/GroupCollectorInterface.php) 在提取交易时自动计算 `pc_amount` 字段，v2 API 控制器直接使用该字段而无需再调用 `ExchangeRateConverter`。

### 15.3 convertToPrimary 标志的传播路径

```
用户偏好设置
    ↓
Amount::convertToPrimary()  → 读取 preference 'convert_to_primary'
    ↓
Controller 基类中间件
    ├─ v1: Http\Controllers\Controller (line 152)
    │   $this->convertToPrimary = Amount::convertToPrimary();
    │   $this->primaryCurrency  = Amount::getPrimaryCurrency();
    │   View::share('convertToPrimary', ...);  // 传递给 Twig
    │
    └─ v2: Api\V1\Controllers\Controller (line 89-90)
        $this->convertToPrimary = Amount::convertToPrimary();
        $this->primaryCurrency  = Amount::getPrimaryCurrency();
    ↓
各控制器方法内部
    ├─ v1: 条件替换原值
    │   if ($this->convertToPrimary && $currencyId !== $this->primaryCurrency->id) {
    │       $field = 'pc_amount';  // 使用预计算字段
    │       $currencySymbol = $this->primaryCurrency->symbol;  // 替换符号
    │   }
    │
    └─ v2: 同时填充两套数据
        $row['entries']    = ['spent' => $originalSpent, ...];
        $row['pc_entries'] = ['spent' => $convertedSpent, ...];
```

### 15.4 货币转换的边界情况处理

**1) 外币交易（foreign_currency）的优先级**

在 [BudgetController::expenseAsset](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BudgetController.php#L248-L258) 中，如果交易的 `foreign_currency_id` 等于主货币 ID，则直接使用 `foreign_amount` 而非 `pc_amount`：

```php
if ($journal['foreign_currency_id'] === $this->primaryCurrency->id) {
    $amount = $journal['foreign_amount'];
}
```

这种逻辑在 AccountController 的 `period` 方法中也有体现，体现了"真实汇率优先于计算汇率"的设计原则。

**2) pc_amount 可能为 null**

[BillController::single](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BillController.php#L171-L176) 中：

```php
$amount = bcmul($journal['pc_amount'] ?? '0', '-1');
```

使用 `?? '0'` 防护 `pc_amount` 为 null 的情况，说明 `GroupCollector` 的预计算可能不总是产生 `pc_amount`。

**3) BudgetLimit 的 native_amount 字段**

在货币转换场景下，[BudgetController::budgetLimit](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BudgetController.php#L173-L176) 使用 `BudgetLimit` 模型的 `native_amount` 字段：

```php
if ($this->convertToPrimary) {
    $amount = $budgetLimit->native_amount ?? $amount;
}
```

这是 Firefly III 多货币支持中"模型层预存转换值"的设计——`BudgetLimit` 同时存储 `amount`（原始货币）和 `native_amount`（主货币等值），避免运行时汇率查询。

---

## 十六、补充代码引用索引

| 文件 | 核心功能 |
|------|----------|
| [AccountController.php (v1)](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/AccountController.php) | v1 账户图表（混合 multiSet + pieChart） |
| [BillController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BillController.php) | v1 账单图表（唯一 line+bar 混搭） |
| [BudgetController.php (v1)](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BudgetController.php) | v1 预算图表（唯一使用 singleSet） |
| [TransactionController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/TransactionController.php) | v1 交易图表（纯饼图） |
| [ReportController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/ReportController.php) | v1 报表图表（净资产+操作） |
| [BudgetReportController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/BudgetReportController.php) | v1 预算报表子图 |
| [AccountController.php (v2)](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/AccountController.php) | v2 API 账户概览 |
| [BudgetController.php (v2)](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/BudgetController.php) | v2 API 预算概览 |
| [BalanceController.php (v2)](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/BalanceController.php) | v2 API 余额图表 |
| [CleansChartData.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Http/Api/CleansChartData.php) | v2 数据校验 Trait |
| [DateCalculation.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Http/Controllers/DateCalculation.php) | 时间步进计算 Trait |
| [AccountBalanceGrouped.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Http/Api/AccountBalanceGrouped.php) | v2 余额分组聚合 |

---

## 十七、报表子控制器数据契约深度拆解

### 17.1 四个报表控制器的方法集合总览

| 控制器 | mainChart | categoryExpense | categoryIncome | budgetExpense | tagExpense | tagIncome | destinationExpense | destinationIncome | sourceExpense | sourceIncome |
|--------|:---------:|:---------------:|:--------------:|:-------------:|:----------:|:---------:|:------------------:|:-----------------:|:-------------:|:------------:|
| **CategoryReportController** | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |
| **TagReportController** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **DoubleReportController** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| **ExpenseReportController** | ✅(唯一) | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

使用的 Trait 也有微妙差异：

| 控制器 | `use AugumentData` | `use ResolvesJournalAmountAndCurrency` | `use TransactionCalculation` |
|--------|:------------------:|:--------------------------------------:|:----------------------------:|
| **CategoryReportController** | ✅ | ✅ | ✅ |
| **TagReportController** | ✅ | ✅ | ❌ |
| **DoubleReportController** | ❌ | ❌ | ❌ |
| **ExpenseReportController** | ✅ | ❌ | ✅ |

### 17.2 CategoryReportController：维度聚合最丰富的报表控制器

[CategoryReportController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/CategoryReportController.php) 的方法集合与 BudgetReportController 完全一致，但**没有任何方法使用 CacheProperties**——这与 BudgetReportController 形成鲜明对比。

**方法分类**：

#### （1）mainChart — 单分类下的时间序列趋势图

```php
public function mainChart(Collection $accounts, Category $category, Carbon $start, Carbon $end): JsonResponse
```

**原始数据格式**（输入 `multiSet` 前）：

```php
$chartData = [
    '1-spent' => [
        'label'           => '在 "餐饮" 分类的支出 (人民币)',
        'type'            => 'bar',
        'currency_symbol' => '¥',
        'currency_code'   => 'CNY',
        'currency_id'     => 1,
        'entries'         => [
            '2024-01' => '1200.50',
            '2024-02' => '890.00',
        ],
    ],
    '1-earned' => [
        'label'           => '在 "餐饮" 分类的收入 (人民币)',
        'type'            => 'bar',
        // ...
    ],
];
```

与普通 [CategoryController::reportPeriodChart](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/CategoryController.php#L216-L290) 的差异：
- **参数不同**：reportPeriodChart 接受 `?Collection $categories`（可为空 = 未分类），mainChart 强制 `Category $category`（单一分类）
- **维度粒度不同**：reportPeriodChart 按分类×时间聚合，mainChart 按货币×时间聚合（分类固定）
- **Label 格式不同**：reportPeriodChart 使用 `Navigation::periodShow()`，mainChart 使用 `preferredCarbonLocalizedFormat`

#### （2）8 个饼图方法 — 维度切换的矩阵化设计

8 个饼图方法共享完全相同的代码骨架，唯一差异是**取值字段**和**聚合维度**：

| 方法名 | 交易类型 | 聚合维度（title 来源） | 取值字段 |
|--------|---------|------------------------|---------|
| `budgetExpense` | 支出 | `$journal['budget_name']` | resolveJournalAmountAndCurrency |
| `categoryExpense` | 支出 | `$category['name']` | resolveJournalAmountAndCurrency |
| `categoryIncome` | 收入 | `$category['name']` | resolveJournalAmountAndCurrency |
| `destinationExpense` | 支出 | `$journal['destination_account_name']` | resolveJournalAmountAndCurrency |
| `destinationIncome` | 收入 | `$journal['destination_account_name']` | resolveJournalAmountAndCurrency |
| `sourceExpense` | 支出 | `$journal['source_account_name']` | resolveJournalAmountAndCurrency |
| `sourceIncome` | 收入 | `$journal['source_account_name']` | resolveJournalAmountAndCurrency |

饼图输入格式（`multiCurrencyPieChart`）：

```php
$result = [
    '餐饮 (人民币)' => [
        'amount'          => '1200.50',
        'currency_symbol' => '¥',
        'currency_code'   => 'CNY',
    ],
    '交通 (美元)' => [
        'amount'          => '150.00',
        'currency_symbol' => '$',
        'currency_code'   => 'USD',
    ],
];
```

**关键发现**：title 中**内嵌货币名称**（`%s (%s)` 格式），这是报表控制器饼图独有的特征——普通饼图控制器（如 AccountController::expenseBudget）的 title 不包含货币名。

### 17.3 TagReportController：与 CategoryReportController 的镜像设计

[TagReportController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/TagReportController.php) 是 CategoryReportController 的**几乎完全镜像**，差异仅体现在三处：

1. **Repository 不同**：使用 `Tag\OperationsRepositoryInterface`（按 tag 分组），而非 `Category\OperationsRepositoryInterface`（按 category 分组）
2. **嵌套循环层级不同**：`$currency['tags']` 代替 `$currency['categories']`
3. **多 2 个 tag 方法**：`tagExpense()` 和 `tagIncome()` 直接按 tag 名称聚合（CategoryReportController 无需此方法，因为分类报表下的 categoryExpense 已承担此角色）

```php
// CategoryReportController 的循环层级
foreach ($spent as $currency) {
    foreach ($currency['categories'] as $category) {
        foreach ($category['transaction_journals'] as $journal) { ... }
    }
}

// TagReportController 的循环层级
foreach ($spent as $currency) {
    foreach ($currency['tags'] as $tag) {
        foreach ($tag['transaction_journals'] as $journal) { ... }
    }
}
```

**mainChart 的参数差异**：
```php
// CategoryReportController
public function mainChart(Collection $accounts, Category $category, Carbon $start, Carbon $end)

// TagReportController
public function mainChart(Collection $accounts, Tag $tag, Carbon $start, Carbon $end)
```

### 17.4 DoubleReportController：唯一做 `expandWithDoubles` 预处理的控制器

[DoubleReportController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/DoubleReportController.php) 的**最本质差异**体现在每个方法的前 3 行：

```php
public function categoryExpense(Collection $accounts, Collection $others, Carbon $start, Carbon $end): JsonResponse
{
    $joined   = $this->repository->expandWithDoubles($others);  // ★ 关键调用
    $accounts = $accounts->merge($joined);                       // ★ 合并到 $accounts
    $spent    = $this->opsRepository->listExpenses($start, $end, $accounts);
    // ...
}
```

`expandWithDoubles` 展开**双向关联账户**（如信用卡还款账户与其对应银行账户）——这是"双重账户报表"的核心逻辑，其他三个控制器均不处理此场景。

#### DoubleReportController 的 tagExpense / tagIncome 特殊性

这两个方法中存在**唯一的防重复记账逻辑**：

```php
$includedJournals = [];
foreach ($currency['transaction_journals'] as $journal) {
    $journalId = $journal['transaction_journal_id'];

    // 无标签的交易：计一次
    if (0 === count($journal['tags'])) {
        $includedJournals[] = $journalId;
        $tagName = trans('firefly.no_tags');
        // 累加金额 ...
    }

    // 有标签的交易：只计入第一个标签，防止重复
    foreach ($journal['tags'] as $tag) {
        if (in_array($journalId, $includedJournals, true)) {
            continue;  // ★ 已处理过则跳过
        }
        $includedJournals[] = $journalId;
        // 累加金额 ...
    }
}
```

其他控制器的 tag 方法（TagReportController::tagExpense）没有 `$includedJournals` 去重——当一个交易有多个 tag 时，金额会被计入**每个** tag，总和会超过交易总额。

### 17.5 ExpenseReportController：唯一拥有累计曲线的控制器

[ExpenseReportController](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/ExpenseReportController.php) 只有一个 `mainChart` 方法，但这是整个系统中**最复杂的图表**：

```php
// 每个 expense 账户生成 4 个数据集：
$chartData[$exp->id.'-in']         = ['type' => 'bar',  'yAxisID' => 'y-axis-0', ...]; // 当期收入(柱)
$chartData[$exp->id.'-out']        = ['type' => 'bar',  'yAxisID' => 'y-axis-0', ...]; // 当期支出(柱)
$chartData[$exp->id.'-total-in']   = ['type' => 'line', 'yAxisID' => 'y-axis-1', 'fill' => false]; // 累计收入(线)
$chartData[$exp->id.'-total-out']  = ['type' => 'line', 'yAxisID' => 'y-axis-1', 'fill' => false]; // 累计支出(线)
```

累计曲线逻辑：

```php
$sumOfIncome[$exp->id]  ??= '0';
$sumOfExpense[$exp->id] ??= '0';
$sumOfIncome[$exp->id]      = bcadd($sumOfIncome[$exp->id], $currentIncome);
$sumOfExpense[$exp->id]     = bcadd($sumOfExpense[$exp->id], $currentExpense);
// 当前期柱 + 累计线同步写入
$chartData[$labelSumIn]['entries'][$label]  = $sumOfIncome[$exp->id];
$chartData[$labelSumOut]['entries'][$label] = $sumOfExpense[$exp->id];
```

**空数据过滤**（独有）：

```php
$newSet = [];
foreach ($chartData as $key => $entry) {
    if (array_sum($entry['entries']) > 0) {  // ★ 非零才保留
        $newSet[$key] = $entry;
    }
}
```

这是**唯一做空数据集剔除**的控制器——其他控制器保留所有数据集（即使 entries 全为 0），导致 Chart.js 渲染出空图例。

### 17.6 四个报表控制器与普通图表控制器的契约差异总结

| 特征 | 报表子控制器（Category/Tag/Double/Expense） | 普通图表控制器（Category/Budget/Account） |
|------|------------------------------------------|----------------------------------------|
| **路由调用方式** | 方法参数注入（`Collection $accounts`） | 路由参数解析 + Session（`session('start')`） |
| **CacheProperties** | ❌ 除 ExpenseReportController 外均不使用 | ✅ 几乎所有方法都用 |
| **convertToPrimary** | ❌ 完全不读取 `$this->convertToPrimary` | ✅ 方法级条件判断 |
| **Resolver Trait** | Category/Tag 使用 `ResolvesJournalAmountAndCurrency` | 各自内联处理（`Steam::positive/bcmul`） |
| **时间步长函数** | `preferredCarbonLocalizedFormat` + `preferredEndOfPeriod` | 混用 `periodShow` / `calculateStep` / `preferredCarbonFormat` |
| **Label 货币名** | 饼图 title 内嵌货币名 `%s (%s)` | 饼图 title 不含货币名 |
| **v2 API 对应** | ❌ v2 API 目前不提供报表专用图表 | ✅ 提供 dashboard/overview 系列 |
| **输入 Repository** | 多维分组数据结构 `$currency['categories']['journals']` | 扁平 `$currency['transaction_journals']` |

---

## 十八、仪表盘多图表并发请求的缓存竞争处理

### 18.1 v2 仪表盘并发请求模型

v2 仪表盘通过 [dashboard.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/dashboard.js) 的 `Alpine.start()` 同时启动 **7 个独立 Alpine 组件**：

```
Alpine.start()
    ├─ boxes              → /api/v1/.../boxes
    ├─ accounts           → /api/v1/chart/account/dashboard
    ├─ budgets            → /api/v1/chart/budget/dashboard
    ├─ categories         → /api/v1/chart/category/dashboard
    ├─ sankey             → /api/v1/chart/sankey
    ├─ subscriptions      → /api/v1/chart/subscriptions
    └─ piggies            → /api/v1/chart/piggies
```

7 个组件的 `init()` 在同一事件循环中执行，**同时发出 7 个并发 HTTP 请求**。这是前端并发的唯一来源。

### 18.2 v1 并发请求的缓存竞争

v1 使用 `CacheProperties::forever()`（永久缓存），存在以下竞争场景：

```
请求 A：/chart/category/frontpage（用户 A，1 月数据）
请求 B：/chart/category/frontpage（用户 A，1 月数据）
请求 C：/chart/category/frontpage（用户 B，1 月数据）
```

**三个请求同时到达时的竞争时序**：

```
T=0  A→new CacheProperties()
     ├─ auth()->user()->id  → 1
     ├─ lastActivity        → 1700000000
     └─ properties: [1, 1700000000, false, start, end, 'chart']

T=1  B→new CacheProperties()
     └─ 完全相同的属性（同用户同时间）
     
T=2  A→hash()  → "a1b2c3d4..."
     A→has()   → false（缓存为空）

T=3  B→hash()  → "a1b2c3d4..."（同哈希）
     B→has()   → false（A 尚未写入）

T=4  A→执行业务逻辑（SQL 查询，100ms）
     B→执行业务逻辑（SQL 查询，100ms）
     ★ 两个请求重复执行相同计算

T=104 A→store(data)  Cache::forever("a1b2c3...", $data)
      B→store(data)  Cache::forever("a1b2c3...", $data)
      ★ B 覆盖 A，但内容相同，无功能影响，仅浪费计算资源
```

**Firefly III 没有使用 Cache::lock() 或原子性写入**——[CacheProperties.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/CacheProperties.php#L87-L90) 直接 `Cache::forever()`，依赖 `CacheProperties` 构造函数中的 `Preferences::lastActivity()` 作为隐式去重。

**`lastActivity()` 的隐式竞争防护**：

```php
// CacheProperties 构造函数
$this->addProperty(auth()->user()->id);        // ① 用户隔离
$this->addProperty(Preferences::lastActivity());// ② 时间戳版本号
$this->addProperty(Steam::anonymous());         // ③ 匿名用户隔离
```

当用户提交交易时触发 `Preferences::touchLastActivity()`，`lastActivity` 时间戳更新，**同一用户新老请求不会共享同一缓存键**——正在处理中的旧请求写入的缓存对新请求无效，间接避免了读脏数据。

### 18.3 v2 前端缓存的并发竞争防护

v2 前端通过两个机制应对并发：每个组件的 `loading` 标志，以及基于 `getCacheKey` 的确定性键。

**（1）loading 标志 — 防止单组件重复触发**

[budgets.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/budgets.js#L38-L42)：

```javascript
loadChart() {
    if (true === this.loading) {  // ★ 并发防护
        return;
    }
    this.loading = true;
    // ...发请求
},
```

但这是**组件级防护**，不跨组件。当 convert-to-primary 事件发出时，7 个组件分别收到事件，各自触发 `chartData = null; this.loadChart()`，7 个请求仍然并发发出。

**（2）getCacheKey — 确定性键，不防重复计算但统一返回结果**

```javascript
const cacheKey = getCacheKey('ds_bdg_chart', {
    convertToPrimary: this.convertToPrimary,
    start: start,
    end: end
});
```

竞争时序：

```
T=0    convert-to-primary 事件广播
       accounts/budgets/categories 分别收到
       
T=1    accounts→chartData=null, loading=true  → 发出请求 A
       budgets →chartData=null, loading=true  → 发出请求 B
       categories→chartData=null, loading=true→ 发出请求 C
       
T=2    A 回来 → window.store.set(cacheKey_A, data_A)
       B 回来 → window.store.set(cacheKey_B, data_B)
       C 回来 → window.store.set(cacheKey_C, data_C)
       ★ 无覆盖冲突，因为各组件使用不同前缀（ds_bdg_ / ds_accounts_ / category 内置）
```

**`cacheValid` 的设计缺陷**：[budgets.js line 74](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/budgets.js#L74) 硬编码 `const cacheValid = false;`，导致缓存读取逻辑**永远走不到**。`window.store.set()` 只是空写，实际每次都发新鲜请求。

```javascript
// budgets.js 第 72-82 行
const cacheKey = getCacheKey('ds_bdg_chart', { ... });
//const cacheValid = window.store.get('cacheValid');
const cacheValid = false;  // ★ 硬编码关闭
let cachedData = window.store.get(cacheKey);

if (cacheValid && typeof cachedData !== 'undefined') {  // ★ 永远为 false
    // 此分支不可达
}
const dashboard = new Dashboard();
dashboard.dashboard(...).then(...)  // 每次都请求
```

[accounts.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/accounts.js#L65) 正常读取 `cacheValid`：

```javascript
const cacheValid = window.store.get('cacheValid');  // 正常读取
```

这意味着预算图表在并发请求下**完全不利用前端缓存**，而账户图表可以命中缓存。

### 18.4 缓存失效的竞争时序：lastActivity vs 正在处理的请求

用户提交一笔交易后，仪表盘各图表的行为：

```
T=0    用户点击"保存交易"（触发 touchLastActivity）
       lastActivity 旧值：1700000000
       lastActivity 新值：1700000300
       
T=1    浏览器跳转回仪表盘（或前端自动刷新）
       v1: 重新加载首页 → Twig 模板渲染 → 7 个图表开始请求
       v2: dispatch('refresh') → 各组件 loadChart()
       
T=2    v1 CategoryController::frontPage 启动
       → new CacheProperties()
       → lastActivity = 1700000300
       → hash = 不同于上次任何请求的新哈希
       → has() = false → 必须重新计算 ✅  正确性得到保证
       
T=102  v1 请求返回新数据（含刚保存的交易）
       v2 API 请求也返回新数据（无服务端缓存）
```

关键安全保障：`Preferences::lastActivity()` **在 CacheProperties 构造时同步读取**，不是静态缓存。即使同一 PHP 进程处理连续两个请求（先 touch 再读），后续请求也会读到新值。

---

## 十九、API 入口鉴权与多用户隔离实现

### 19.1 v1 Web 路由的鉴权栈

v1 `/chart/*` 路由不直接声明中间件，而是通过 Controller 基类 + Route 分组隐式获取：

| 层级 | 机制 | 代码位置 |
|------|------|---------|
| 路由分组 | `web` middleware 组（session + auth） | routes/web.php 顶层分组 |
| 基类中间件 | auth()->check() 仅读取，不强制抛异常 | [Http\Controllers\Controller](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Controller.php#L146-L168) |
| CacheProperties | 构造函数 `if (auth()->check())` 注入用户 ID | [CacheProperties L46-L50](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/CacheProperties.php#L46-L50) |

**多用户隔离的三道防线**：

```
防线①：Session 层
    不同用户的 PHP Session 完全隔离，auth()->user() 返回不同对象
    
防线②：SQL 层（Eloquent 全局 Scope）
    UserClass::userGroup() / Account::user() 等全局 Scope 自动追加
    WHERE user_group_id = ? 或 WHERE user_id = ?
    
防线③：缓存层
    CacheProperties 构造函数 auth()->user()->id 参与哈希
    不同用户的缓存键天然不冲突
```

**缓存用户隔离的边界情况**：`auth()->check()` 在构造函数中为 `false` 时，**不会抛异常**，CacheProperties 仅得到空 properties：

```php
// CacheProperties 构造函数
if (auth()->check()) {
    $this->addProperty(auth()->user()->id);       // 未登录则跳过
    $this->addProperty(Preferences::lastActivity());
    $this->addProperty(Steam::anonymous());
}
```

这意味着**如果 `/chart/*` 路由被未登录用户访问**，缓存键将不含用户信息——所有访客共享同一空哈希。但这是可接受的，因为：
- `$start = session('start')` 未登录时取不到 session，默认用 `today()`，哈希变化不受影响
- SQL 层 UserClass Scope 会返回空集合，数据为空，共享缓存无安全风险

### 19.2 v2 API 路由的显式鉴权栈

v2 `/api/v1/chart/*` 使用独立的路由分组和更严格的机制：

| 层级 | 机制 | 代码位置 |
|------|------|---------|
| 路由分组 | `auth:api` middleware（OAuth2 + Bearer token） | routes/api.php 分组 |
| 基类中间件 | Accept 头校验（仅允许 application/json 等） | [Api\Controller L94-L97](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Controller.php#L94-L97) |
| Trait 显式校验 | `ValidatesUserGroupTrait::validateUserGroup()` | [ValidatesUserGroupTrait L52-L114](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Http/Api/ValidatesUserGroupTrait.php#L52-L114) |
| 控制器 $acceptedRoles | 声明允许访问的角色枚举 | [Api\Chart\CategoryController L66-68](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Chart/CategoryController.php#L66-L68) |

#### ValidatesUserGroupTrait 执行流程

```
请求到达
    ↓
validateUserGroup($request)
    │
    ├─ auth()->check() 验证 OAuth token 有效
    │   否 → 抛 AuthenticationException（HTTP 401）
    │
    ├─ 解析 $groupId
    │   ├─ URL?user_group_id=X → 用 X
    │   └─ 未指定 → 用 auth()->user()->user_group_id
    │
    ├─ UserGroupRepository 查询 $groupId 的 membership
    │   0 条 memberships → 抛 AuthorizationException（HTTP 403）
    │
    ├─ count($this->acceptedRoles) == 0 ?
    │   是 → 抛"没有定义可接受角色"异常
    │
    └─ 遍历 acceptedRoles: UserRoleEnum[]
        $user->hasRoleInGroupOrOwner($group, $role) ?
        任一命中 → 返回 $group ✅  通过
        全部未命中 → 抛"无权限"（HTTP 403）
```

**各图表控制器声明的 acceptedRoles**：

```php
// CategoryController
protected array $acceptedRoles = [UserRoleEnum::OWNER, UserRoleEnum::FULL, UserRoleEnum::VIEW_TRANSACTIONS, UserRoleEnum::VIEW_REPORTS];

// BudgetController  
protected array $acceptedRoles = [UserRoleEnum::OWNER, UserRoleEnum::FULL, UserRoleEnum::VIEW_BUDGETS, UserRoleEnum::VIEW_REPORTS];

// AccountController
protected array $acceptedRoles = [UserRoleEnum::OWNER, UserRoleEnum::FULL, UserRoleEnum::VIEW_REPORTS, UserRoleEnum::VIEW_ACCOUNTS];
```

UserRoleEnum 权限矩阵（各图表能力映射）：

| 角色 | Category 图表 | Budget 图表 | Account 图表 | 说明 |
|------|:------------:|:-----------:|:------------:|------|
| `OWNER` | ✅ | ✅ | ✅ | 组所有者，拥有全部权限 |
| `FULL` | ✅ | ✅ | ✅ | 完全控制，可读写全部资源 |
| `VIEW_TRANSACTIONS` | ✅ | ❌ | ❌ | 只能看交易/分类维度图表 |
| `VIEW_BUDGETS` | ❌ | ✅ | ❌ | 只能看预算维度图表 |
| `VIEW_ACCOUNTS` | ❌ | ❌ | ✅ | 只能看账户维度图表 |
| `VIEW_REPORTS` | ✅ | ✅ | ✅ | 只读报表权限，覆盖全部图表 |
| `EDIT_TRANSACTIONS` | ❌ | ❌ | ❌ | 图表不可见 |

这是整个系统中**唯一的细粒度权限控制**——v1 图表控制器没有任何基于角色的判断，只要登录就能访问所有图表。

### 19.3 v1 vs v2 多用户隔离设计对比

| 维度 | v1 Web 图表 | v2 API 图表 |
|------|-----------|------------|
| **认证方式** | Session Cookie | OAuth2 Bearer Token |
| **强制登录** | 隐式（通过路由分组 `web`） | 显式 `auth:api` + `ValidatesUserGroupTrait` |
| **资源归属校验** | Eloquent Scope 自动 WHERE | 额外 `validateUserGroup()` 显式检查 |
| **角色粒度** | 登录=全能（所有角色等同） | `$acceptedRoles` 细粒度声明（OWNER/FULL/VIEW_*） |
| **多用户组支持** | 仅当前用户所在组 | 支持 URL `?user_group_id=` 跨组访问（需有权限） |
| **未登录访问** | 静默降级（返回空数据） | 硬失败（HTTP 401） |
| **Accept 头校验** | 无 | 必须匹配 `application/json` 或 `application/vnd.api+json` |

---

## 二十、最终代码引用索引

### 报表子控制器

| 文件 | 核心功能 |
|------|----------|
| [CategoryReportController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/CategoryReportController.php) | 分类报表：1 个时间序列 + 8 个饼图维度，无 Cache |
| [TagReportController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/TagReportController.php) | 标签报表：Category 的镜像，增加 tagExpense/tagIncome |
| [DoubleReportController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/DoubleReportController.php) | 双向账户报表：唯一 expandWithDoubles + tag 去重逻辑 |
| [ExpenseReportController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Chart/ExpenseReportController.php) | 收支报表：唯一带累计曲线 + 空集剔除 + 有 Cache |

### v2 仪表盘前端

| 文件 | 核心功能 |
|------|----------|
| [dashboard.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/dashboard.js) | 仪表盘入口，Alpine 7 组件并发启动 |
| [budgets.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/budgets.js) | 预算图表：硬编码 cacheValid=false 导致缓存失效 |
| [accounts.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/accounts.js) | 账户图表：正常读取 cacheValid，支持前端缓存命中 |
| [categories.js](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/resources/assets/v2/src/pages/dashboard/categories.js) | 分类图表：监听 convert-to-primary 事件 |

### 鉴权与用户隔离

| 文件 | 核心功能 |
|------|----------|
| [ValidatesUserGroupTrait.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Support/Http/Api/ValidatesUserGroupTrait.php) | v2 API 用户组+角色校验，5 级权限矩阵 |
| [Http\Controllers\Controller.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Http/Controllers/Controller.php#L137-L173) | v1 基类中间件：convertToPrimary + primaryCurrency 注入 |
| [Api\V1\Controllers\Controller.php](file:///d:/fz/0601-1/solo-dogfeeding/code/79-firefly-iii/app/Api/V1/Controllers/Controller.php#L84-L100) | v2 基类中间件：Accept 头校验 + 相同货币参数注入 |
