# Firefly III 周期交易调度器关键点源码级分析

---

## 一、CLI 与 API 入口下 `last_rt_job` 的写入路径差异

### 1.1 两条入口的完整调用链

#### CLI 入口：`php artisan firefly-iii:cron`

```
[Cron.php::handle()]                          ← Artisan 命令入口
    $date = new Carbon($this->option('date')); ← --date 选项可能为 null
    $force = (bool) $this->option('force');
    ↓
[Cron.php::recurringCronJob($force, $date)]
    $recurring = new RecurringCronjob();       ← 构造器: $this->date = today() (零点)
    $recurring->setForce($force);
    if ($date instanceof Carbon) {             ← 仅当 --date 有值时
        $recurring->setDate($date);            ← 覆盖构造器中的 today()
    }
    $recurring->fire();
```

代码位置：[Cron.php#L211-L231](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Console/Commands/Tools/Cron.php#L211-L231)

**关键**：CLI 的 `--date` 选项解析后是一个 `?Carbon`（可空）。当未传 `--date` 时，`$date = null`，`if ($date instanceof Carbon)` 不成立，因此 **不会调用 `setDate()`**，`$this->date` 保持构造器中的 `today()` 值。

#### API 入口：`GET /api/v1/cron/{token}`

```
[CronRequest::getAll()]                       ← API 请求参数解析
    $data = ['force' => false, 'date' => today(config('app.timezone'))];
    if ($this->has('force')) { $data['force'] = $this->boolean('force'); }
    if ($this->has('date'))  { $data['date'] = $this->getCarbonDate('date'); }
    if (!$data['date'] instanceof Carbon) { $data['date'] = today(...); }
    ↓
[Api\CronController::cron($request)]
    $config = $request->getAll();
    $this->runRecurring($config['force'], $config['date']);
    ↓
[CronRunner::runRecurring($force, $date)]
    $recurring = app(RecurringCronjob::class); ← 构造器: $this->date = today() (零点)
    $recurring->setForce($force);
    $recurring->setDate($date);                ← 始终调用，覆盖构造器值
    $recurring->fire();
```

代码位置：
- [CronRequest.php#L49-L64](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Api/V1/Requests/System/CronRequest.php#L49-L64)
- [CronRunner.php#L103-L122](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Http/Controllers/CronRunner.php#L103-L122)

**关键**：API 入口**始终调用 `setDate($date)`**，因为 `CronRequest::getAll()` 保证返回一个非空 `Carbon`（无 `date` 参数时默认为 `today()`）。

### 1.2 三条 `$this->date` 赋值路径对比

| 路径 | 触发条件 | `$this->date` 值 | 是否 `startOfDay` |
|------|---------|------------------|------------------|
| **AbstractCronjob 构造器** | 始终执行 | `today(config('app.timezone'))` | ✅ `today()` 返回零点 |
| **CLI: `setDate($date)`** | `--date` 有值 | 用户指定日期 | ❌ `setDate()` 不调用 `startOfDay` |
| **API: `setDate($date)`** | 始终调用 | CronRequest 解析的日期（可能含时分秒） | ❌ `setDate()` 不调用 `startOfDay` |

代码位置：[AbstractCronjob.php#L45-L56](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/AbstractCronjob.php#L45-L56)

```php
public function __construct()
{
    $this->date = today(config('app.timezone'));  // 零点
}

final public function setDate(Carbon $date): void
{
    $newDate    = clone $date;   // 直接 clone，不做 startOfDay
    $this->date = $newDate;
}
```

**但是**，在 `CreateRecurringTransactions` 构造器中有二次处理：

```php
public function __construct(?Carbon $date)
{
    $newDate = new Carbon();
    $newDate->startOfDay();           // 强制零点
    $this->date = $newDate;

    if ($date instanceof Carbon) {
        $newDate = clone $date;
        $newDate->startOfDay();       // 再次强制零点
        $this->date = $newDate;
    }
}
```

代码位置：[CreateRecurringTransactions.php#L71-L93](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L71-L93)

### 1.3 `last_rt_job` 写入时的语义差异

`last_rt_job` 在 [RecurringCronjob.php#L94](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php#L94) 写入：

```php
FireflyConfig::set('last_rt_job', (int) $this->date->format('U'));
```

| 入口 | `setDate` 是否调用 | `$this->date` 进入 Job 时 | `last_rt_job` 写入值 |
|------|-------------------|--------------------------|---------------------|
| CLI 无 `--date` | ❌ 不调用 | 构造器的 `today()` 零点 | 当天零点时间戳 |
| CLI 有 `--date=2024-06-15` | ✅ 调用 | 用户指定日期（`setDate` 不做 startOfDay） | 指定日期的 **原始时间** |
| API 无 date 参数 | ✅ 调用 `setDate(today())` | `today()` 零点 | 当天零点时间戳 |
| API 有 date 参数 | ✅ 调用 `setDate(解析日期)` | 解析后的日期（可能含时分秒） | 指定日期的 **原始时间** |

### 1.4 12小时冷却比较的语义差异

在 [RecurringCronjob.php#L48-L57](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php#L48-L57)：

```php
$lastTime = (int) FireflyConfig::get('last_rt_job', 0)->data;
$diff = now(config('app.timezone'))->getTimestamp() - $lastTime;
if ($lastTime > 0 && $diff <= 43_200) { ... }
```

**场景分析**：

| 上次写入的 `last_rt_job` | 含义 | 本次 `now()` | `$diff` | 12h 判断 |
|--------------------------|------|-------------|---------|---------|
| 周一 00:00 (零点) | CLI 无 `--date` 正常执行 | 周一 14:00 | 14h | `> 12h` ✅ 可执行 |
| 周一 10:30 (非零点) | CLI `--date` 指定了非零点时间 | 周二 00:30 | 14h | `> 12h` ✅ 可执行 |
| 周一 00:00 (零点) | 正常执行 | 周一 10:00 | 10h | `≤ 12h` ❌ 冷却中 |
| 周一 23:50 (非零点) | API 传了非零点 date | 周二 00:01 | 0.18h | `≤ 12h` ❌ 冷却中 |

**核心差异**：当 `last_rt_job` 存储的是非零点时间戳时（CLI `--date` 或 API date 参数），冷却判断会基于该**具体时刻**计算差值，而非「天数」语义。这导致：
- 若写入的是当天 23:50，则次日 00:01 的 `$diff` 仅 11 分钟，不会触发执行
- 若写入的是当天 00:00，则次日 12:01 的 `$diff` 为 12 小时 1 分钟，可以执行

---

## 二、`RecurrenceRepetitionWeekend` 四档位与 `FiltersWeekends` 命中分支

### 2.1 枚举定义

代码位置：[RecurrenceRepetitionWeekend.php#L30-L36](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Enums/RecurrenceRepetitionWeekend.php#L30-L36)

```php
enum RecurrenceRepetitionWeekend: int
{
    case WEEKEND_DO_NOTHING    = 1;
    case WEEKEND_SKIP_CREATION = 2;
    case WEEKEND_TO_FRIDAY     = 3;
    case WEEKEND_TO_MONDAY     = 4;
}
```

### 2.2 `FiltersWeekends` 的分支逻辑

代码位置：[FiltersWeekends.php#L41-L99](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/FiltersWeekends.php#L41-L99)

```
输入: $dates = [周六, 周日, 周一, 周二, ...]

foreach $dates as $date:
    ├─ 不是周末 → 直接入栈 $return[] = $date
    └─ 是周末:
        ├─ WEEKEND_DO_NOTHING (1)    → 不会进入此分支（函数开头已 return $dates）
        ├─ WEEKEND_TO_FRIDAY (3)     → $clone->addDays(5 - dayOfWeekIso); 入栈
        ├─ WEEKEND_TO_MONDAY (4)     → $clone->addDays(8 - dayOfWeekIso); 入栈
        └─ WEEKEND_SKIP_CREATION (2) → 不入栈（无匹配分支，跳过）
```

### 2.3 四档位的详细行为

| 档位 | 枚举值 | 工作日 | 周六 | 周日 | 行为描述 |
|------|-------|--------|------|------|---------|
| `DO_NOTHING` | 1 | 原样保留 | 原样保留 | 原样保留 | 不做任何过滤，直接返回原数组 |
| `SKIP_CREATION` | 2 | 原样保留 | **不入栈** | **不入栈** | 周末日期直接丢弃，不生成交易 |
| `TO_FRIDAY` | 3 | 原样保留 | → 周五 | → 周五 | 周末统一提前到前一个周五 |
| `TO_MONDAY` | 4 | 原样保留 | → 周一 | → 周一 | 周末统一推迟到下一个周一 |

### 2.4 `SKIP_CREATION` 不入栈的深层含义

当 `weekend = 2`（SKIP_CREATION）时，代码逻辑为：

```php
if (!$isWeekend) {
    $return[] = clone $date;   // 工作日入栈
    continue;
}
// 是周末，但没有匹配的分支
// TO_FRIDAY? 不匹配 (weekend=2 ≠ 3)
// TO_MONDAY? 不匹配 (weekend=2 ≠ 4)
// 什么都没做 → 该日期不入栈 → 被静默丢弃
```

**效果**：如果周期交易的**所有发生日期都落在周末**（如每周六的周交易），且配置了 `SKIP_CREATION`，则该周期交易**永远不会生成任何交易**。

### 2.5 `TO_FRIDAY` 与 `TO_MONDAY` 错过原始执行日的对称差异

#### 顺延算法

```php
// TO_FRIDAY: 向前回退到周五
$clone->addDays(5 - $date->dayOfWeekIso);
// 周六(6): 5-6 = -1 → 回退1天 → 周五
// 周日(7): 5-7 = -2 → 回退2天 → 周五

// TO_MONDAY: 向后推迟到周一
$clone->addDays(8 - $date->dayOfWeekIso);
// 周六(6): 8-6 = +2 → 推迟2天 → 周一
// 周日(7): 8-7 = +1 → 推迟1天 → 周一
```

#### 对称差异分析

| 维度 | TO_FRIDAY (提前) | TO_MONDAY (推迟) |
|------|-----------------|-----------------|
| 顺延方向 | **向过去**回退 | **向未来**推迟 |
| 错过原始日 | 周六→周五，交易日期在原始日**之前** | 周六→周一，交易日期在原始日**之后** |
| 跨周问题 | 无，回退到同周周五 | 无，推迟到次周周一 |
| 与 `+2天` 范围的关系 | 周五执行时，范围覆盖到周日，周六可顺延到**已过的**周五 → `handleOccurrence` 中 `$date->ne($this->date)` 会跳过 | 周五执行时，范围覆盖到周日，周六可顺延到**未来的**周一 → 同样被跳过（周一 ≠ 今天周五） |

**关键不对称点**：

- **TO_FRIDAY**：周六的原始执行日顺延到周五。如果 Cron **在周五当天运行**，周五已是今天，可正常创建交易。如果 Cron 在周六才运行，周五已过，`$date->ne($this->date)` 跳过，**交易不会被补建**。
- **TO_MONDAY**：周六的原始执行日顺延到周一。如果 Cron **在周六运行**，周一尚未到来，`$date->ne($this->date)` 跳过。如果 Cron **在周一运行**，周一就是今天，可正常创建交易。

**本质差异**：TO_FRIDAY 的顺延日期在原始日之前，**Cron 必须在顺延日当天（周五）运行才能命中**；TO_MONDAY 的顺延日期在原始日之后，**Cron 在顺延日当天（周一）运行即可命中**。TO_MONDAY 天然更「宽容」，因为顺延日还在未来。

### 2.6 去重处理

在 [FiltersWeekends.php#L90-L94](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/FiltersWeekends.php#L90-L94)：

```php
$collection = new Collection()->push(...$return);
$filtered   = $collection->unique();
$return     = $filtered->toArray();
```

当两个不同的周末日期顺延到同一个工作日时（如周六和周日都 TO_FRIDAY → 同一个周五），`unique()` 会去重，只保留一个。这防止了同一个工作日被创建两次交易。

---

## 三、`force` 绕过四道关卡、`timeBetweenRuns` 与硬编码 `43_200`、`handleRepetitions` 传 `first_date`

### 3.1 `force` 绕过四道关卡的完整追踪

#### 关卡 1：12小时冷却

代码位置：[RecurringCronjob.php#L57-L68](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php#L57-L68)

```php
if ($lastTime > 0 && $diff <= 43_200) {
    if (false === $this->force) {    // ← force=true 时跳过此判断
        return;                       // 不执行
    }
    Log::info('Execution ... has been FORCED.');  // force时仅记录警告
}
```

**绕过方式**：`force=true` 时，即使 `$diff ≤ 43200` 也继续执行 `fireRecurring()`。

#### 关卡 2：`hasFiredToday` 过滤

代码位置：[CreateRecurringTransactions.php#L455-L460](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L455-L460)

```php
if (false === $this->force && $this->hasFiredToday($recurrence)) {
    Log::info(sprintf('Recurrence #%d has already fired today. Skipped.', $recurrence->id));
    return false;
}
```

**绕过方式**：`force=true` 时，`false === $this->force` 为 `false`，整个 `if` 条件为 `false`，不跳过。

#### 关卡 3：`getJournalCount` 当日交易计数

代码位置：[CreateRecurringTransactions.php#L270-L275](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L270-L275)

```php
$journalCount = $this->repository->getJournalCount($recurrence, $date, $date);
if ($journalCount > 0 && false === $this->force) {
    return null;
}
```

**绕过方式**：`force=true` 时，即使 `$journalCount > 0` 也不返回 `null`，而是继续执行。

并且有专门的 force 日志：
```php
if ($journalCount > 0 && $this->force) {
    Log::warning(sprintf('Already created %d groups for date %s but FORCED to continue.', ...));
}
```

#### 关卡 4：`createdPreviously` 标记检查

代码位置：[CreateRecurringTransactions.php#L277-L281](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L277-L281)

```php
if ($this->repository->createdPreviously($recurrence, $date) && false === $this->force) {
    return null;
}
```

**绕过方式**：同上，`force=true` 时 `false === $this->force` 为 `false`。

#### 四道关卡对比

| 关卡 | 层级 | force 绕过方式 | 绕过后行为 |
|------|------|---------------|-----------|
| 12h 冷却 | 调度层 | `if (false === $this->force)` | 直接执行 `fireRecurring()` |
| `hasFiredToday` | 过滤层 | `if (false === $this->force && ...)` | 周期交易不被跳过，进入 handleRepetitions |
| `getJournalCount` | 生成层 | `if (... && false === $this->force)` | **允许同日重复创建交易** |
| `createdPreviously` | 生成层 | `if (... && false === $this->force)` | **允许同日重复创建交易** |

**严重后果**：`force=true` 会绕过关卡 3 和 4，导致**同一周期交易在同一天生成多条重复交易**。这是设计上的权衡——force 用于修复或补偿，使用者需自行承担重复风险。

### 3.2 `timeBetweenRuns` 字段与硬编码 `43_200` 不关联

代码位置：[AbstractCronjob.php#L38](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/AbstractCronjob.php#L38)

```php
public int $timeBetweenRuns = 43_200;
```

而在 [RecurringCronjob.php#L57](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php#L57)：

```php
if ($lastTime > 0 && $diff <= 43_200) {
```

**两者无关联**：
- `AbstractCronjob` 定义了 `$timeBetweenRuns = 43_200`，但**没有任何代码读取此属性**
- `RecurringCronjob::fire()` 中硬编码了 `43_200`，而不是使用 `$this->timeBetweenRuns`
- `timeBetweenRuns` 是一个**死字段**，可能最初设计为可配置间隔，但最终未接入

**证据**：在整个 `RecurringCronjob` 中搜索，`$this->timeBetweenRuns` 从未被引用：

```php
// RecurringCronjob.php 中只有以下逻辑：
$diff <= 43_200    // 硬编码字面量
$diff > 43_200     // 硬编码字面量
// 从未出现 $this->timeBetweenRuns
```

**影响**：如果要修改冷却时间，必须同时修改 `RecurringCronjob::fire()` 中的两处硬编码，`timeBetweenRuns` 的修改不会生效。

### 3.3 `handleRepetitions` 传递 `first_date` 而非 `getStartDate` 的选择

#### 代码现状

代码位置：[CreateRecurringTransactions.php#L369](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L369)

```php
$occurrences = $this->repository->getOccurrencesInRange(
    $repetition, $recurrence->first_date, $includeWeekend
);
```

而 `getStartDate` 的定义在 [CreateRecurringTransactions.php#L190-L198](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L190-L198)：

```php
private function getStartDate(Recurrence $recurrence): Carbon
{
    $startDate = clone $recurrence->first_date;
    if (null !== $recurrence->latest_date && $recurrence->latest_date->gte($startDate)) {
        return clone $recurrence->latest_date;
    }
    return $startDate;
}
```

`getStartDate` 的逻辑：如果有 `latest_date` 且不早于 `first_date`，则返回 `latest_date`，否则返回 `first_date`。

#### 为何选择 `first_date` 而非 `getStartDate`

**1. `handleOccurrence` 中的日期过滤已足够**

在 [CreateRecurringTransactions.php#L263-L266](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L263-L266)：

```php
$date->startOfDay();
if ($date->ne($this->date)) {
    return null;    // 只处理当天的发生日期
}
```

无论 `getOccurrencesInRange` 的范围多大，最终只有 `$date === today` 的发生日期才会生成交易。因此从 `first_date` 开始计算不会导致多余的创建——过去的日期在 `handleOccurrence` 中会被 `ne($this->date)` 过滤掉。

**2. 如果使用 `getStartDate`（即 `latest_date`），会丢失中间未执行的发生日期**

假设一个月度周期：
- `first_date` = 2024-01-15
- `latest_date` = 2024-03-15（上次执行日期）
- 今天 = 2024-06-13

若使用 `getStartDate`，计算范围是 `2024-03-15 ~ 2024-06-15`，可以正确计算出 4/15、5/15、6/15 三个日期。

若使用 `first_date`，计算范围是 `2024-01-15 ~ 2024-06-15`，会计算出 1/15、2/15、3/15、4/15、5/15、6/15 六个日期。

但由于 `handleOccurrence` 只处理 `date == today` 的日期，**两种方案的结果完全一致**——都只会创建 6/15 的交易。

**3. 但 `first_date` 方案有一个隐含风险**

当 `force=true` 时，`handleOccurrence` 仍然只处理 `date == today`，所以不会重复。然而，如果 `first_date` 距离今天非常久远（如 5 年前），`getOccurrencesInRange` 需要遍历从 5 年前到今天的所有日期，可能造成**性能问题**。使用 `getStartDate` 可以缩小遍历范围。

**4. `getStartDate` 在哪里被使用？**

`getStartDate` 实际上只在 `hasNotStartedYet` 中被调用 [CreateRecurringTransactions.php#L391-L398](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L391-L398)：

```php
private function hasNotStartedYet(Recurrence $recurrence): bool
{
    $startDate = $this->getStartDate($recurrence);
    return $startDate->gt($this->date);
}
```

用于判断周期交易是否已经开始。此处 `getStartDate` 返回 `latest_date`（如果有）是合理的——如果上次执行日期在未来，说明周期交易尚未实际开始。

**5. 设计选择总结**

| 方案 | 优点 | 缺点 |
|------|------|------|
| `first_date` | 确保不遗漏任何历史发生日期；逻辑简单 | 长期周期交易性能较差 |
| `getStartDate` | 缩小计算范围，性能更优 | 理论上可能遗漏 `latest_date` 之前未执行的发生日期 |

当前代码选择了 `first_date`，属于「宁可多算不可遗漏」的保守策略。配合 `handleOccurrence` 的 `date == today` 过滤，实际结果等价但更安全。

---

## 四、关键发现汇总

### 4.1 `last_rt_job` 写入的不一致性

- CLI 无 `--date` 时：写入零点时间戳 → 冷却比较基于零点
- CLI 有 `--date` 或 API 有 date 参数时：写入可能非零点时间戳 → 冷却比较基于具体时刻
- 这导致非零点写入后，冷却判断从「天数语义」退化为「时刻语义」

### 4.2 `SKIP_CREATION` 的静默丢弃

- 不像其他三个档位有显式分支，`SKIP_CREATION` 的行为是「不匹配任何分支导致不入栈」
- 如果所有发生日期都落在周末且配置了 `SKIP_CREATION`，周期交易将永远不会产生任何交易
- 没有 Log 输出提示用户周末日期被跳过

### 4.3 `TO_FRIDAY` vs `TO_MONDAY` 的方向性不对称

- TO_FRIDAY 顺延到过去（已过的周五），Cron 必须在顺延日当天运行
- TO_MONDAY 顺延到未来（尚未到的周一），Cron 在顺延日运行即可
- TO_FRIDAY 在「周末后的首个工作日运行」时会**错过**交易（周五已过）
- TO_MONDAY 在「周末后的首个工作日运行」时会**命中**交易（周一就是今天）

### 4.4 `force` 的全局破坏性

- `force=true` 绕过全部四道关卡，**直接导致可能创建重复交易**
- 没有任何机制阻止 `force` 模式下的重复（如事务去重或唯一约束）
- 使用者必须在 force 执行后手动清理重复交易

### 4.5 `timeBetweenRuns` 是死字段

- 定义在 `AbstractCronjob` 中但从未被读取
- 实际冷却阈值通过 `43_200` 硬编码在 `RecurringCronjob::fire()` 中
- 修改 `timeBetweenRuns` 不会影响任何行为

### 4.6 `first_date` vs `getStartDate` 的保守选择

- `handleRepetitions` 使用 `first_date` 作为计算起点
- `getStartDate`（优先返回 `latest_date`）仅在 `hasNotStartedYet` 中使用
- 两者配合 `handleOccurrence` 的 `date == today` 过滤，结果等价
- 但 `first_date` 方案在长期周期交易下有性能隐患

---

## 五、相关核心文件索引

| 文件 | 分析要点 |
|------|---------|
| [Cron.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Console/Commands/Tools/Cron.php#L211-L231) | CLI 入口，`$date` 可能为 null，条件调用 `setDate` |
| [Api\CronController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Api/V1/Controllers/System/CronController.php#L45-L61) | API 入口，始终调用 `runRecurring($force, $date)` |
| [CronRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Api/V1/Requests/System/CronRequest.php#L49-L64) | API 请求参数解析，默认 `today()`，保证非空 |
| [CronRunner.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Http/Controllers/CronRunner.php#L103-L122) | API 调用路径，始终调用 `setDate` |
| [AbstractCronjob.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/AbstractCronjob.php#L38-L56) | `timeBetweenRuns` 死字段、`setDate` 不做 startOfDay |
| [RecurringCronjob.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php#L43-L97) | 硬编码 43200、force 绕过冷却、`last_rt_job` 写入 |
| [CreateRecurringTransactions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L71-L93) | Job 构造器强制 startOfDay、force 绕过三道关卡 |
| [FiltersWeekends.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/FiltersWeekends.php#L41-L99) | 四档位分支、SKIP_CREATION 静默丢弃、unique 去重 |
| [RecurrenceRepetitionWeekend.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Enums/RecurrenceRepetitionWeekend.php#L30-L36) | 枚举定义：DO_NOTHING=1, SKIP=2, TO_FRIDAY=3, TO_MONDAY=4 |
