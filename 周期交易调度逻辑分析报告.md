# Firefly III 周期交易调度逻辑分析报告

## 一、整体调度架构概览

周期交易（Recurring Transactions）是 Firefly III 中用于自动生成预定计划交易的核心功能。其调度流程采用「**外部 Cron 触发 → 命令入口 → 任务调度器 → 异步 Job 执行**」的四层架构设计。

```
系统 Cron 定时器
        ↓ (定时调用)
[Cron.php] 命令入口
        ↓ (分发给各子任务)
[RecurringCronjob.php] 周期交易调度器
        ↓ (创建并执行 Job)
[CreateRecurringTransactions.php] 实际交易生成 Job
        ↓ (计算、过滤、生成)
[RecurringRepository.php] + [计算 Trait] 核心业务逻辑
        ↓
实际交易记录 (TransactionGroup)
```

---

## 二、调度器唤醒机制

### 2.1 第一层：外部 Cron 触发

Firefly III 自身不包含轮询机制，必须依赖操作系统的 Cron 服务来唤醒。官方文档要求用户配置类似如下的 Cron 任务：

```bash
# 每分钟调用一次 Firefly III 的 Cron 命令
* * * * * php /var/www/html/artisan firefly-iii:cron
```

### 2.2 第二层：命令入口 [Cron.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Console/Commands/Tools/Cron.php#L1-L279)

**核心入口方法：`handle()`** [Cron.php#L58-L144](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Console/Commands/Tools/Cron.php#L58-L144)

```php
public function handle(): int
{
    // 判断是否执行所有任务（没有指定单个任务时）
    $doAll = !$this->option('download-cer') 
           && !$this->option('create-recurring')
           && ...;
    
    // 依次调用各 cron 任务
    if ($doAll || $this->option('create-recurring')) {
        $this->recurringCronJob($force, $date);  // 调用周期交易任务
    }
    // ... 其他 cron 任务
}
```

**调度入口方法：`recurringCronJob()`** [Cron.php#L211-L231](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Console/Commands/Tools/Cron.php#L211-L231)

```php
private function recurringCronJob(bool $force, ?Carbon $date): void
{
    $recurring = new RecurringCronjob();
    $recurring->setForce($force);
    if ($date instanceof Carbon) {
        $recurring->setDate($date);
    }
    $recurring->fire();  // 触发调度器
    // 处理结果反馈...
}
```

**设计要点：**
- 支持 `--force` 参数强制执行（忽略防重复机制）
- 支持 `--date` 参数模拟特定日期执行（用于调试）
- 支持 `--create-recurring` 单独执行周期交易任务

---

### 2.3 第三层：周期交易调度器 [RecurringCronjob.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php#L1-L98)

**核心方法：`fire()`** [RecurringCronjob.php#L43-L78](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php#L43-L78)

```php
public function fire(): void
{
    // 读取上次执行时间
    $config = FireflyConfig::get('last_rt_job', 0);
    $lastTime = (int) $config->data;
    $diff = now()->getTimestamp() - $lastTime;
    
    // 防重复执行：距离上次执行不足 12 小时（43200秒）时，不执行
    if ($lastTime > 0 && $diff <= 43_200) {
        if (false === $this->force) {
            $this->message = 'It has been "%s" since the last run. Will not fire now.';
            return;
        }
    }
    
    // 执行实际生成逻辑
    $this->fireRecurring();
}
```

**实际执行方法：`fireRecurring()`** [RecurringCronjob.php#L80-L97](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php#L80-L97)

```php
private function fireRecurring(): void
{
    // 创建 Job 实例并同步执行
    $job = new CreateRecurringTransactions($this->date);
    $job->setForce($this->force);
    $job->handle();  // 注意：此处直接调用 handle()，而非 dispatch() 到队列
    
    // 记录本次执行时间，用于防重复判断
    FireflyConfig::set('last_rt_job', (int) $this->date->format('U'));
}
```

**调度关键机制：**
- **12 小时冷却机制**：正常情况下每 12 小时最多执行一次（`43_200` 秒）
- **配置持久化**：`last_rt_job` 存储在 `configuration` 表中，防止重复执行
- **Force 模式**：`--force` 参数可跳过冷却检查，强制执行
- **同步执行**：直接调用 `$job->handle()` 而非队列分发，确保 Cron 调用时立即执行

---

## 三、周期交易有效性过滤机制

### 3.1 Job 入口：[CreateRecurringTransactions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L1-L465)

**核心执行流程：`handle()`** [CreateRecurringTransactions.php#L103-L154](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L103-L154)

```php
public function handle(): void
{
    // 1. 获取所有周期交易
    $this->recurrences = $this->repository->getAll();
    
    // 2. 过滤有效的周期交易
    $filtered = $this->filterRecurrences($this->recurrences);
    
    // 3. 逐个处理
    foreach ($filtered as $recurrence) {
        $createdReps = $this->handleRepetitions($recurrence);
    }
    
    // 4. 发送报告邮件
    event(new TransactionGroupsRequestedReporting($userId, $journals));
}
```

### 3.2 有效性过滤：`validRecurrence()` [CreateRecurringTransactions.php#L412-L464](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L412-L464)

共 5 层过滤判断，必须全部通过才会继续处理：

| 检查项 | 代码位置 | 判断逻辑 |
|--------|---------|---------|
| **是否激活** | [L416-L420](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L416-L420) | `$recurrence->active` 必须为 `true` |
| **次数限制** | [L423-L428](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L423-L428) | 已创建次数 `<` 设定的最大次数 `$recurrence->repetitions` |
| **截止日期** | [L432-L441](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L432-L441) | `repeat_until` 日期未过 |
| **是否已开始** | [L444-L453](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L444-L453) | `first_date` 或 `latest_date` 早于等于今天 |
| **今日是否已执行** | [L456-L460](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L456-L460) | `latest_date` 不等于今天 |

> **注意**：第 2、5 项检查可被 `--force` 参数跳过。

---

## 四、周期发生日期计算逻辑

### 4.1 计算入口：`getOccurrencesInRange()` [RecurringRepository.php#L259-L286](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Repositories/Recurring/RecurringRepository.php#L259-L286)

```php
public function getOccurrencesInRange(RecurrenceRepetition $repetition, Carbon $start, Carbon $end): array
{
    $skipMod = $repetition->repetition_skip + 1;  // 跳过因子：skip=0 表示每次都执行
    
    // 根据重复类型分派到不同计算方法
    switch ($repetition->repetition_type) {
        case 'daily':   $occurrences = $this->getDailyInRange(...);   break;
        case 'weekly':  $occurrences = $this->getWeeklyInRange(...);  break;
        case 'monthly': $occurrences = $this->getMonthlyInRange(...); break;
        case 'ndom':    $occurrences = $this->getNdomInRange(...);    break;
        case 'yearly':  $occurrences = $this->getYearlyInRange(...);  break;
    }
    
    // 应用周末调整规则
    return $this->filterWeekends($repetition, $occurrences);
}
```

**调用时机**：在 `handleRepetitions()` 中被调用 [CreateRecurringTransactions.php#L352-L378](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L352-L378)

```php
private function handleRepetitions(Recurrence $recurrence): Collection
{
    foreach ($recurrence->recurrenceRepetitions as $repetition) {
        // 计算范围：从首次日期 到 今天+2天（包含周末缓冲）
        $includeWeekend = clone $this->date;
        $includeWeekend->addDays(2);
        $occurrences = $this->repository->getOccurrencesInRange(
            $repetition, $recurrence->first_date, $includeWeekend
        );
        // 处理每次发生...
    }
}
```

### 4.2 支持的周期类型

使用 [CalculateRangeOccurrences](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/CalculateRangeOccurrences.php) Trait 实现 5 种周期计算：

| 周期类型 | `repetition_type` | `repetition_moment` 格式 | 说明 |
|---------|------------------|--------------------------|------|
| 每日 | `daily` | 忽略 | 每天执行 |
| 每周 | `weekly` | `1-7` (周一到周日) | 指定星期几执行 |
| 每月 | `monthly` | `1-31` | 指定某月某日执行 |
| 月内星期 | `ndom` | `n,dow` (如 `1,1` 表示第1个周一) | 第 N 个星期几执行 |
| 每年 | `yearly` | `YYYY-MM-DD` | 每年的指定日期 |

**核心算法示例（每周）**：[CalculateRangeOccurrences.php#L110-L141](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/CalculateRangeOccurrences.php#L110-L141)

```php
protected function getWeeklyInRange(Carbon $start, Carbon $end, int $skipMod, string $moment): array
{
    $dayOfWeek = (int) $moment;
    
    // 1. 如果当前日期的星期已过目标星期，跳到下一周
    if ($start->dayOfWeekIso > $dayOfWeek) {
        $start->addWeek();
    }
    
    // 2. 调整到目标星期几
    $dayDifference = $dayOfWeek - $start->dayOfWeekIso;
    $start->addDays($dayDifference);
    
    // 3. 循环生成所有日期（考虑 skip 因子）
    while ($start <= $end) {
        if (0 === ($attempts % $skipMod)) {
            $return[] = clone $start;
        }
        $start->addWeek();
    }
}
```

### 4.3 跳过机制（Skip）

`repetition_skip` 字段控制跳过频率：
- `skip = 0`：每次都执行（`skipMod = 1`）
- `skip = 1`：每 2 次执行 1 次（`skipMod = 2`）
- `skip = N`：每 N+1 次执行 1 次

核心判断：`if (0 === ($attempts % $skipMod))` 当尝试次数能被 `skipMod` 整除时才记录。

### 4.4 周末调整规则

使用 [FiltersWeekends](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/FiltersWeekends.php) Trait 处理周末：

| 规则 | `weekend` 字段值 | 处理方式 |
|------|-----------------|----------|
| 不处理 | `1` | 周末日期保持不变 |
| 提前到周五 | `2` | 周六/周日 → 当周周五 |
| 推迟到周一 | `3` | 周六/周日 → 下周周一 |

算法示例 [FiltersWeekends.php#L62-L85](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/FiltersWeekends.php#L62-L85)：
```php
// 提前到周五（周六=6 → +5-6 = -1天；周日=7 → +5-7 = -2天）
$clone->addDays(5 - $date->dayOfWeekIso);

// 推迟到周一（周六=6 → +8-6 = +2天；周日=7 → +8-7 = +1天）
$clone->addDays(8 - $date->dayOfWeekIso);
```

---

## 五、实际交易生成流程

### 5.1 单次发生处理：`handleOccurrence()` [CreateRecurringTransactions.php#L261-L322](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L261-L322)

```php
private function handleOccurrence(Recurrence $recurrence, RecurrenceRepetition $repetition, Carbon $date): ?TransactionGroup
{
    // 1. 只处理当天的交易（即使计算出了未来日期也不生成）
    $date->startOfDay();
    if ($date->ne($this->date)) {
        return null;
    }
    
    // 2. 防重复检查1：今天是否已创建过该周期的交易
    $journalCount = $this->repository->getJournalCount($recurrence, $date, $date);
    if ($journalCount > 0 && false === $this->force) {
        return null;
    }
    
    // 3. 防重复检查2：是否已有标记为该 recurrence_date 的交易
    if ($this->repository->createdPreviously($recurrence, $date) && false === $this->force) {
        return null;
    }
    
    // 4. 组装交易数据
    $array = [
        'user' => $recurrence->user,
        'group_title' => $count > 1 ? $recurrence->title : null,
        'transactions' => $this->getTransactionData($recurrence, $repetition, $date),
    ];
    
    // 5. 创建交易组
    $group = $this->groupRepository->store($array);
    
    // 6. 更新周期交易的 latest_date（防止下次重复）
    $this->repository->setLatestDate($recurrence, $date);
    
    return $group;
}
```

### 5.2 交易数据组装：`getTransactionData()` [CreateRecurringTransactions.php#L203-L255](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L203-L255)

从周期交易模板中提取数据，生成实际交易的完整字段：

```php
$single = [
    'type' => strtolower($recurrence->transactionType->type),  // 交易类型
    'date' => $date,                                           // 交易日期
    'amount' => $transaction->amount,                          // 金额
    'description' => $transaction->description,                // 描述
    'source_id' => $transaction->source_id,                    // 源账户
    'destination_id' => $transaction->destination_id,          // 目标账户
    'category_id' => $this->repository->getCategoryId(...),    // 分类
    'budget_id' => $this->repository->getBudget(...),          // 预算
    'tags' => $this->repository->getTags(...),                 // 标签
    'recurrence_id' => $recurrence->id,                        // 关联的周期交易ID
    'recurrence_total' => $total,                              // 总预期次数
    'recurrence_count' => $count,                              // 当前是第几次
    'notes' => trans('firefly.created_from_recurrence', ...),  // 自动备注
];
```

---

## 六、关键防重复机制汇总

| 层级 | 检查点 | 位置 | 跳过条件 |
|------|--------|------|----------|
| 调度层 | 12小时冷却 | [RecurringCronjob.php#L57-L67](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php#L57-L67) | `--force` |
| 过滤层 | 今日已执行（`latest_date`） | [CreateRecurringTransactions.php#L456-L460](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L456-L460) | `--force` |
| 生成层 | 当日已创建交易计数 | [CreateRecurringTransactions.php#L270-L275](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L270-L275) | `--force` |
| 生成层 | Meta 表中 `recurrence_date` 标记 | [CreateRecurringTransactions.php#L277-L281](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php#L277-L281) | `--force` |

---

## 七、核心数据模型字段说明

### `recurrences` 表（周期交易主表）

| 字段 | 用途 |
|------|------|
| `active` | 是否激活 |
| `first_date` | 首次执行日期 |
| `latest_date` | 最近一次成功执行日期（关键，用于防重复） |
| `repeat_until` | 截止日期（可为 null） |
| `repetitions` | 最大执行次数（0 表示不限制） |
| `transaction_type_id` | 交易类型（提款/转账/存款） |

### `recurrence_repetitions` 表（周期规则表）

| 字段 | 用途 |
|------|------|
| `repetition_type` | 周期类型：daily/weekly/monthly/ndom/yearly |
| `repetition_moment` | 周期参数（如每周几、每月几号） |
| `repetition_skip` | 跳过次数（0=不跳过，1=隔一次） |
| `weekend` | 周末处理规则（1=不处理，2=提前到周五，3=推迟到周一） |

---

## 八、执行时序总结

```
1. 操作系统 Cron 每分钟调用:
   php artisan firefly-iii:cron
   ↓
2. Cron.php 判断是否需要执行周期交易任务
   ↓
3. RecurringCronjob::fire() 检查:
   ├─ 读取 last_rt_job（上次执行时间）
   └─ 距上次 >12小时 或 --force → 继续
   ↓
4. CreateRecurringTransactions Job 执行:
   ├─ 获取所有周期交易
   ├─ 5层过滤（激活、次数、截止、开始、今日未执行）
   └─ 对每个有效周期:
      ├─ 计算日期范围内的所有发生日期
      ├─ 应用周末调整规则
      └─ 对每个发生日期 == 今天:
         ├─ 两层防重复检查
         ├─ 组装交易数据
         ├─ 创建 TransactionGroup
         └─ 更新 recurrence.latest_date
   ↓
5. 更新 last_rt_job 为当前时间
   ↓
6. 发送报告邮件给用户
```

---

## 九、设计亮点与优化点

### 设计亮点
1. **多层防重复机制**：从调度层到生成层共 4 道关卡，确保不会重复生成交易
2. **周末智能调整**：支持提前到周五/推迟到周一，符合实际业务场景
3. **灵活的周期类型**：5 种周期类型 + skip 机制，满足绝大多数定期交易需求
4. **可测试性**：支持 `--date` 参数模拟任意日期，便于测试和调试
5. **幂等设计**：无论 Cron 调用多少次，一天内最多生成一次交易

### 潜在优化点
1. `RecurringCronjob::fireRecurring()` 中直接调用 `$job->handle()` 而非 `dispatch()`，若配置了队列系统，可改为异步执行提高响应速度
2. 日期计算使用了 Carbon 的可变对象（mutable），多处 `clone` 操作增加了复杂度

---

## 十、相关核心文件索引

| 文件 | 职责 |
|------|------|
| [Cron.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Console/Commands/Tools/Cron.php) | Artisan 命令入口，调度所有 Cron 任务 |
| [RecurringCronjob.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/RecurringCronjob.php) | 周期交易调度器，12小时冷却检查 |
| [CreateRecurringTransactions.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Jobs/CreateRecurringTransactions.php) | 核心 Job，过滤、计算、生成实际交易 |
| [RecurringRepository.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Repositories/Recurring/RecurringRepository.php) | 周期交易数据访问层，业务逻辑封装 |
| [CalculateRangeOccurrences.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/CalculateRangeOccurrences.php) | Trait：日期范围内周期计算 |
| [CalculateXOccurrences.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/CalculateXOccurrences.php) | Trait：未来 N 次周期计算 |
| [FiltersWeekends.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Repositories/Recurring/FiltersWeekends.php) | Trait：周末日期调整逻辑 |
| [AbstractCronjob.php](file:///d:/fz/0601-1/solo-dogfeeding/code/52-firefly-iii/app/Support/Cronjobs/AbstractCronjob.php) | Cron 任务抽象基类 |
