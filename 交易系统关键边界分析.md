# Firefly III 交易系统关键边界深度分析

## 一、业务规则在验证器与工厂中的执行顺序及职责分工

### 1.1 整体执行流程概览

```
用户请求
    ↓
[请求层] StoreRequest::rules() - 基础格式验证
    ↓
[请求层] StoreRequest::withValidator() - 业务规则验证
    │
    ├─ validateTransactionArray()        - 数组格式
    ├─ validateOneTransaction()          - 至少一笔交易
    ├─ validateDescriptions()            - 描述非空
    ├─ validateTransactionTypes()        - 交易类型一致性
    ├─ validateForeignCurrencyInformation() - 外币信息
    ├─ validateAccountInformation()      - 账户合法性
    │   └─ AccountValidator::validateSource()
    │   └─ AccountValidator::validateDestination()
    ├─ validateEqualAccounts()           - 拆分交易账户一致性
    └─ validateGroupDescription()        - 拆分交易组标题
    ↓
[仓库层] TransactionGroupRepository::store()
    ↓
[工厂层] TransactionGroupFactory::create()
    ↓
[工厂层] TransactionJournalFactory::create()
    ↓
[工厂层] TransactionJournalFactory::createJournal()
    │
    ├─ 重复哈希检查
    ├─ $this->validateAccounts()         - 工厂内账户验证
    │   └─ AccountValidator::validateSource()
    │   └─ AccountValidator::validateDestination()
    ├─ $this->getAccount()               - 获取/创建账户
    │   ├─ findAccountById()
    │   ├─ findAccountByIban()
    │   ├─ findAccountByNumber()
    │   ├─ findAccountByName()
    │   └─ createAccount()               - 动态创建账户
    ├─ TransactionJournal::create()     - 创建日记账
    ├─ TransactionFactory::createNegative() - 创建借方交易
    ├─ TransactionFactory::createPositive() - 创建贷方交易
    └─ 关联元数据（预算/分类/标签/备注）
```

### 1.2 验证器（Validator）职责

**核心职责：前置验证，拒绝无效输入**

验证器层的所有检查都是**防御性的**，目的是在数据进入业务逻辑前拦截无效输入。

| 验证方法 | 所在文件 | 验证内容 | 失败处理 |
|---------|---------|---------|---------|
| `validateTransactionArray()` | `TransactionValidation.php:142` | 交易数组必须是数字索引数组 | 添加错误，终止后续验证 |
| `validateOneTransaction()` | `TransactionValidation.php:123` | 至少提交一笔交易 | 添加错误，终止后续验证 |
| `validateTransactionTypes()` | `TransactionValidation.php:161` | 所有拆分交易类型必须一致 | 添加错误，终止后续验证 |
| `validateDescriptions()` | `GroupValidation.php:143` | 至少有一个描述 | 添加错误，终止后续验证 |
| `validateEqualAccounts()` | `TransactionValidation.php:700` | 拆分交易的源/目标账户一致性 | 添加错误，终止后续验证 |
| `validateGroupDescription()` | `GroupValidation.php:166` | 多笔拆分交易必须有组标题 | 添加错误，终止后续验证 |
| `validateAccountInformation()` | `TransactionValidation.php:52` | 账户类型与交易类型匹配 | 添加错误，终止后续验证 |
| `validateForeignCurrencyInformation()` | `CurrencyValidation.php` | 外币金额与币种匹配 | 添加错误，终止后续验证 |

**AccountValidator 的具体验证逻辑：**

`app/Validation/AccountValidator.php:167` - `validateSource()`
```php
switch ($this->transactionType) {
    case TransactionTypeEnum::WITHDRAWAL->value:
        $result = $this->validateWithdrawalSource($array);
        break;
    case TransactionTypeEnum::DEPOSIT->value:
        $result = $this->validateDepositSource($array);
        break;
    case TransactionTypeEnum::TRANSFER->value:
        $result = $this->validateTransferSource($array);
        break;
    // ...
}
```

**验证器的特点：**
1. **只读操作**：不修改任何数据，只做检查
2. **快速失败**：任何验证失败立即返回，不继续执行
3. **无副作用**：不会创建、修改或删除任何数据库记录
4. **用户友好**：返回具体的错误消息，指导用户修正输入
5. **不假设数据存在**：所有验证都是基于输入数据本身

### 1.3 工厂（Factory）职责

**核心职责：数据持久化，处理复杂业务逻辑**

工厂层在验证通过后执行，负责实际的数据创建和复杂业务规则处理。

| 方法 | 所在文件 | 核心逻辑 | 与验证器的区别 |
|-----|---------|---------|-------------|
| `TransactionJournalFactory::validateAccounts()` | `TransactionJournalFactory.php:624` | 再次验证账户合法性 | 验证失败抛出异常，上层捕获后直接 return null，**完全中断**创建流程 |
| `JournalServiceTrait::getAccount()` | `JournalServiceTrait.php:59` | 按优先级查找账户（ID→IBAN→卡号→名称） | 验证器只检查是否存在，工厂实际获取账户对象 |
| `JournalServiceTrait::createAccount()` | `JournalServiceTrait.php:243` | 动态创建不存在的账户 | 验证器从不创建账户，工厂会根据需要创建 |
| `TransactionFactory::createNegative()` | `TransactionFactory.php:56` | 创建借方交易（负数金额） | 验证器不涉及数据持久化 |
| `TransactionFactory::createPositive()` | `TransactionFactory.php:73` | 创建贷方交易（正数金额） | 验证器不涉及数据持久化 |

**工厂内的二次验证逻辑：**

`TransactionJournalFactory.php:259-267`
```php
try {
    // 再次验证源账户和目标账户
    $this->validateAccounts($row);
} catch (FireflyException $e) {
    Log::error('Could not validate source or destination.');
    Log::error($e->getMessage());
    return null;  // 直接中断，不会继续后续的 getAccount() 和现金兜底
}
```

**`validateAccounts()` 内部实现：**

`TransactionJournalFactory.php:624-659`
```php
private function validateAccounts(NullArrayObject $data): void
{
    $validSource = $this->accountValidator->validateSource($array);
    if (false === $validSource) {
        throw new FireflyException(sprintf('Source: %s', $this->accountValidator->sourceError));
    }

    $validDestination = $this->accountValidator->validateDestination($array);
    if (false === $validDestination) {
        throw new FireflyException(sprintf('Destination: %s', $this->accountValidator->destError));
    }
}
```

**账户获取/创建的完整流程：**

`JournalServiceTrait.php:59-114` - `getAccount()`
```php
protected function getAccount(string $transactionType, string $direction, array $data, ?Account $opposite = null): ?Account
{
    // 1. 按优先级查找现有账户
    $result = $this->findAccountById($data, $expectedTypes, $opposite);
    $result = $this->findAccountByIban($result, $data, $expectedTypes, $opposite);
    $result = $this->findAccountByNumber($result, $data, $expectedTypes, $opposite);
    $result = $this->findAccountByName($result, $data, $expectedTypes, $opposite);

    // 2. 如果没找到但允许创建，则创建新账户
    $creatableType = $this->getCreatableType($expectedTypes);
    if (null === $result && null !== $creatableType) {
        $result = $this->createAccount($result, $data, $creatableType);
    }

    // 3. 如果还没找到，尝试返回现金账户（有条件）
    if (null === $result) {
        $result = $this->getCashAccount($result, $data, $expectedTypes);
    }

    return $result;
}
```

**`getCashAccount()` 的条件限制：**

`JournalServiceTrait.php:439-448`
```php
private function getCashAccount(?Account $account, array $data, array $types): ?Account
{
    // 只有当 name 为空 且 允许现金账户类型时，才返回现金账户
    if (!$account instanceof Account && '' === (string) $data['name'] && in_array(AccountTypeEnum::CASH->value, $types, true)) {
        $account = $this->accountRepository->getCashAccount();
    }
    return $account;
}
```

### 1.4 同一规则的双重验证分析

以**账户验证**为例，同一规则在验证器和工厂中都有执行，但**失败处理路径完全不同**：

**验证器层验证（AccountValidator）：**
- **目的**：确保用户输入的账户信息是有效的
- **检查内容**：
  - 账户是否存在
  - 账户类型是否符合交易类型要求
  - 转账时源账户和目标账户不能相同
- **失败处理**：返回错误消息给用户，要求修正

**工厂层验证（TransactionJournalFactory::validateAccounts）：**
- **目的**：防御性编程，防止验证器被绕过
- **检查内容**：与验证器完全相同的检查逻辑
- **失败处理**：
  - 抛出 `FireflyException` 异常
  - 上层捕获后直接 `return null`，**完全中断**当前日记账的创建
  - **不会**进入后续的 `getAccount()` 逻辑
  - **不会**触发现金账户兜底

**现金账户兜底的触发场景（只有这两处）：**

| 兜底位置 | 触发条件 | 适用范围 |
|---------|---------|---------|
| `getAccount()` 内部 `getCashAccount()` | 账户找不到 + 无法创建 + **name 为空** + 允许 CASH 类型 | 源账户和目标账户都有可能 |
| `TransactionJournalFactory.php:305-308` | `getAccount()` 返回 null（即 name 不为空导致上面的兜底没生效） | **仅 destinationAccount**，sourceAccount 没有此兜底 |

```php
// 只有 destinationAccount 有无条件兜底！sourceAccount 没有！
if (!$destinationAccount instanceof Account) {
    Log::warning('Destination account is NULL, will replace with cash account.');
    $destinationAccount = $this->accountRepository->getCashAccount();
}
```

**设计意图：**
1. **分层防御**：即使上层验证被绕过（如直接调用 API、代码缺陷），工厂层的 `validateAccounts()` 仍能拦截无效数据
2. **中断优先**：账户验证是核心业务规则，失败时直接中断，不做兜底
3. **有限兜底**：只有在账户验证通过但实际获取失败时（极罕见场景），才对 destinationAccount 做现金账户兜底
4. **不对称设计**：sourceAccount 没有兜底，因为源账户的合法性对交易语义更关键（支出的钱从哪来？）

### 1.5 工厂层二次校验与账户获取的执行分支链

工厂层 `createJournal()` 方法内的完整执行顺序和分支条件：

```
TransactionJournalFactory::createJournal() 入口
    ↓
┌─ 重复哈希检查（errorIfDuplicate）
│  ├─ 重复 → 抛出 DuplicateTransactionException
│  └─ 不重复 → 继续
    ↓
├─ 交易类型、币种、日期等基础字段解析
    ↓
├─ 二次账户验证（validateAccounts）
│  ├─ 调用 AccountValidator::validateSource()
│  │   ├─ Withdrawal/Transfer → 必须找到现有账户（findExistingAccount）
│  │   │   ├─ 找不到 → 返回 false → 抛出 FireflyException → return null ✘ 中断
│  │   │   └─ 找到 → 验证通过 ✓
│  │   └─ Deposit → 找到账户 OR 可创建 REVENUE 账户
│  │       ├─ 都不满足 → 返回 false → 抛出 FireflyException → return null ✘ 中断
│  │       └─ 满足其一 → 验证通过 ✓
│  ├─ 调用 AccountValidator::validateDestination()
│  │   └─ 失败 → 抛出 FireflyException → return null ✘ 中断
│  └─ 全部验证通过 → 继续
    ↓
├─ [关键并发窗口起点] 构建 sourceInfo / destInfo
│  （使用用户提交的原始 ID/name/iban/number，不使用验证器找到的账户）
    ↓
├─ 根据交易类型执行 getAccount()
│  ├─ Deposit → 先 destination 后 source
│  │   ├─ getAccount(destination) → 失败 → null
│  │   │   └─ 后续无条件兜底为现金账户 ✓
│  │   └─ getAccount(source) → 失败 → null
│  │       └─ CASH 在 expected_types 中 → getCashAccount() 返回 ✓
│  ├─ Withdrawal/Transfer → 先 source 后 destination
│  │   ├─ getAccount(source) → 失败 → null
│  │   │   └─ CASH 不在 expected_types 中 → 无法兜底 ✘ 风险点
│  │   └─ getAccount(destination) → 失败 → null
│  │       └─ 后续无条件兜底为现金账户 ✓
│  └─ Reconciliation → 特殊流程
│      └─ 后续 reconciliationSanityCheck() 兜底 ✓
    ↓
├─ destinationAccount null 检查（无条件兜底为现金账户）
    ↓
├─ reconciliationSanityCheck()（仅对账交易）
    ↓
└─ [关键并发窗口终点] 开始创建 TransactionJournal
```

**关键发现：**
- `validateAccounts()` 找到的账户对象**没有被复用**，`getAccount()` 会**再次**按 ID/name/iban 独立查找
- 两次查找之间存在时间差，这是并发风险的根源
- 验证器校验通过只代表"提交的数据是有效的"，不代表"账户在后续获取时一定存在"

### 1.6 并发可达性窗口分析

**极窄并发窗口**：从 `validateAccounts()` 验证通过，到 `getAccount()` 完成查找，之间的时间窗口。

**窗口内的操作**：
1. 构建 `sourceInfo` / `destInfo` 数组（纯内存操作，耗时 < 1ms）
2. 调用 `getAccount()` 执行数据库查询（耗时 ~ 1-10ms）

**总窗口大小**：约 1-11ms，仅在这个窗口内删除账户才会触发问题

#### 按交易类型的分支分析

**1. Withdrawal（支出）**

```
validateWithdrawalSource() 通过
    ↓ （必须找到账户）
    ↓ 【并发窗口】账户可能在此时被删除
    ↓
getAccount(source) 执行
    ├─ findAccountById() → 账户已删除 → null
    ├─ findAccountByIban() → null
    ├─ findAccountByNumber() → null
    ├─ findAccountByName() → null
    ├─ creatableType = null（ASSET 不可创建）
    └─ getCashAccount() → CASH 不在 expected_types → null
    ↓
sourceAccount = null ✘
    ↓
destinationAccount 兜底为现金账户 ✓
    ↓
后续代码使用 null 的 sourceAccount → 类型错误
```

**触发前提（必须同时满足）：**
- ✅ 验证器调用 `findExistingAccount()` 时账户存在
- ✅ 在验证通过后 ~11ms 内，该账户被并发删除
- ✅ 用户通过 ID 而不是 name 引用账户（name 可能匹配到其他账户）
- ✅ 没有其他账户具有相同的 IBAN/number/name（否则会匹配到其他账户）

**2. Transfer（转账）**

与 Withdrawal 完全相同的分支链。

**3. Deposit（收入）**

```
validateDepositSource() 通过
    ↓ （找到账户 OR 可创建 REVENUE）
    ↓ 【并发窗口】账户可能在此时被删除
    ↓
getAccount(source) 执行
    ├─ findAccountById() → 账户已删除 → null
    ├─ findAccountByIban() → null
    ├─ findAccountByNumber() → null
    ├─ findAccountByName() → null
    ├─ creatableType = REVENUE（可创建）
    │   ├─ name 不为空 → createAccount() 创建新 REVENUE 账户 ✓
    │   └─ name 为空 → createAccount() 返回 null
    │       └─ getCashAccount() → CASH 在 expected_types 中 → 返回现金账户 ✓
    └─ 所有路径都不会返回 null ✓
    ↓
sourceAccount 永远不为 null ✓
```

**Deposit 永远安全的原因：**
- expected_types 包含 CASH → `getCashAccount()` 可以兜底
- 可创建 REVENUE 账户 → 即使 name 不为空也能创建新账户

**4. Reconciliation（对账）**

```
验证通过
    ↓
getAccount() 可能返回 null
    ↓
reconciliationSanityCheck() 兜底
    ├─ sourceAccount 为 null → getReconciliation(destination) 创建对账账户 ✓
    └─ destinationAccount 为 null → getReconciliation(source) 创建对账账户 ✓
    ↓
永远不会有 null 账户 ✓
```

### 1.7 风险收敛结论

| 维度 | 结论 |
|-----|------|
| **触发概率** | 极低，仅在 1-11ms 窗口内的并发删除才可能触发 |
| **影响范围** | 仅 Withdrawal/Transfer 的 sourceAccount |
| **前置条件** | 必须同时满足 4 个条件（见上文） |
| **实际可观测性** | 几乎不可能在生产环境中观测到 |
| **代码证据** | `isBetweenAssetAndLiability(Account $source, Account $destination)` 声明不接受 null，证明设计预期中该分支永远不应执行 |

**风险等级**：理论存在，实际可忽略。属于典型的"教科书级并发问题"，但在实际业务场景中几乎不可能发生。

---

## 二、拆分交易更新时的新增、删除与类型继承判定流程

### 2.1 更新入口

`app/Services/Internal/Update/GroupUpdateService.php:48` - `update()`

```php
public function update(TransactionGroup $transactionGroup, array $data): TransactionGroup
{
    // 1. 更新组标题
    if (array_key_exists('group_title', $data)) {
        $transactionGroup->title = $data['group_title'];
        $transactionGroup->save();
    }

    // 2. 简单场景：单日记账组
    if (1 === count($transactions) && 1 === $transactionGroup->transactionJournals()->count()) {
        $first = $transactionGroup->transactionJournals()->first();
        $this->updateTransactionJournal($transactionGroup, $first, reset($transactions));
        return $transactionGroup;
    }

    // 3. 复杂场景：拆分交易更新
    $existing = $transactionGroup->transactionJournals->pluck('id')->toArray();
    $updated = $this->updateTransactions($transactionGroup, $transactions);
    
    // 4. 删除不再存在的日记账
    $result = array_diff($existing, $updated);
    foreach ($result as $deletedId) {
        $journal = $transactionGroup->transactionJournals()->find((int) $deletedId);
        $service = app(JournalDestroyService::class);
        $service->destroy($journal);
    }

    return $transactionGroup;
}
```

### 2.2 拆分交易更新的核心逻辑

`GroupUpdateService.php:172-221` - `updateTransactions()`

```php
private function updateTransactions(TransactionGroup $transactionGroup, array $transactions): array
{
    $updated = [];

    foreach ($transactions as $index => $transaction) {
        $journalId = (int) ($transaction['transaction_journal_id'] ?? 0);
        $journal = $transactionGroup->transactionJournals()->find($journalId);

        if (null === $journal) {
            // 新增拆分交易
            $newJournal = $this->createTransactionJournal($transactionGroup, $transaction);
            if ($newJournal instanceof TransactionJournal) {
                $updated[] = $newJournal->id;
            }
        }
        if (null !== $journal) {
            // 更新现有拆分交易
            $this->updateTransactionJournal($transactionGroup, $journal, $transaction);
            $updated[] = $journal->id;
        }
    }

    return $updated;
}
```

### 2.3 新增条目的判定流程

**判定条件：** `transaction_journal_id` 为 0 或不存在于当前组中

`GroupUpdateService.php:188-210`
```php
if (null === $journal) {
    Log::debug('This entry has no existing journal: make a new split.');
    
    // 关键：交易类型继承逻辑
    if (!array_key_exists('type', $transaction)) {
        Log::debug('No transaction type is indicated.');
        
        // 从组内现有日记账继承交易类型
        $randomJournal = $transactionGroup->transactionJournals()
            ->inRandomOrder()
            ->with(['transactionType'])
            ->first();
        
        if (null !== $randomJournal) {
            $transaction['type'] = $randomJournal->transactionType->type;
            Log::debug(sprintf('Transaction type set to %s.', $transaction['type']));
        }
    }
    
    Log::debug('Call createTransactionJournal');
    $newJournal = $this->createTransactionJournal($transactionGroup, $transaction);
}
```

**交易类型继承规则：**
1. 如果提交的数据中包含 `type` 字段，使用提交的类型
2. 如果不包含 `type` 字段，从组内现有日记账中**随机**获取一个日记账的类型
3. 由于拆分交易的所有日记账类型必须相同（验证器已保证），所以随机获取是安全的

**新增日记账的创建流程：**

`GroupUpdateService.php:121-145` - `createTransactionJournal()`
```php
private function createTransactionJournal(TransactionGroup $transactionGroup, array $data): ?TransactionJournal
{
    // 包装成单个交易的格式
    $submission = ['transactions' => [$data]];

    // 复用创建逻辑
    $factory = app(TransactionJournalFactory::class);
    $factory->setUser($transactionGroup->user);

    $collection = $factory->create($submission);
    
    // 关联到交易组
    $collection->each(static function (TransactionJournal $journal) use ($transactionGroup): void {
        $transactionGroup->transactionJournals()->save($journal);
    });

    return $collection->first();
}
```

### 2.4 旧条目的删除判定流程

**删除判定逻辑：**

`GroupUpdateService.php:84-108`
```php
// 步骤1：获取现有日记账ID
$existing = $transactionGroup->transactionJournals->pluck('id')->toArray();

// 步骤2：获取更新/新增的日记账ID
$updated = $this->updateTransactions($transactionGroup, $transactions);

// 步骤3：计算差集 - 存在于现有但不在更新列表中的ID需要删除
$result = array_diff($existing, $updated);

// 步骤4：删除这些日记账
foreach ($result as $deletedId) {
    $journal = $transactionGroup->transactionJournals()->find((int) $deletedId);
    $service = app(JournalDestroyService::class);
    $service->destroy($journal);
}
```

**关键设计细节：**

1. **只有当有成功更新时才执行删除**
   ```php
   if (0 === count($updated)) {
       Log::error('There were no transactions updated or created. Will not delete anything.');
       return $transactionGroup;
   }
   ```
   这是一个安全机制：如果所有更新都失败了，不会误删现有数据

2. **使用软删除**
   - `JournalDestroyService` 内部调用的是 Eloquent 的 `delete()` 方法
   - 模型使用了 `SoftDeletes` trait，所以数据不会真正从数据库删除
   - 可以通过 `withTrashed()` 恢复

### 2.5 更新现有条目的流程

`GroupUpdateService.php:150-166` - `updateTransactionJournal()`

```php
private function updateTransactionJournal(TransactionGroup $transactionGroup, TransactionJournal $journal, array $data): void
{
    // 空数据跳过
    if (0 === count($data)) {
        return;
    }
    // 只有 journal_id 的数据跳过
    if (1 === count($data) && array_key_exists('transaction_journal_id', $data)) {
        return;
    }

    // 委托给 JournalUpdateService 处理具体更新
    $updateService = app(JournalUpdateService::class);
    $updateService->setTransactionGroup($transactionGroup);
    $updateService->setTransactionJournal($journal);
    $updateService->setData($data);
    $updateService->update();
}
```

### 2.6 验证层的配合

在更新拆分交易时，验证器也有对应的规则：

`GroupValidation.php:186-230` - `validateJournalIds()`

```php
protected function validateJournalIds(Validator $validator, TransactionGroup $transactionGroup): void
{
    $transactions = $this->getTransactionsArray($validator);

    if (count($transactions) < 2) {
        return; // 单条交易不需要 journal_id
    }

    foreach ($transactions as $index => $transaction) {
        $journalId = 0;
        if (array_key_exists('transaction_journal_id', $transaction)) {
            $journalId = $transaction['transaction_journal_id'];
        }

        // 0 表示新增，允许
        if (0 === $journalId || '' === $journalId || '0' === $journalId) {
            return;
        }

        // 非0必须是组内有效的日记账ID
        $journalId = (int) $journalId;
        $count = $transactionGroup->transactionJournals()->where('transaction_journals.id', $journalId)->count();
        
        if (0 === $count) {
            $validator->errors()->add(
                sprintf('transactions.%d.source_name', $index),
                trans('validation.need_id_in_edit')
            );
        }
    }
}
```

### 2.7 完整更新状态机

```
提交更新请求
    ↓
验证层检查
    ├─ 交易类型一致性
    ├─ 账户一致性
    └─ journal_id 有效性（0 或 组内有效ID）
    ↓
GroupUpdateService::update()
    ├─ [单日记账场景] 直接更新
    │   └─ JournalUpdateService::update()
    │
    └─ [拆分交易场景]
        ├─ 遍历所有提交的交易
        │   ├─ journal_id = 0 → 新增（继承类型）
        │   ├─ journal_id 存在 → 更新
        │   └─ 收集所有成功的 journal_id
        ├─ 计算需要删除的 journal_id（现有 - 更新）
        └─ 删除不再存在的日记账
    ↓
返回更新后的交易组
```

### 2.8 关键边界规则总结

| 场景 | 判定条件 | 处理方式 |
|-----|---------|---------|
| **新增拆分** | `transaction_journal_id` = 0 或不存在 | 继承现有日记账类型，创建新日记账 |
| **更新拆分** | `transaction_journal_id` 存在于组内 | 调用 JournalUpdateService 更新 |
| **删除拆分** | 现有 ID 不在提交列表中，且至少有一个更新成功 | 软删除该日记账 |
| **类型继承** | 新增拆分未提交 `type` 字段 | 从组内现有日记账随机获取类型 |
| **安全保护** | 所有更新都失败（`count($updated) = 0`） | 不执行任何删除，保证数据安全 |
| **空数据跳过** | 只提交了 `transaction_journal_id` 无其他字段 | 不执行任何操作 |

---

## 三、设计洞察

### 3.1 双重验证的设计权衡

**实际行为与之前理解的关键修正：

| 之前的错误理解 | 实际代码行为 |
|---------------|-------------|
| 验证失败后进入现金账户兜底 | 验证失败直接 return null，**完全中断**，不做任何兜底 |
| sourceAccount 和 destinationAccount 都有兜底 | 只有 destinationAccount 有无条件兜底，sourceAccount 没有 |
| `getCashAccount()` 是无条件的 | `getCashAccount()` 要求 name 为空 且 允许 CASH 类型 |

**设计意图重新解读：**
- **安全性**：分层防御，防止验证器被绕过
- **核心规则严格**：账户类型校验是核心业务规则，失败时直接中断，不妥协
- **有限容错**：只有在极罕见的边缘场景（验证通过但实际获取失败）才做有限兜底
- **不对称设计**：sourceAccount 无兜底，保证交易语义的完整性（钱从哪来必须明确）

**潜在风险：**
- **代码重复**：相同的验证逻辑在两处实现，维护成本高
- **行为不一致**：如果两处验证逻辑不同步，可能导致意外行为

### 3.3 sourceAccount 为空的可达性分析（修正版）

**核心前提：验证器已经通过**

#### 可达性总览

| 交易类型 | 正常流程 | 并发窗口删除 | 最终可达性 |
|---------|---------|-------------|-----------|
| **Withdrawal（支出）** | ❌ 不可达 | ⚠️ 理论可达（1-11ms 窗口） | **极端低概率可达** |
| **Deposit（收入）** | ❌ 不可达 | ❌ 不可达（CASH 兜底） | **不可达** |
| **Transfer（转账）** | ❌ 不可达 | ⚠️ 理论可达（1-11ms 窗口） | **极端低概率可达** |
| **Reconciliation（对账）** | ❌ 不可达 | ❌ 不可达（sanityCheck 兜底） | **不可达** |

#### 关键收敛点

1. **验证器和工厂的两次独立查找**：
   - `validateAccounts()` 找到的账户对象**没有被缓存或复用**
   - `getAccount()` 会**再次**按 ID/name/iban 独立执行数据库查询
   - 两次查询之间的 1-11ms 窗口是唯一的并发风险点

2. **Withdrawal/Transfer 的风险链**：
   ```
   验证通过 → 账户在 11ms 窗口内被删除 → getAccount() 返回 null → 无兜底 → sourceAccount = null
   ```
   必须同时满足 4 个前置条件（见 1.6 节）才会触发

3. **Deposit 为何永远安全**：
   - expected_types 包含 CASH → `getCashAccount()` 总能兜底
   - REVENUE 类型可动态创建 → name 不为空时创建新账户

4. **强类型设计证据**：
   ```php
   // 参数类型声明为 Account，不是 ?Account，证明设计预期中永远不为 null
   private function isBetweenAssetAndLiability(Account $source, Account $destination): bool
   ```

#### 最终风险评级

| 维度 | 评级 | 说明 |
|-----|------|------|
| 触发概率 | **极低** | 11ms 窗口内的精确并发删除 |
| 影响范围 | **极小** | 仅 sourceAccount 变量 |
| 可观测性 | **几乎为 0** | 生产环境中几乎不可能复现 |
| 严重程度 | **中** | 如果触发会导致类型错误，但不会导致数据损坏 |

**结论**：这是一个理论上存在但实际可忽略的边缘情况。代码设计本身假设该分支永远不会执行，类型声明也反映了这一预期。

### 3.4 拆分交易更新的设计智慧

1. **增量更新而非全量替换**：通过 `transaction_journal_id` 区分新增/更新/删除，而不是删除所有后重建
2. **操作原子性**：先完成所有新增和更新，再执行删除，确保失败时不会丢失数据
3. **安全删除机制**：只有当有成功更新时才执行删除，防止批量误删
4. **类型继承简化**：利用验证器已保证的类型一致性，直接继承现有类型，简化用户输入
