# Firefly III 存钱罐进度机制事实整理与修正分析

## 目录

1. [三类存钱罐关联修复工具的职责对比](#一三类存钱罐关联修复工具的职责对比)
2. [start_date_tz 时区语义 Bug 确认](#二start_date_tz-时区语义-bug-确认)
3. [图表缓存机制与修复路径](#三图表缓存机制与修复路径)
4. [关键事实速查表](#四关键事实速查表)

---

## 一、三类存钱罐关联修复工具的职责对比

### 1.1 工具全景

Firefly III 中有三类组件处理存钱罐事件关联的清理与修复，职责边界清晰：

| 工具 | 触发时机 | 处理逻辑 | 代码位置 |
|------|---------|---------|---------|
| `DeletedTransactionJournalObserver` | 交易删除**实时**触发 | 将 `piggy_bank_events.transaction_journal_id` 置为 NULL | `app/Handlers/Observer/DeletedTransactionJournalObserver.php:58` |
| `correction:piggy-banks` | 人工或 cron**定期**执行 | 扫描所有事件，清理已硬删除交易的孤立关联 | `app/Console/Commands/Correction/CorrectsPiggyBanks.php:42-67` |
| `correction:remove-links-to-deleted-objects` | 人工或 cron**定期**执行 | 仅清理标签/预算/分类，**不处理存钱罐** | `app/Console/Commands/Correction/RemovesLinksToDeletedObjects.php:108-130` |

### 1.2 DeletedTransactionJournalObserver 详解

**代码位置**：`app/Handlers/Observer/DeletedTransactionJournalObserver.php:38-81`

```php
public function deleting(TransactionJournal $transactionJournal): void
{
    // ... 其他清理逻辑
    
    // 🔴 关键：仅解除关联，不删除事件记录
    $transactionJournal->piggyBankEvents()->update(['transaction_journal_id' => null]);
    
    // 其他关联直接删除
    DB::table('budget_transaction_journal')->where('transaction_journal_id', $transactionJournal->id)->delete();
    DB::table('category_transaction_journal')->where('transaction_journal_id', $transactionJournal->id)->delete();
    DB::table('tag_transaction_journal')->where('transaction_journal_id', $transactionJournal->id)->delete();
}
```

**职责边界**：
- ✅ 处理**正在被删除**的单条交易
- ✅ 实时执行，保证删除操作的即时性
- ✅ 保留 `PiggyBankEvent` 记录，仅置空关联字段
- ❌ 不处理已软删除或硬删除的历史数据

### 1.3 correction:piggy-banks 详解

**代码位置**：`app/Console/Commands/Correction/CorrectsPiggyBanks.php:42-67`

```php
public function handle(): int
{
    $count = 0;
    $set = PiggyBankEvent::with(['PiggyBank', 'TransactionJournal'])->get();

    /** @var PiggyBankEvent $event */
    foreach ($set as $event) {
        // 跳过已置空的关联
        if (null === $event->transaction_journal_id) {
            continue;
        }

        /** @var null|TransactionJournal $journal */
        $journal = $event->transactionJournal;

        // 🔴 关键：关联的交易已不存在（硬删除），则置空
        if (null === $journal) {
            $event->transaction_journal_id = null;
            $event->save();
            ++$count;
        }
    }
    if (0 !== $count) {
        $this->friendlyInfo(sprintf('Fixed %d piggy bank event(s).', $count));
    }

    return 0;
}
```

**职责边界**：
- ✅ 扫描**所有** `PiggyBankEvent` 记录
- ✅ 修复**观察者遗漏的场景**：交易被硬删除（非软删除）导致的孤立关联
- ✅ 定期执行，作为数据完整性的最后一道防线
- ❌ 不处理软删除交易的关联（观察者已处理）
- ❌ 不删除事件记录，仅修复关联字段

### 1.4 correction:remove-links-to-deleted-objects 详解

**代码位置**：`app/Console/Commands/Correction/RemovesLinksToDeletedObjects.php:108-130`

```php
private function cleanupJournals(array $journals): void
{
    foreach (array_chunk($journals, 1337) as $set) {
        DB::table('tag_transaction_journal')->whereIn('transaction_journal_id', $set)->delete();
        DB::table('budget_transaction_journal')->whereIn('transaction_journal_id', $set)->delete();
        DB::table('category_transaction_journal')->whereIn('transaction_journal_id', $set)->delete();
        // 🔴 故意不处理 piggy_bank_events
    }
}
```

**职责边界**：
- ✅ 清理标签、预算、分类的孤立关联
- ✅ 处理已软删除的交易数据
- ❌ **不处理存钱罐事件关联**（设计决策，非遗漏）

### 1.5 三类工具的协作关系

```
交易删除场景
    │
    ├─> 软删除（用户界面删除）
    │     │
    │     └─> DeletedTransactionJournalObserver 实时触发
    │          └─> 置空 transaction_journal_id
    │
    └─> 硬删除（直接删除或强制删除）
          │
          └─> 无观察者触发（或观察者未处理）
          │
          └─> correction:piggy-banks 定期扫描
               └─> 检测到关联不存在，置空 transaction_journal_id

correction:remove-links-to-deleted-objects 独立运行，仅处理标签/预算/分类
```

### 1.6 结论

- **不是遗漏**：`remove-links-to-deleted-objects` 不处理存钱罐是有意设计
- **分层修复**：观察者处理实时删除，`correction:piggy-banks` 处理硬删除等边缘场景
- **数据完整性优先**：所有处理都保留事件记录，仅修复关联字段

---

## 二、start_date_tz 时区语义 Bug 确认

### 2.1 Bug 代码

**代码位置**：`app/Repositories/PiggyBank/ModifiesPiggyBanks.php:388-391`

```php
if (array_key_exists('start_date', $data)) {
    $piggyBank->start_date = $data['start_date'];
    $piggyBank->start_date_tz = $data['target_date']?->format('e');  // 🔴 BUG: 使用 target_date 的时区
}
```

### 2.2 正确代码对照

**代码位置**：`app/Factory/PiggyBankFactory.php:252-253`（创建时的正确实现）

```php
$piggyBankData['start_date_tz'] = $piggyBankData['start_date']?->format('e');  // ✅ 正确
$piggyBankData['target_date_tz'] = $piggyBankData['target_date']?->format('e'); // ✅ 正确
```

### 2.3 影响分析

#### 时区转换逻辑

**代码位置**：`app/Casts/SeparateTimezoneCaster.php:45-55`

```php
public function get(Model $model, string $key, mixed $value, array $attributes): ?Carbon
{
    if ('' === $value || null === $value) {
        return null;
    }
    $timeZone = $attributes[sprintf('%s_tz', $key)] ?? config('app.timezone');
    
    // 用存储的时区解析日期，然后转换为应用时区
    return Carbon::parse($value, $timeZone)->setTimezone(config('app.timezone'));
}
```

#### 触发场景与影响

| 场景 | start_date | target_date | 存储的 start_date_tz | 实际影响 |
|------|-----------|------------|---------------------|---------|
| 同时设置，相同时区 | 2026-01-01 (UTC+8) | 2026-12-31 (UTC+8) | UTC+8 | ✅ 无影响 |
| 仅设置 start_date | 2026-01-01 (UTC+8) | null | null | ⚠️ 退化为应用默认时区 |
| 跨时区设置 | 2026-01-01 (UTC+8) | 2026-12-31 (UTC-5) | UTC-5 | 🚨 日期可能偏移一天 |

#### 严重偏差示例

```
用户输入（用户在上海，目标日期用纽约时间）：
- start_date = "2026-01-01" (Asia/Shanghai, UTC+8)
- target_date = "2026-12-31" (America/New_York, UTC-5)

实际存储：
- start_date = "2026-01-01"
- start_date_tz = "America/New_York" （错误地使用了 target_date 的时区）

读取时的转换：
Carbon::parse("2026-01-01", "America/New_York")
    ->setTimezone("Asia/Shanghai")

结果：
"2026-01-01 00:00 New York" → 转换为上海时间是 "2026-01-01 13:00"
日期显示可能偏移一天
```

### 2.4 修复方案

```php
// 修复后的代码
if (array_key_exists('start_date', $data)) {
    $piggyBank->start_date = $data['start_date'];
    $piggyBank->start_date_tz = $data['start_date']?->format('e');  // ✅ 使用 start_date 本身的时区
}
```

---

## 三、图表缓存机制与修复路径

### 3.1 CacheProperties 实际哈希键生成逻辑

**代码位置**：`app/Support/CacheProperties.php:38-104`

```php
class CacheProperties
{
    protected string $hash = '';
    protected Collection $properties;

    public function __construct()
    {
        $this->properties = new Collection();
        if (auth()->check()) {
            // 🔴 构造时自动添加三个属性
            $this->addProperty(auth()->user()->id);           // 用户ID
            $this->addProperty(Preferences::lastActivity());  // 最后活动时间
            $this->addProperty(Steam::anonymous());           // 是否匿名用户
        }
    }

    public function addProperty($property): void
    {
        $this->properties->push($property);
    }

    private function hash(): void
    {
        $content = '';
        foreach ($this->properties as $property) {
            try {
                $content = sprintf('%s%s', $content, json_encode($property, JSON_THROW_ON_ERROR));
            } catch (JsonException) {
                $content = sprintf('%s%s', $content, hash('sha256', (string) Carbon::now()->getTimestamp()));
            }
        }
        // 🔴 取 SHA256 哈希的前16位作为缓存键
        $this->hash = substr(hash('sha256', $content), 0, 16);
    }
}
```

### 3.2 存钱罐图表的缓存属性

**代码位置**：`app/Http/Controllers/Chart/PiggyBankController.php:63-71`

```php
public function history(PiggyBankRepositoryInterface $repository, PiggyBank $piggyBank): JsonResponse
{
    $cache = new CacheProperties();
    $cache->addProperty('chart.piggy-bank.history');  // 图表标识
    $cache->addProperty($piggyBank->id);               // 存钱罐ID
    if ($cache->has()) {
        return response()->json($cache->get());
    }
    // ... 生成图表数据
    $cache->store($data);
    return response()->json($data);
}
```

### 3.3 实际缓存键构成

存钱罐图表的缓存键由以下属性拼接后哈希生成：

```
属性列表（按顺序）：
1. auth()->user()->id                          // 用户ID（构造时自动添加）
2. Preferences::lastActivity()                 // 最后活动时间（构造时自动添加）
3. Steam::anonymous()                          // 是否匿名（构造时自动添加）
4. 'chart.piggy-bank.history'                  // 图表标识
5. $piggyBank->id                              // 存钱罐ID

哈希算法：
cache_key = substr(sha256(json_encode(属性1) + json_encode(属性2) + ...), 0, 16)
```

### 3.4 缓存失效的触发条件

基于缓存键构成，缓存会在以下情况下自动失效：

| 触发条件 | 原因 |
|---------|------|
| 用户登出后重新登录 | `Preferences::lastActivity()` 变化 |
| 用户的偏好设置更新 | `Preferences::lastActivity()` 变化 |
| 切换用户 | `auth()->user()->id` 变化 |
| 匿名/登录状态切换 | `Steam::anonymous()` 变化 |

⚠️ **重要**：存钱罐事件记录变化（新增/删除/修改）**不会自动使缓存失效**。缓存使用 `Cache::forever()` 永久存储，除非手动清除或上述条件触发。

### 3.5 基于事实的缓存修复路径

#### 路径1：依赖自动失效（被动）

等待以下事件自然发生：
- 用户下次登录时 `lastActivity` 更新
- 用户修改偏好设置
- 切换用户或登出/登录

#### 路径2：手动清除用户所有缓存（主动）

```php
// 获取当前用户的所有缓存键前缀特征
$userId = auth()->user()->id;
$lastActivity = Preferences::lastActivity();
$anonymous = Steam::anonymous();

// 但由于哈希是单向的，无法精确匹配，只能：
// 方案A：清除所有缓存（粗暴但有效）
Cache::flush();

// 方案B：通过特定 API 触发缓存失效（如果实现）
```

#### 路径3：代码层面的自动失效机制（需新增）

在事件记录变化后主动清除缓存：

```php
// 在 CreatesPiggyBankEventForChangedAmount 监听器中新增
public function handle(PiggyBankAmountIsChanged $event): void
{
    // ... 现有创建事件逻辑
    
    // 🔴 新增：清除该存钱罐的图表缓存
    $cache = new CacheProperties();
    $cache->addProperty('chart.piggy-bank.history');
    $cache->addProperty($event->piggyBank->id);
    Cache::forget($cache->getHash());
}
```

**注意**：上述代码为建议的新增逻辑，**当前代码库中未实现**。

---

## 四、关键事实速查表

| 事实 | 代码位置 | 状态 |
|------|---------|------|
| 删除观察者置空 `transaction_journal_id` | `app/Handlers/Observer/DeletedTransactionJournalObserver.php:58` | ✅ 已确认 |
| `correction:piggy-banks` 修复硬删除关联 | `app/Console/Commands/Correction/CorrectsPiggyBanks.php:56-59` | ✅ 已确认 |
| `remove-links-to-deleted-objects` 不处理存钱罐 | `app/Console/Commands/Correction/RemovesLinksToDeletedObjects.php:108-130` | ✅ 已确认（设计决策） |
| `start_date_tz` Bug | `app/Repositories/PiggyBank/ModifiesPiggyBanks.php:390` | ✅ 已确认 |
| CacheProperties 构造时自动添加3个属性 | `app/Support/CacheProperties.php:46-50` | ✅ 已确认 |
| 图表缓存使用 SHA256 前16位作为键 | `app/Support/CacheProperties.php:103` | ✅ 已确认 |
| 图表缓存永久存储（forever） | `app/Support/CacheProperties.php:89` | ✅ 已确认 |
| 事件变化不自动清除图表缓存 | - | ✅ 已确认（代码中无此逻辑） |
| 三类修复工具职责互补 | - | ✅ 已确认 |

---

## 总结

### 已确认的事实

1. **三类工具职责明确**：删除观察者实时处理、`correction:piggy-banks` 定期修复硬删除关联、`remove-links-to-deleted-objects` 仅处理其他关联。不处理存钱罐是设计决策而非遗漏。

2. **start_date_tz Bug 真实存在**：更新存钱罐时错误地使用 `target_date` 的时区设置 `start_date_tz`，跨时区场景会导致日期偏移。

3. **图表缓存无自动失效**：缓存键包含用户ID、最后活动时间等属性，但存钱罐事件变化不会触发缓存失效，可能导致图表显示旧数据。

### 已移除的推测性内容

- ❌ 移除了未实现的 `enforceConsistency()` 一致性检查方法
- ❌ 移除了未实现的 `rebuildEventsFromCurrentAmount()` 重建方法
- ❌ 移除了未实现的 `createAdjustmentEvent()` 调整事件方法
- ❌ 移除了未实现的 `syncCurrentAmountFromEvents()` 反向同步方法
- ❌ 移除了基于错误缓存键逻辑的修复方案

### 保留的内容

- ✅ 保留了 `start_date_tz` Bug 的完整分析与修复方案
- ✅ 保留了三类修复工具的职责对比
- ✅ 保留了基于 CacheProperties 实际逻辑的缓存分析
- ✅ 保留了所有已确认的代码证据
