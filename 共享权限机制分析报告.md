# Firefly III 财务数据共享与权限范围机制分析报告

## 1. 总体架构概述

Firefly III 采用 **双轨权限体系 + 数据归属双字段** 的设计，确保多个用户共享财务数据时能精确控制可见范围与操作权限：

- **全局权限体系**：基于 `Role` 模型的系统级角色（如 `owner`、`demo`）
- **群组权限体系**：基于 `UserGroup` + `GroupMembership` + `UserRole` 的群组内细粒度角色
- **数据归属模型**：每条财务数据同时携带 `user_id`（创建者）和 `user_group_id`（所属行政群组）

---

## 2. 核心数据模型与关系

### 2.1 ER 关系图

```
┌──────────────┐       ┌──────────────────────┐       ┌──────────────────┐
│     User     │       │   GroupMembership    │       │    UserGroup     │
│  (用户表)    │1─────*│  (组成员关系表)      │*─────1│  (行政群组表)    │
├──────────────┤       ├──────────────────────┤       ├──────────────────┤
│ id           │◄──┐   │ id                   │   ┌──►│ id               │
│ email        │   │   │ user_id              │   │   │ title            │
│ password     │   │   │ user_group_id        │───┘   │ created_at       │
│ user_group_id│───┘   │ user_role_id         │───┐   └──────────────────┘
│ blocked      │       │ created_at           │   │          │
└──────────────┘       └──────────────────────┘   │          │ 1
         │ 1                                      ▼          │ *
         │                                        ┌──────────────────┐
         │                                        │     UserRole     │
         │                                        │  (组内角色表)     │
         │                                        ├──────────────────┤
         │                                        │ id               │
         │                                        │ title (枚举)     │
         │                                        └──────────────────┘
         │
         │ * (全局角色)
         ▼
┌──────────────────┐         财务数据（Account、Transaction等）
│       Role       │         ┌─────────────────────────────────┐
│  (全局角色表)    │         │ 每条记录同时有：                  │
├──────────────────┤         │  • user_id       ← 创建者归属    │
│ id               │         │  • user_group_id ← 群组归属（可见域）│
│ name             │         └─────────────────────────────────┘
│ display_name     │
└──────────────────┘
```

### 2.2 关键模型文件

| 模型 | 文件路径 | 核心职责 |
|------|----------|----------|
| User | [User.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/User.php) | 用户主体，持有 `user_group_id`（当前激活的群组） |
| UserGroup | [UserGroup.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/UserGroup.php) | 行政群组（Administration），财务数据的隔离边界 |
| GroupMembership | [GroupMembership.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/GroupMembership.php) | 用户-群组-角色三元关系表 |
| UserRole | [UserRole.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/UserRole.php) | 组内可分配的角色定义 |
| Role | [Role.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/Role.php) | 全局系统角色（与群组无关） |

---

## 3. 组内角色定义与权限矩阵

所有组内角色在 [UserRoleEnum.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Enums/UserRoleEnum.php) 中以枚举形式定义，共 **20 种角色权限**，可归纳为 4 个层级：

### 3.1 层级 0：基础可见性

| 角色 | 枚举值 | 说明 |
|------|--------|------|
| `READ_ONLY` | `ro` | 最基础权限：只读所有财务数据、元数据；**不能看到其他成员信息** |

### 3.2 层级 1：核心操作权限

| 角色 | 枚举值 | 说明 |
|------|--------|------|
| `MANAGE_TRANSACTIONS` | `mng_trx` | 管理交易（创建/编辑/删除账户、交易） |
| `MANAGE_META` | `mng_meta` | 管理分类、标签、对象组 |

### 3.3 层级 2：专项读权限

| 角色 | 枚举值 | 可读对象 |
|------|--------|----------|
| `READ_BUDGETS` | `read_budgets` | 预算 |
| `READ_PIGGY_BANKS` | `read_piggies` | 储蓄罐 |
| `READ_SUBSCRIPTIONS` | `read_subscriptions` | 订阅/账单 |
| `READ_RULES` | `read_rules` | 规则 |
| `READ_RECURRING` | `read_recurring` | 循环交易 |
| `READ_WEBHOOKS` | `read_webhooks` | Webhook |
| `READ_CURRENCIES` | `read_currencies` | 货币 |

### 3.4 层级 3：专项写权限

| 角色 | 枚举值 | 可管理对象 |
|------|--------|----------|
| `MANAGE_BUDGETS` | `mng_budgets` | 预算 |
| `MANAGE_PIGGY_BANKS` | `mng_piggies` | 储蓄罐 |
| `MANAGE_SUBSCRIPTIONS` | `mng_subscriptions` | 订阅/账单 |
| `MANAGE_RULES` | `mng_rules` | 规则 |
| `MANAGE_RECURRING` | `mng_recurring` | 循环交易 |
| `MANAGE_WEBHOOKS` | `mng_webhooks` | Webhook |
| `MANAGE_CURRENCIES` | `mng_currencies` | 货币 |
| `VIEW_REPORTS` | `view_reports` | 查看和生成报表 |
| `VIEW_MEMBERSHIPS` | `view_memberships` | 查看成员和角色（管理需 FULL） |

### 3.5 层级 4：超级权限

| 角色 | 枚举值 | 说明 |
|------|--------|------|
| `FULL` | `full` | 除"移除原始创建者/删除群组"外的所有权限 |
| `OWNER` | `owner` | 群组创建者专属，拥有一切权限 |

### 3.6 权限覆盖机制

在 [UserGroupRepository.php::simplifyListByName()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php#L308-L322) 中实现了 **高权限自动覆盖**：

```php
// 如果角色列表中包含 OWNER，则只保留 OWNER（覆盖所有其他权限）
if (in_array(UserRoleEnum::OWNER->value, $roles, true)) {
    return [UserRoleEnum::OWNER->value];
}
// 如果包含 FULL，则只保留 FULL
if (in_array(UserRoleEnum::FULL->value, $roles, true)) {
    return [UserRoleEnum::FULL->value];
}
```

此外，`hasRoleInGroupOrOwner()` 方法（见 [User.php#L257-L262](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/User.php#L257-L262)）提供了 **权限向上兼容**：检查某权限时，自动同时允许 `OWNER` 和 `FULL` 角色通过。

---

## 4. 可见范围判定的五层防御机制

Firefly III 采用 **纵深防御**，在 5 个不同层面同时执行范围检查：

### 第 1 层：路由参数绑定（Route Binder）

每个财务模型都实现了静态 `routeBinder()` 方法，由 [Binder.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Middleware/Binder.php) 中间件在路由解析阶段自动调用。

**典型实现** — 以 [Account.php::routeBinder()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/Account.php#L67-L86) 为例：

```php
public static function routeBinder(self|string $value): self
{
    if (auth()->check()) {
        $accountId = (int) $value;
        $user      = auth()->user();
        // 关键：通过 user->accounts() 关系查询，隐式过滤 user_id
        $account   = $user->accounts()->with(['accountType'])->find($accountId);
        if (null !== $account) {
            return $account;
        }
    }
    throw new NotFoundHttpException();
}
```

**UserGroup 的特殊检查** — [UserGroup.php::routeBinder()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/UserGroup.php#L47-L73)：

```php
// 用户必须对该群组至少拥有 READ_ONLY 权限，或是全局 owner
$access = $user->hasRoleInGroupOrOwner($userGroup, UserRoleEnum::READ_ONLY) 
       || $user->hasRole('owner');
```

### 第 2 层：Eloquent 关系查询

`User` 和 `UserGroup` 模型对所有财务对象都定义了一对多关系。查询时通过调用 `$user->xxx()` 或 `$userGroup->xxx()` 自动限定范围：

| 查询方式 | 代码示例 | 过滤条件 |
|----------|----------|----------|
| 按用户查询 | `$user->accounts()->find($id)` | `WHERE accounts.user_id = ?` |
| 按群组查询 | `$userGroup->accounts()->get()` | `WHERE accounts.user_group_id = ?` |

关键关系定义见 [User.php#L110-L476](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/User.php#L110-L476) 和 [UserGroup.php#L78-L201](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/UserGroup.php#L78-L201)。

### 第 3 层：Collector 查询构建器

对于交易等复杂查询，使用 [GroupCollector.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Helpers/Collector/GroupCollector.php) 构建 SQL。它提供两种初始化方式：

**按用户（传统单用户模式）** — `startQuery()`：
```php
$this->query = $this->user
    ->transactionJournals()
    ->leftJoin(...)
    // ...
```

**按群组（共享模式）** — [startQueryForGroup()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Helpers/Collector/GroupCollector.php#L1099-L1127)：
```php
$this->query = $this->userGroup
    ->transactionJournals()
    ->leftJoin('transaction_groups', ...)
    // 关键：通过 userGroup 关系自动限定 user_group_id
    ->whereNull('transaction_groups.deleted_at')
    // ...
```

### 第 4 层：表单请求授权（FormRequest Authorization）

所有 `FormRequest` 类使用 [ChecksLogin](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Request/ChecksLogin.php) Trait，在 `authorize()` 阶段检查组内角色：

```php
public function authorize(): bool
{
    $user      = auth()->user();
    $userGroup = $this->getUserGroup();  // 从路由/请求参数/用户默认值获取群组
    
    foreach ($this->acceptedRoles as $role) {
        // 检查用户在该群组中是否拥有任一接受的角色
        if ($user->hasRoleInGroupOrOwner($userGroup, $role)) {
            return true;
        }
    }
    return false;
}
```

**群组解析优先级**（[ChecksLogin::getUserGroup()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Request/ChecksLogin.php#L80-L106)）：
1. 路由参数 `{userGroup}`
2. 请求参数 `user_group_id`
3. 用户当前激活的群组 `user->user_group_id`

**实际应用示例**：

| 请求类 | 所需角色 | 代码位置 |
|--------|----------|----------|
| AccountFormRequest | `MANAGE_TRANSACTIONS` | [AccountFormRequest.php#L48](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Requests/AccountFormRequest.php#L48) |
| TagFormRequest | 空数组（只要登录即可） | [TagFormRequest.php#L44](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Requests/TagFormRequest.php#L44) |
| Transaction StoreRequest | 空数组（仅需登录） | [StoreRequest.php#L57](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Api/V1/Requests/Models/Transaction/StoreRequest.php#L57) |

### 第 5 层：输入数据归属验证

使用自定义验证规则确保提交的数据 ID 确实属于当前用户或当前群组：

| 规则类 | 用途 | 过滤条件 |
|--------|------|----------|
| [BelongsUser](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Rules/BelongsUser.php) | 验证对象属于当前用户 | `WHERE user_id = auth()->id()` |
| [BelongsUserGroup](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Rules/BelongsUserGroup.php) | 验证对象属于指定群组 | `WHERE user_group_id = ?` |

**BelongsUserGroup 验证示例**（验证账户 ID）：
```php
// BelongsUserGroup.php#L125-L134
private function validateAccountId(int $value): bool
{
    $count = Account::where('id', '=', $value)
        ->where('user_group_id', '=', $this->userGroup->id)
        ->count();
    return 1 === $count;
}
```

### 第 5.5 层：API 专用群组验证

API 控制器使用 [ValidatesUserGroupTrait](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Http/Api/ValidatesUserGroupTrait.php) 在方法入口处统一验证：

```php
protected function validateUserGroup(Request $request): UserGroup
{
    // 1. 确定群组ID（请求参数 > 用户默认）
    $groupId = $request->has('user_group_id') 
        ? (int)$request->get('user_group_id') 
        : (int)$user->user_group_id;
    
    // 2. 检查用户是否为该群组成员（通过 GroupMembership）
    $memberships = $repository->getMembershipsFromGroupId($groupId);
    if (0 === $memberships->count()) {
        throw new AuthorizationException();
    }
    
    // 3. 遍历 acceptedRoles 验证权限
    foreach ($this->acceptedRoles as $role) {
        if ($user->hasRoleInGroupOrOwner($group, $role)) {
            return $group;
        }
    }
    throw new AuthorizationException();
}
```

---

## 5. 群组切换（切换所属行政群组）机制

### 5.1 核心原理：`user_group_id` 字段

`users` 表中的 `user_group_id` 字段是 **"当前激活的行政群组"** 指针。所有未明确指定群组的操作，都以此字段作为默认可见范围。

```
users 表
┌────┬───────────┬───────────────┐
│ id │  email    │ user_group_id │ ← 当前激活的群组
├────┼───────────┼───────────────┤
│  1 │ alice@x   │      101      │ ← Alice 目前看的是群组 101
│  2 │ bob@x     │      101      │ ← Bob 目前看的也是群组 101
│  1 │ alice@x   │      102      │ ← 切换后：Alice 改为看群组 102
└────┴───────────┴───────────────┘
```

### 5.2 群组切换的代码实现

在 [UserGroupRepository.php::useUserGroup()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php#L278-L282)：

```php
public function useUserGroup(UserGroup $userGroup): void
{
    $this->user->user_group_id = $userGroup->id;
    $this->user->save();
}
```

**前置校验**（切换前）通过 [UserGroupTrait::setUserGroupById()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Repositories/UserGroup/UserGroupTrait.php#L99-L112) 确保用户确实是目标群组的成员：

```php
public function setUserGroupById(int $userGroupId): void
{
    // 必须存在组成员关系
    $memberships = GroupMembership::where('user_id', $this->user->id)
        ->where('user_group_id', $userGroupId)
        ->count();
    if (0 === $memberships) {
        throw new FireflyException('User has no access to administration.');
    }
    $this->setUserGroup(UserGroup::find($userGroupId));
}
```

### 5.3 群组切换对可见性的链式影响

切换 `user_group_id` 后，以下所有机制的过滤条件 **自动同步变化**：

| 影响层面 | 变化效果 |
|----------|----------|
| **FormRequest 授权** | `getUserGroup()` 返回新群组，基于新群组做角色检查 |
| **Collector 查询** | `setUserGroup()` 后 `startQueryForGroup()` 查询新群组的交易 |
| **输入验证** | `BelongsUserGroup` 规则用新群组 ID 验证提交的数据 |
| **数据创建** | 新创建的财务对象 `user_group_id` 会设置为当前激活的群组 |
| **Repository 层** | `UserGroupTrait::setUser()` 自动读取用户的新 `user_group_id` |

### 5.4 用户可切换的群组列表

通过 [UserGroupRepository.php::get()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php#L120-L141) 获取用户所有成员身份的群组：

```php
public function get(): Collection
{
    $memberships = $this->user->groupMemberships()->get();
    foreach ($memberships as $membership) {
        // 收集所有用户有 membership 的群组
        $set[$groupId] = $membership->userGroup;
    }
    return $collection;
}
```

---

## 6. 群组与成员的生命周期管理

### 6.1 新用户注册初始化流程

在 [HandlesNewUserRegistration.php::createGroupMembership()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Listeners/Security/System/HandlesNewUserRegistration.php#L79-L111) 中自动完成：

```
用户注册
  │
  ▼
① 创建以用户邮箱命名的 UserGroup（避免重名循环加后缀）
  │  例如：alice@example.com → alice@example.com-1 → alice@example.com-2
  │
  ▼
② 创建 OWNER 角色的 GroupMembership
  │  user_id = 新用户ID
  │  user_group_id = 新建群组ID  
  │  user_role_id = OWNER 角色ID
  │
  ▼
③ 设置 users.user_group_id = 新建群组ID（激活该群组）
  │
  ▼
④ 如果是系统第一个注册用户，额外赋予全局 Role = "owner"
```

实现代码（[UserGroupFactory.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Factory/UserGroupFactory.php#L41-L60)）：
```php
public function create(array $data): UserGroup
{
    $userGroup = new UserGroup(['title' => $data['title']]);
    $userGroup->save();
    
    $role = UserRole::whereTitle(UserRoleEnum::OWNER->value)->first();
    GroupMembership::create([
        'user_group_id' => $userGroup->id,
        'user_id'       => $data['user']->id,
        'user_role_id'  => $role->id,
    ]);
    return $userGroup;
}
```

### 6.2 成员角色更新

[UserGroupRepository.php::updateMembership()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php#L206-L275) 实现了**防呆保护**：

| 保护规则 | 触发条件 | 行为 |
|----------|----------|------|
| 最后成员不可移除 | 群组只有1人且移除其全部角色 | 抛出异常 |
| 最后成员必须是 Owner | 群组只有1人且不给 Owner 角色 | 抛出异常 |
| 最后 Owner 不可剥夺 | 群组有多成员但只剩一个 Owner 且剥夺其 Owner 角色 | 抛出异常 |

### 6.3 群组删除流程

[UserGroupRepository.php::destroy()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php#L49-L109) 按序执行：

```
1. 遍历每个成员
   ├─ 如果成员没有其他群组 → 创建新的个人群组并赋予 Owner
   └─ 如果成员有其他群组 → 随机选一个作为激活群组
2. 删除所有 GroupMembership
3. 逐个删除群组内所有财务对象（触发 Observer 级联清理）
   ├─ availableBudgets → bills → budgets → categories
   ├─ currencyExchangeRates → objectGroups → recurrences
   ├─ rules → ruleGroups → tags → transactionGroups
   ├─ transactionJournals → piggyBanks → accounts → webhooks
   └─ 最后删除 UserGroup 本身
```

---

## 7. 完整调用链路示例

### 场景：用户 Bob（群组 A 的 READ_ONLY 成员）切换到群组 B（MANAGE_TRANSACTIONS），然后创建账户

```
请求 POST /accounts
  │
  ▼
① [Binder 中间件] 路由参数绑定（无特殊参数，跳过）
  │
  ▼
② [ChecksLogin::authorize()] 
  │   ├─ getUserGroup() → 读取 Bob.user_group_id = B
  │   └─ hasRoleInGroupOrOwner(B, MANAGE_TRANSACTIONS) → ✓ 通过
  │
  ▼
③ [表单验证] AccountFormRequest::rules()
  │   └─ 各种字段格式校验（无 BelongsUserGroup，因为是新建）
  │
  ▼
④ [AccountFactory::create()] 
  │   └─ 新建 Account 时写入：
  │        • user_id = Bob.id
  │        • user_group_id = B.id  ← 当前激活群组
  │
  ▼
⑤ 返回成功，新账户属于群组 B
```

---

## 8. 将用户加入现有群组的完整路径与鉴权门禁

Firefly III 存在 **两条独立路径** 把一个用户加入已有群组，两条路径的入口权限、加入方式和入组后的默认角色截然不同。

### 8.1 路径 A：全局管理员邀请（注册制入口）

此路径由**全局 owner 角色的管理员**操作，邀请的是一个**尚不存在**的用户注册系统后再入组。

```
管理员访问 /settings/users
  │
  ▼
① [IsAdmin 中间件] 检查当前用户是否拥有全局 Role = "owner"
  │   代码：IsAdmin.php#L44-L64
  │   逻辑：$repository->hasRole($user, 'owner')
  │   ❌ 挡掉：所有非全局 owner 的普通用户
  │
  ▼
② 管理员在用户管理页面提交邮箱地址
  │   路由：POST /settings/users/invite
  │   请求类：InviteUserFormRequest（acceptedRoles = []，仅检查登录）
  │   校验：invited_user 字段必须是 email 且在 invited_users 表中不重复
  │
  ▼
③ UserRepository::inviteUser() 生成邀请记录
  │   代码：UserRepository.php#L293-L310
  │   生成：InvitedUser 记录（64 位随机 invite_code，2 天过期）
  │   写入字段：user_id(邀请者)、email、invite_code、expires、redeemed
  │   ⚠️ 注意：user_group_id 字段在 fillable 中存在，但 inviteUser() 并未赋值，
  │        即邀请记录不携带任何群组归属信息
  │
  ▼
④ 事件 NewInvitationCreated 触发邮件通知
  │   监听器：NotifiesAboutNewInvitation.php
  │   邮件：InvitationMail，包含注册链接 /invitee/{invite_code}
  │
  ▼
⑤ 被邀请者点击链接 → RegisterController::showInviteForm()
  │   校验：UserRepository::validateInviteCode()
  │   条件：invite_code 匹配 AND expires > now AND redeemed = false
  │   ❌ 挡掉：过期邀请、已兑换邀请、无效 code
  │
  ▼
⑥ 被邀请者填写注册表单 → RegisterController::register()
  │   校验：即使关闭了开放注册（single_user_mode），持有有效邀请码仍可注册
  │   注册后触发 NewUserRegistered 事件
  │
  ▼
⑦ HandlesNewUserRegistration::createGroupMembership()
  │   ⚠️ 注意：新用户注册后创建的是**以自己邮箱命名的新群组**，
  │   并 NOT 自动加入邀请者的群组！
  │   新用户在自己的群组中是 OWNER。
  │
  ▼
⑧ 邀请码兑换：UserRepository::redeemCode()
  │   将 InvitedUser.redeemed 设为 true
  │
  ▼
⑨ ⚠️ 当前代码中**没有任何活的入口**能把这个新用户加入到邀请者的已有群组。
  │   邀请流程到此为止，新用户只能作为自己个人群组的 OWNER 独立存在。
  │   （见 8.2 节详析"路径 B 的不可达性"）
```

**路径 A 的鉴权门禁汇总**：

| 门禁 | 位置 | 检查内容 | 挡掉的越权行为 |
|------|------|----------|----------------|
| IsAdmin 中间件 | [IsAdmin.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Middleware/IsAdmin.php#L44-L64) | 全局 `owner` 角色 | 非管理员无法发送邀请 |
| 邀请码验证 | [UserRepository.php#L412-L418](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/User/UserRepository.php#L412-L418) | code 存在、未过期、未兑换 | 防止伪造/重放邀请链接 |
| 注册开关 | [RegisterController.php#L77-L84](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Controllers/Auth/RegisterController.php#L77-L84) | 开放注册 OR 有效邀请码 | 防止无邀请自行注册 |

### 8.2 路径 B：群组内 OWNER/FULL 添加已有用户（**当前代码不可达**）

此路径的底层逻辑在仓库层已完整实现，但在**所有路由层入口均被禁用或从未接入**，因此在当前代码状态下是**不可达**的死代码。

#### 8.2.1 三层证据链——为何不可达

**证据 1：`updateMembership()` 零调用**

对整个代码库执行全文搜索，`updateMembership(` 仅出现在 7 处：
- 1 处：仓库层的方法**定义**（[UserGroupRepository.php#L206](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php#L206)）
- 1 处：接口中的方法**声明**（[UserGroupRepositoryInterface.php#L59](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepositoryInterface.php#L59)）
- 5 处：本报告中的文档引用

**没有任何一个控制器、监听器或命令实际调用它。**

**证据 2：API 路由中组员变更端点被注释**

在 [api.php#L354-L359](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/routes/api.php#L354-L359) 中：

```php
// 唯一活的路由（3 行未注释）：
Route::get('',                   ['uses' => 'IndexController@index',   'as' => 'index']);   // 列表
Route::get('{userGroup}',        ['uses' => 'ShowController@show',    'as' => 'show']);    // 详情
Route::put('{userGroup}',        ['uses' => 'UpdateController@update','as' => 'update']);  // ← 只有这个 PUT 活的

// 以下全部被注释（死路由）：
// Route::post('', ['uses' => 'StoreController@store', ...]);
// Route::post('{userGroup}/use', ['uses' => 'UpdateController@useUserGroup', ...]);
// Route::put('{userGroup}/update-membership', ['uses' => 'UpdateController@updateMembership', ...]);  // ← 组员变更
// Route::delete('{userGroup}', ['uses' => 'DestroyController@destroy', ...]);
```

**唯一活的 `PUT /v1/user-groups/{userGroup}` 实际做什么？** 跟踪完整数据流：

| 步骤 | 代码位置 | 实际行为 |
|------|----------|---------|
| 1. 请求校验 | [UpdateRequest::rules()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Api/V1/Requests/Models/UserGroup/UpdateRequest.php#L55-L62) | 只接收 `title`、`primary_currency_id`、`primary_currency_code` 三个字段 |
| 2. 提取数据 | [UpdateRequest::getData()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Api/V1/Requests/Models/UserGroup/UpdateRequest.php#L41-L50) | 同上，无 `id`、`email`、`roles` 等组员字段 |
| 3. 控制器 | [UpdateController::update()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Api/V1/Controllers/Models/UserGroup/UpdateController.php#L55-L67) | 调用 `$this->repository->update($userGroup, $data)` |
| 4. 仓库层 | [UserGroupRepository::update()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php#L176-L199) | 只修改 `user_groups.title` + 调用 `CurrencyRepository::makePrimary()` 设默认币种 |
| 5. 仓库层 | 同上 | **从未调用** `updateMembership()`，也从未读写 `GroupMembership` 表 |

**证据 3：Web 路由中只存在"编辑页 GET"，无 POST/PUT**

在 [web.php#L1451-L1469](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/routes/web.php#L1451-L1469) 中：

```php
// 活的路由（只有 GET）：
Route::get('',                      'UserGroup\IndexController@index');    // 管理列表
Route::get('create',                'UserGroup\CreateController@create'); // 创建页
Route::get('edit/{userGroup?}',     'UserGroup\EditController@edit');     // 编辑页

// 后续全部是被注释的示例模板（Bill 控制器的残留），
// 包括 store / update / destroy 全部被注释掉。
```

管理员能看到 `/administrations/edit/42` 编辑页面，但**提交按钮无对应 POST 路由接收**——等于一个只有表单没有后端的只读页面。

#### 8.2.2 `updateMembership()` 逻辑的内部实现（作为死代码存档）

虽然不可达，但其内部实现逻辑完整且设计严谨，保留如下作为参考：

```
updateMembership($userGroup, $data) 实际执行流程：
  │
  ├─ ① 定位目标用户：优先 $data['id']，其次 $data['email']
  │
  ├─ ② 三重"最后所有者"保护（见 8.3 节）
  │
  ├─ ③ simplifyListByName($roles)：
  │     若含 OWNER → 只保留 OWNER
  │     若含 FULL  → 只保留 FULL
  │
  ├─ ④ 删除旧成员关系：
  │     $user->groupMemberships()
  │           ->where('user_group_id', $userGroup->id)
  │           ->delete()
  │
  └─ ⑤ 创建新成员关系：每个角色一条 GroupMembership 记录
```

#### 8.2.3 管理员用户编辑页（`/settings/users/edit/{id}`）是否能改群组？

答案是**也不能**。在 [UserController::update()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Controllers/Admin/UserController.php#L234-L261) 中只做三件事：

```php
// 1. 有密码则改密码
$this->repository->changePassword($user, $data['password']);

// 2. 改封禁状态
$this->repository->changeStatus($user, $data['blocked'], $data['blocked_code']);

// 3. 改邮箱
$this->repository->updateEmail($user, $data['email']);
```

对应 [UserFormRequest::getUserData()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Requests/UserFormRequest.php#L45-L55) 中只有 4 个字段：`email`、`blocked`、`blocked_code`、`password`，**没有 `user_group_id`，也没有任何组成员操作字段**。

#### 8.2.4 结论：当前是否存在"把已有用户加进现有群组"的活入口？

**不存在。** 所有可能的路径均被堵死：

| 路径 | 状态 | 堵点 |
|------|------|------|
| API: `PUT /v1/user-groups/{id}` | 活的 | 只改 `title` 和默认币种，不调 `updateMembership()` |
| API: `PUT /v1/user-groups/{id}/update-membership` | 路由注释 | 端点不存在 |
| Web: `POST /administrations/update/{id}` | 路由注释 | 端点不存在 |
| 管理员 `/settings/users/edit/{id}` | 活的 | 无组员/群组字段，只能改邮箱/密码/封禁 |
| 邀请码注册流程 | 活的 | 新用户自成新群组，不进邀请者群组 |
| 直接操作数据库 `GroupMembership` 表 | — | 理论可行，但无代码入口 |

这意味着：在当前代码版本下，每个用户只能在**注册时自动生成的个人群组**中存在，多用户共享财务数据的机制在**存储层和权限校验层已就绪**，但**路由层和控制器层未开放**——属于"代码半成品"状态，未完成的功能被通过注释路由的方式安全地禁用了。

### 8.3 `updateMembership()` 中的角色重赋约束与"最后所有者"保护（死代码逻辑参考）

虽然不可达，但 [UserGroupRepository.php::updateMembership()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php#L206-L275) 中三层递进的保护逻辑设计严谨，保留作为参考：

**保护 1：最后成员不可被完全移除**

```php
// 代码行 #L233-L239
$membershipCount = $userGroup->groupMemberships()->distinct()->count('group_memberships.user_id');
if (1 === $membershipCount) {
    $lastUserId = $userGroup->groupMemberships()->distinct()->first(['group_memberships.user_id'])->user_id;
    if ($lastUserId === (int) $user->id && 0 === count($data['roles'])) {
        throw new FireflyException(
            'You cannot remove the last member from this user group. Delete the user group instead.'
        );
    }
}
```

| 场景 | 群组人数 | 操作 | 结果 |
|------|----------|------|------|
| 群组只有 Bob 一人 | 1 | 清空 Bob 的所有角色 | ❌ 抛异常 |
| 群组有 Bob + Alice | 2 | 清空 Bob 的所有角色 | ✅ 允许（Bob 脱离群组，Alice 还在） |

**保护 2：最后成员必须保留 OWNER 角色**

```php
// 代码行 #L241-L247
if ($lastUserId === (int) $user->id && count($data['roles']) > 0 
    && !in_array(UserRoleEnum::OWNER->value, $data['roles'], true)) {
    throw new FireflyException(
        'The last member in this user group must get or keep the "owner" role.'
    );
}
```

| 场景 | 群组人数 | 操作 | 结果 |
|------|----------|------|------|
| 群组只有 Bob | 1 | 给 Bob 设 READ_ONLY | ❌ 抛异常 |
| 群组只有 Bob | 1 | 给 Bob 设 OWNER | ✅ 允许 |
| 群组有 Bob + Alice | 2 | 给 Bob 设 READ_ONLY | → 进入保护 3 |

**保护 3：群组多成员时，最后一个 OWNER 不可被剥夺**

```php
// 代码行 #L248-L257
if ($membershipCount > 1) {
    $ownerCount = $userGroup->groupMemberships()
        ->where('user_role_id', $owner->id)
        ->where('user_id', '!=', $user->id)
        ->count();
    if (0 === $ownerCount && (0 === count($data['roles']) 
        || !in_array(UserRoleEnum::OWNER->value, $data['roles'], true))) {
        throw new FireflyException(
            'The last owner in this user group must keep the "owner" role.'
        );
    }
}
```

| 场景 | 群组 OWNER 数量 | 操作 | 结果 |
|------|----------------|------|------|
| Bob 是唯一 OWNER | 1（不含被编辑者） | 剥夺 Bob 的 OWNER | ❌ 抛异常 |
| Bob + Alice 都是 OWNER | 2（不含被编辑者） | 剥夺 Bob 的 OWNER | ✅ 允许（Alice 仍是 OWNER） |

**角色重赋的"先删后建"策略**：

```php
// 代码行 #L262-L272
// 先删除该用户在此群组的所有旧 GroupMembership
$user->groupMemberships()->where('user_group_id', $userGroup->id)->delete();

// 再按简化后的角色列表逐条创建
foreach ($rolesSimplified as $role) {
    $enum = UserRoleEnum::from($role);
    $userRole = UserRole::whereTitle($enum->value)->first();
    $user->groupMemberships()->create([
        'user_group_id' => $userGroup->id,
        'user_role_id'  => $userRole->id,
    ]);
}
```

这意味着角色更新是**原子性替换**——不存在"新旧角色叠加"的风险。

### 8.4 两条路径的可达性与功能对比

| 维度 | 路径 A（管理员邀请） | 路径 B（组内添加） |
|------|----------------------|---------------------|
| 操作者 | 全局 `owner` 角色 | 群组内 `OWNER` 或 `FULL` 角色（理论上） |
| 目标用户 | 未注册的新用户 | 已注册的现有用户 |
| 中间件保护 | `IsAdmin` | `user-full-auth` + `UserGroup::routeBinder`（路由活但不接组员操作） |
| 入口路由 | `POST /settings/users/invite`（活） | `PUT /v1/user-groups/{userGroup}`（活但只改标题/币种） |
| 组员变更专用端点 | 无 | `PUT /v1/user-groups/{id}/update-membership`（**路由注释，不可达**） |
| 入组后角色 | 自成新群组的 OWNER（与邀请者完全无关） | 由操作者指定（READ_ONLY ~ OWNER 任选，死代码） |
| 实际可达性 | ✅ 完全可达 | ❌ 无任何活入口 |
| 关键代码 | [UserRepository::inviteUser()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/User/UserRepository.php#L293-L310) | [UserGroupRepository::updateMembership()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php#L206-L275)（死代码） |

---

## 9. 五层可见性判定各挡哪类越权——逐层拆解

### 9.1 第 1 层：路由参数绑定（Route Binder）

**挡掉的越权类型**：**URL 篡改访问他人数据**

| Binder 实现 | 过滤方式 | 越权场景举例 |
|-------------|----------|-------------|
| `Account::routeBinder()` | `$user->accounts()->find($id)` | 用户 A 直接访问 `/accounts/show/42`，而 42 属于用户 B 的群组 |
| `UserGroupAccount::routeBinder()` | `WHERE user_group_id = $user->user_group_id` | 用户 A 已切换到群组 X，但仍试图用 URL 访问群组 Y 的账户 |
| `UserGroupTransaction::routeBinder()` | `WHERE user_group_id = $user->user_group_id` | 同上，但目标是交易组 |
| `UserGroupBill::routeBinder()` | `WHERE user_group_id = $user->user_group_id` | 同上，但目标是账单 |
| `UserGroup::routeBinder()` | `hasRoleInGroupOrOwner(READ_ONLY)` | 非成员试图访问群组编辑页 |

**关键区别**：
- 旧式 `routeBinder()`（如 `Account::routeBinder()`）通过 `$user->accounts()` 关系按 `user_id` 过滤
- 新式 `UserGroupXxx::routeBinder()` 按 `user_group_id` 过滤，**与用户当前激活群组绑定**，切换群组后自动改变可见范围

**单独依赖此层的缺陷**：只能挡住 URL 直接访问，无法阻止用户通过 API 参数提交不属于自己群组的 ID 作为关联字段（如创建交易时指定他人的账户 ID）。

### 9.2 第 2 层：Eloquent 关系查询

**挡掉的越权类型**：**列表页数据泄露**

控制器中常见的查询模式：

```php
// UserController::index()
$accounts = $user->accounts()->get();           // 只返回 user_id 匹配的
$journals = $userGroup->transactionJournals();  // 只返回 user_group_id 匹配的
```

**越权场景举例**：
- 用户 A 访问 `/accounts` 列表页，如果查询不限定 `user_id` 或 `user_group_id`，就会看到所有人的账户
- 列表 API（如 `GET /v1/accounts`）如果不通过关系查询，会返回跨群组数据

**单独依赖此层的缺陷**：只能控制"读列表"，无法阻止"单条记录的越权写操作"——因为写操作通常通过 `Model::find($id)` 直接定位，不走关系查询。

### 9.3 第 3 层：Collector 查询构建器

**挡掉的越权类型**：**复杂聚合查询的数据泄露**

`GroupCollector` 是交易数据查询的核心入口，用于列表、报表、搜索等场景。

| 初始化方式 | 生成的 SQL 起始条件 | 挡住什么 |
|------------|---------------------|----------|
| `setUser($user)` → `startQuery()` | `WHERE transaction_journals.user_id = ?` | 单用户模式下看到别人的交易 |
| `setUserGroup($group)` → `startQueryForGroup()` | `WHERE transaction_journals.user_group_id = ?` | 共享模式下看到其他群组的交易 |

**越权场景举例**：
- 用户 A 切换到群组 X 后查看交易列表，如果不通过 `startQueryForGroup()` 而直接 `TransactionJournal::all()`，就会看到所有群组的交易
- 报表/图表数据如果不用 Collector 而直接裸查，会泄露跨群组统计

**单独依赖此层的缺陷**：只能控制"查询阶段的数据范围"，无法阻止"写操作时关联了不属于当前群组的外键"。

### 9.4 第 4 层：表单请求授权（FormRequest Authorization）

**挡掉的越权类型**：**角色不符的操作请求**

此层回答的核心问题是：**"你在这个群组里有资格做这件事吗？"**

| 请求类 | acceptedRoles | 挡住的越权 |
|--------|---------------|-----------|
| `AccountFormRequest` | `[MANAGE_TRANSACTIONS]` | READ_ONLY 用户试图创建/编辑账户 |
| `BudgetFormStoreRequest` | `[]`（仅登录） | 未登录用户 |
| `InviteUserFormRequest` | `[]`（仅登录） | — 但由 `IsAdmin` 中间件在上游挡住 |
| `UserGroup\UpdateRequest` | `[]`（仅登录） | — 但 `UserGroup::routeBinder` 在上游挡住非成员 |

**`acceptedRoles` 为空数组的含义**：不检查组内角色，只要登录即可。这通常发生在：
1. 该操作已被上游中间件保护（如 `IsAdmin`）
2. 该操作需要更细粒度的后续检查（如 `BelongsUser` 验证规则）

**越权场景举例**：
- 用户 A 在群组 X 中只有 `READ_ONLY` 角色，试图 `POST /accounts` 创建账户 → `ChecksLogin::authorize()` 遍历 `acceptedRoles = [MANAGE_TRANSACTIONS]`，发现用户在群组 X 中不持有此角色 → ❌ 拒绝

**单独依赖此层的缺陷**：只检查"你能不能做这个动作"，不检查"你操作的对象是不是你的"。

### 9.5 第 5 层：输入数据归属验证

**挡掉的越权类型**：**外键关联越权（IDOR）**

此层是防止 IDOR（Insecure Direct Object Reference）的核心防线。

| 验证规则 | 检查方式 | 挡住的越权 |
|----------|----------|-----------|
| `BelongsUser` | `WHERE id = ? AND user_id = auth()->id()` | 创建交易时，提交属于他人账户的 `source_id` |
| `BelongsUserGroup` | `WHERE id = ? AND user_group_id = ?` | 同上，但按群组范围验证 |

**越权场景举例**：
- 用户 A 在群组 X 中有 `MANAGE_TRANSACTIONS` 角色，创建交易时提交 `source_id = 99`（账户 99 属于群组 Y）
  - 第 4 层检查通过（A 确实在 X 中有 `MANAGE_TRANSACTIONS`）
  - 第 5 层 `BelongsUser` 检查：`Account::where('id', 99)->where('user_id', A.id)->count()` = 0 → ❌ 拒绝

**API 中的等价层**：[ValidatesUserGroupTrait::validateUserGroup()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Http/Api/ValidatesUserGroupTrait.php#L52-L114) 在 API 控制器入口处执行三步检查：

```
① 用户已登录？（AuthenticationException）
② 用户是目标群组的成员？（AuthorizationException）
③ 用户在目标群组中持有 acceptedRoles 中的任一角色？（AuthorizationException）
```

### 9.6 五层协同——任何一层失效时其他层的兜底效果

| 假设失效的层 | 剩余层能否兜底 | 场景说明 |
|-------------|---------------|---------|
| 第 1 层失效（URL 直接访问无 routeBinder 的路由） | 第 4 层 + 第 5 层兜底 | 控制器中直接 `Model::find($id)` 时，FormRequest 授权和 BelongsUser 验证仍会拦截 |
| 第 2 层失效（控制器绕过关系查询） | 第 3 层 + 第 4 层兜底 | Collector 查询自带群组过滤，FormRequest 检查角色权限 |
| 第 4 层失效（acceptedRoles 为空数组） | 第 1 层 + 第 5 层兜底 | routeBinder 限制可见对象，BelongsUser 验证关联 IDOR |
| 第 5 层失效（无 BelongsUser 验证） | 第 1 层 + 第 2 层兜底 | routeBinder 已确保对象归属，关系查询限定列表范围 |

**注意**：第 3 层（Collector）失效是**最危险的**，因为复杂聚合查询（报表/图表）如果不用 Collector 而裸查数据库，且第 2 层的关系查询也被绕过，就可能出现跨群组数据泄露。这正是为什么 Firefly III 坚持所有交易查询都通过 `GroupCollector` 进行。

---

## 10. 双归属字段为何不能压成单字段

财务数据同时持有 `user_id` 和 `user_group_id` 两个归属字段，看似冗余，但两者承载的语义和功能完全不同，无法合并。

### 10.1 语义差异

```
user_id        = "谁创建了这条记录"  →  创建溯源（不可变事实）
user_group_id  = "这条记录对谁可见"  →  可见域边界（可随群组迁移而变）
```

如果压成单字段 `owner_id`，则"创建者"和"可见域"被迫绑定，以下场景无法处理：

### 10.2 场景 1：群组内多用户协作

```
群组 101 中：
  - Alice (user_id=1) 创建了账户 A
  - Bob   (user_id=2) 创建了账户 B

accounts 表：
┌────┬─────────┬───────────────┐
│ id │ user_id │ user_group_id │
├────┼─────────┼───────────────┤
│  A │    1    │     101       │  ← Alice 创建，群组 101 可见
│  B │    2    │     101       │  ← Bob   创建，群组 101 可见
└────┴─────────┴───────────────┘

如果只保留 user_id：
  - Alice 只能看到 A（WHERE user_id = 1）
  - Bob 只能看到 B（WHERE user_id = 2）
  - ❌ 群组协作失效！同一群组的成员看不到彼此的数据

如果只保留 user_group_id：
  - Alice 和 Bob 都能看到 A 和 B
  - ✅ 协作正常，但 ❌ 无法追溯"谁创建了这条记录"
```

### 10.3 场景 2：用户切换群组后的数据归属不变

```
Alice 当前激活群组 = 101
Alice 在群组 101 中创建了账户 A

之后 Alice 切换到群组 102

accounts 表中：
  账户 A 的 user_id = 1（仍是 Alice，不变）
  账户 A 的 user_group_id = 101（仍是群组 101，不变）

如果只有 user_id：
  - Alice 切换群组后，她的查询 WHERE user_id = 1
  - 会同时返回群组 101 和群组 102 的数据
  - ❌ 群组隔离被打破！

如果只有 user_group_id：
  - 切换后 WHERE user_group_id = 102
  - Alice 看不到自己在群组 101 创建的数据
  - ✅ 隔离正确，但 ❌ 丢失了"Alice 是原始创建者"这一审计信息
```

### 10.4 场景 3：旧式 routeBinder 的兼容性

[Account::routeBinder()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/Account.php#L67-L86) 使用 `$user->accounts()->find($id)`，底层生成 `WHERE accounts.user_id = ?`。

如果去掉 `user_id` 字段：
- 所有旧式 `routeBinder()` 全部失效
- 所有 `$user->accounts()` 等 Eloquent 关系全部失效
- 必须全面改写为 `$userGroup->accounts()`，但这又要求每个查询都先确定群组

`user_id` 的存在使得**单用户模式**（无群组概念）完全兼容，不需要任何额外逻辑。

### 10.5 场景 4：UserGroupAccount 等 Binder 的精确过滤

[UserGroupAccount::routeBinder()](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Binder/UserGroupAccount.php#L39-L54) 的过滤条件是：

```php
Account::where('id', (int) $value)
    ->where('user_group_id', $user->user_group_id)
    ->first();
```

如果去掉 `user_group_id`，只能退化为 `WHERE user_id = ?`，此时：
- 用户切换群组后仍然能看到旧群组的数据（因为 `user_id` 没变）
- 群组隔离完全失效

### 10.6 两个字段各自的查询角色

| 查询场景 | 使用的字段 | SQL 示例 |
|----------|-----------|---------|
| 单用户列表页 | `user_id` | `WHERE accounts.user_id = 1` |
| 群组列表页 | `user_group_id` | `WHERE accounts.user_group_id = 101` |
| 单条记录路由绑定（旧式） | `user_id` | `WHERE id = 42 AND user_id = 1` |
| 单条记录路由绑定（新式） | `user_group_id` | `WHERE id = 42 AND user_group_id = 101` |
| 创建新记录 | 两者同时写入 | `INSERT (user_id=1, user_group_id=101, ...)` |
| 审计/溯源查询 | `user_id` | "找出 Alice 创建的所有账户" |
| 可见域切换 | `user_group_id` | "切换到群组 102 后只看该群组的数据" |

**结论**：`user_id` 是**不可变的溯源锚点**，`user_group_id` 是**可变的可见域开关**。两者服务于不同的查询维度和安全需求，压缩为单字段必然导致至少一个维度的能力丧失。在 Firefly III 的渐进式架构演进中（先有单用户模式，后加群组共享），双字段是保持向后兼容和功能完整的最小代价设计。

---

## 11. 关键设计特点总结

### 11.1 双归属字段（user_id + user_group_id）
每条财务数据同时记录 **创建者** 和 **所属行政群组**：
- `user_id`：保留创建溯源，兼容单用户模式
- `user_group_id`：定义共享可见域，支持多用户协作

### 11.2 权限与范围分离
- **可见范围**：由 `user_group_id` 决定（你能看到什么）
- **操作权限**：由 `GroupMembership` 中的 `UserRole` 决定（你能做什么）

### 11.3 多层冗余校验
路由绑定 → FormRequest 授权 → 输入验证 → Repository 查询 → 数据库级关系，**任意一层未通过即拒绝**，避免单点疏漏。

### 11.4 高权限自动覆盖
OWNER 覆盖所有，FULL 覆盖大部分，简化角色分配逻辑，防止出现"有 OWNER 却无 READ 权限"的矛盾配置。

### 11.5 向后兼容设计
所有旧的 `$user->accounts()` 查询依然有效（按 user_id 过滤）；新增群组协作时使用 `$userGroup->accounts()`（按 user_group_id 过滤）。

---

## 12. 核心文件索引

| 类别 | 文件 |
|------|------|
| **核心模型** | [User.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/User.php)、[UserGroup.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/UserGroup.php)、[GroupMembership.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/GroupMembership.php)、[UserRole.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/UserRole.php)、[InvitedUser.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Models/InvitedUser.php) |
| **角色枚举** | [UserRoleEnum.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Enums/UserRoleEnum.php) |
| **群组仓库** | [UserGroupRepository.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/UserGroup/UserGroupRepository.php) |
| **群组 Trait** | [UserGroupTrait.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Repositories/UserGroup/UserGroupTrait.php) |
| **请求授权** | [ChecksLogin.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Request/ChecksLogin.php)、[ValidatesUserGroupTrait.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Http/Api/ValidatesUserGroupTrait.php) |
| **验证规则** | [BelongsUser.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Rules/BelongsUser.php)、[BelongsUserGroup.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Rules/BelongsUserGroup.php) |
| **路由绑定** | [Binder.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Middleware/Binder.php)、[UserGroupAccount.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Binder/UserGroupAccount.php)、[UserGroupTransaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Binder/UserGroupTransaction.php)、[UserGroupBill.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Support/Binder/UserGroupBill.php) |
| **管理员中间件** | [IsAdmin.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Middleware/IsAdmin.php)、[IsAdminApi.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Middleware/IsAdminApi.php) |
| **数据收集器** | [GroupCollector.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Helpers/Collector/GroupCollector.php) |
| **注册初始化** | [HandlesNewUserRegistration.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Listeners/Security/System/HandlesNewUserRegistration.php)、[UserGroupFactory.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Factory/UserGroupFactory.php) |
| **邀请流程** | [UserController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Controllers/Admin/UserController.php)、[RegisterController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Http/Controllers/Auth/RegisterController.php)、[NotifiesAboutNewInvitation.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Listeners/Security/System/NotifiesAboutNewInvitation.php)、[InvitationMail.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Mail/InvitationMail.php) |
| **用户仓库** | [UserRepository.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Repositories/User/UserRepository.php) |
| **API 群组控制器** | [UpdateController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/app/Api/V1/Controllers/Models/UserGroup/UpdateController.php) |
| **Web 路由** | [web.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/routes/web.php#L1451-L1469) |
| **API 路由** | [api.php](file:///d:/fz/0601-1/solo-dogfeeding/code/69-firefly-iii/routes/api.php#L344-L354) |
