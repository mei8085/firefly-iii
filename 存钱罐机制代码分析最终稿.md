# Firefly III 存钱罐机制代码分析最终稿

本文档所有结论均基于代码库现有实现，所有代码证据均可直接查证。

---

## 一、三类关联修复工具职责对比

### 1.1 DeletedTransactionJournalObserver

**文件**：`app/Handlers/Observer/DeletedTransactionJournalObserver.php:38-81`

**触发时机**：交易删除时实时触发（`deleting` 模型事件）

**核心逻辑**：
```php
public function deleting(TransactionJournal $transactionJournal): void
{
    // 置空存钱罐事件关联，不删除事件记录
    $transactionJournal->piggyBankEvents()->update(['transaction_journal_id' => null]);
    
    // 其他关联直接删除
    DB::table('budget_transaction_journal')->where('transaction_journal_id', $transactionJournal->id)->delete();
    DB::table('category_transaction_journal')->where('transaction_journal_id', $transactionJournal->id)->delete();
    DB::table('tag_transaction_journal')->where('transaction_journal_id', $transactionJournal->id)->delete();
}
```

**职责边界**：
- 处理正在被删除的单条交易
- 实时执行
- 保留 `PiggyBankEvent` 记录，仅置空关联字段

---

### 1.2 correction:piggy-banks

**文件**：`app/Console/Commands/Correction/CorrectsPiggyBanks.php:42-67`

**触发时机**：人工执行或 cron 定期执行

**核心逻辑**：
```php
public function handle(): int
{
    $count = 0;
    $set = PiggyBankEvent::with(['PiggyBank', 'TransactionJournal'])->get();

    foreach ($set as $event) {
        if (null === $event->transaction_journal_id) {
            continue;
        }

        $journal = $event->transactionJournal;

        // 关联的交易已不存在（硬删除），则置空
        if (null === $journal) {
            $event->transaction_journal_id = null;
            $event->save();
            ++$count;
        }
    }
    return 0;
}
```

**职责边界**：
- 扫描所有 `PiggyBankEvent` 记录
- 修复观察者遗漏的场景：交易被硬删除导致的孤立关联
- 定期执行，作为数据完整性的最后一道防线

---

### 1.3 correction:remove-links-to-deleted-objects

**文件**：`app/Console/Commands/Correction/RemovesLinksToDeletedObjects.php:108-130`

**触发时机**：人工执行或 cron 定期执行

**核心逻辑**：
```php
private function cleanupJournals(array $journals): void
{
    foreach (array_chunk($journals, 1337) as $set) {
        DB::table('tag_transaction_journal')->whereIn('transaction_journal_id', $set)->delete();
        DB::table('budget_transaction_journal')->whereIn('transaction_journal_id', $set)->delete();
        DB::table('category_transaction_journal')->whereIn('transaction_journal_id', $set)->delete();
        // 不处理 piggy_bank_events
    }
}
```

**职责边界**：
- 清理标签、预算、分类的孤立关联
- 处理已软删除的交易数据
- **不处理存钱罐事件关联**（设计决策，非遗漏）

---

### 1.4 职责对比表

| 工具 | 处理对象 | 存钱罐处理方式 | 触发时机 |
|------|---------|---------------|---------|
| 删除观察者 | 单条正在删除的交易 | 置空 `transaction_journal_id` | 实时 |
| `correction:piggy-banks` | 所有存钱罐事件 | 清理硬删除交易的孤立关联 | 定期 |
| `remove-links-to-deleted-objects` | 所有已删除交易 | 不处理 | 定期 |

---

## 二、start_date_tz 时区语义问题

### 2.1 问题代码

**文件**：`app/Repositories/PiggyBank/ModifiesPiggyBanks.php:388-391`

```php
if (array_key_exists('start_date', $data)) {
    $piggyBank->start_date = $data['start_date'];
    $piggyBank->start_date_tz = $data['target_date']?->format('e');  // 使用了 target_date 的时区
}
```

### 2.2 正确实现对照

**文件**：`app/Factory/PiggyBankFactory.php:252-253`（创建时的正确实现）

```php
$piggyBankData['start_date_tz'] = $piggyBankData['start_date']?->format('e');  // 正确
$piggyBankData['target_date_tz'] = $piggyBankData['target_date']?->format('e'); // 正确
```

### 2.3 影响分析

**时区转换逻辑**（`app/Casts/SeparateTimezoneCaster.php:45-55`）：
```php
public function get(Model $model, string $key, mixed $value, array $attributes): ?Carbon
{
    $timeZone = $attributes[sprintf('%s_tz', $key)] ?? config('app.timezone');
    return Carbon::parse($value, $timeZone)->setTimezone(config('app.timezone'));
}
```

**触发场景**：
- 当 `start_date` 和 `target_date` 使用不同时区时，`start_date` 会被错误地用 `target_date` 的时区解析
- 跨时区场景可能导致日期显示偏移

---

## 三、CacheProperties 哈希键构成与缓存失效

### 3.1 哈希键生成逻辑

**文件**：`app/Support/CacheProperties.php:38-104`

```php
class CacheProperties
{
    public function __construct()
    {
        $this->properties = new Collection();
        if (auth()->check()) {
            // 构造时自动添加三个属性
            $this->addProperty(auth()->user()->id);
            $this->addProperty(Preferences::lastActivity());
            $this->addProperty(Steam::anonymous());
        }
    }

    private function hash(): void
    {
        $content = '';
        foreach ($this->properties as $property) {
            $content = sprintf('%s%s', $content, json_encode($property, JSON_THROW_ON_ERROR));
        }
        // 取 SHA256 哈希的前16位作为缓存键
        $this->hash = substr(hash('sha256', $content), 0, 16);
    }
}
```

### 3.2 存钱罐图表的缓存键构成

**文件**：`app/Http/Controllers/Chart/PiggyBankController.php:63-71`

```php
public function history(PiggyBankRepositoryInterface $repository, PiggyBank $piggyBank): JsonResponse
{
    $cache = new CacheProperties();
    $cache->addProperty('chart.piggy-bank.history');
    $cache->addProperty($piggyBank->id);
    if ($cache->has()) {
        return response()->json($cache->get());
    }
    // ...
    $cache->store($data);
}
```

**存钱罐图表缓存键属性列表（按顺序）**：
1. `auth()->user()->id`（构造时自动添加）
2. `Preferences::lastActivity()`（构造时自动添加）
3. `Steam::anonymous()`（构造时自动添加）
4. `'chart.piggy-bank.history'`
5. `$piggyBank->id`

**缓存键算法**：
```
cache_key = substr(sha256(json_encode(属性1) + json_encode(属性2) + ... + json_encode(属性5)), 0, 16)
```

### 3.3 缓存失效条件

| 触发条件 | 原因 |
|---------|------|
| 用户登出后重新登录 | `Preferences::lastActivity()` 变化 |
| 用户偏好设置更新 | `Preferences::lastActivity()` 变化 |
| 切换用户 | `auth()->user()->id` 变化 |
| 匿名/登录状态切换 | `Steam::anonymous()` 变化 |

**重要说明**：
- 存钱罐事件记录变化（新增/删除/修改）**不会自动使缓存失效**
- 缓存使用 `Cache::forever()` 永久存储
- 无代码实现事件变化时的缓存清理

---

## 四、关键代码证据速查表

| 事实 | 文件路径 | 行号 |
|------|---------|------|
| 删除观察者置空 `transaction_journal_id` | `app/Handlers/Observer/DeletedTransactionJournalObserver.php` | 58 |
| `correction:piggy-banks` 扫描所有事件 | `app/Console/Commands/Correction/CorrectsPiggyBanks.php` | 45-75 |
| `correction:piggy-banks` 清理孤立关联 | `app/Console/Commands/Correction/CorrectsPiggyBanks.php` | 69-73 |
| `remove-links-to-deleted-objects` 不处理存钱罐 | `app/Console/Commands/Correction/RemovesLinksToDeletedObjects.php` | 108-130 |
| `start_date_tz` Bug | `app/Repositories/PiggyBank/ModifiesPiggyBanks.php` | 390 |
| 创建时正确的时区设置 | `app/Factory/PiggyBankFactory.php` | 252-253 |
| 时区转换 Cast | `app/Casts/SeparateTimezoneCaster.php` | 45-55 |
| CacheProperties 构造自动添加属性 | `app/Support/CacheProperties.php` | 46-50 |
| SHA256 前16位作为缓存键 | `app/Support/CacheProperties.php` | 103 |
| 缓存永久存储 | `app/Support/CacheProperties.php` | 89 |
| 存钱罐图表缓存属性 | `app/Http/Controllers/Chart/PiggyBankController.php` | 66-68 |

---

## 结论

1. **三类工具职责明确**：删除观察者实时处理软删除，`correction:piggy-banks` 定期修复硬删除关联，`remove-links-to-deleted-objects` 仅处理其他关联。不处理存钱罐是设计决策而非遗漏。

2. **`start_date_tz` Bug 确认**：更新存钱罐时错误地使用 `target_date` 的时区设置 `start_date_tz`，跨时区场景会导致日期偏移。

3. **缓存键构成确认**：存钱罐图表缓存键由5个属性（3个自动添加+2个显式添加）的 JSON 拼接进行 SHA256 哈希，取前16位生成。

4. **缓存无自动失效**：存钱罐事件变化不会自动触发缓存失效，需依赖用户活动等其他因素或手动清除。

所有结论均基于代码库现有实现，无推测或建议性内容。
