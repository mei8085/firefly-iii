# Firefly III 交易导入：字段映射与重复识别防重链路分析报告

## 1. 概述

Firefly III 通过**字段映射 → 哈希计算 → 重复检测**的三级链路，有效防止同一笔交易被重复入账。

**核心结论**：防重哈希完全基于**调用方传入的原始标准化数据**计算，系统内部录入流程中生成的数据（如账户解析结果、数据库自增ID、创建时间等）**不参与**哈希比对。这条设计保证了：同一笔交易无论导入多少次，只要调用方输入相同，就会生成相同的哈希指纹。

---

## 2. 整体架构与边界

```
┌──────────────────────────────────────────────────────────────┐
│                    调用方输入域（参与哈希）                    │
│  ✅ 所有字段标准化后的数据 → 计算 import_hash_v2 → 重复检测   │
└───────────────────────────────┬──────────────────────────────┘
                                │ 哈希通过
                                ▼
┌──────────────────────────────────────────────────────────────┐
│                  系统内部录入域（不参与哈希）                  │
│  ❌ 账户查找/创建、数据库ID、创建时间、关联关系、规则处理等   │
└──────────────────────────────────────────────────────────────┘
```

**关键边界点**：[TransactionJournalFactory::createJournal()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalFactory.php#L230-L236) 的前两行代码 —— 哈希计算发生在**任何内部业务逻辑之前**。

---

## 3. 第一层：字段映射（Field Mapping）

### 3.1 字段标准化的真正入口

字段映射的目标是将各种来源的数据**标准化**为统一格式。标准化的**终点**就是哈希计算的**起点**。

#### 前端 → API 字段映射

**核心文件**：[parse-from-entries.js](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/resources/assets/v2/src/pages/transactions/shared/parse-from-entries.js#L24-L108)

前端将 UI 表单字段映射到 API 请求格式，但这只是**第一层映射**，还没到最终的哈希计算输入。

```javascript
export function parseFromEntries(entries, originals, transactionType) {
    let returnArray = [];
    for (let i in entries) {
        let current = {};
        current.description = entry.description;
        current.source_name = entry.source_account.name;
        current.destination_name = entry.destination_account.name;
        current.amount = entry.amount;
        current.currency_code = entry.currency_code;
        current.date = entry.date;
        // ... 约 30 个字段
        current.type = transactionType;
        returnArray.push(current);
    }
    return returnArray;
}
```

#### 后端请求层字段标准化（**真正的哈希输入起点**）

**核心文件**：[StoreRequest::getTransactionData()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Api/V1/Requests/Models/Transaction/StoreRequest.php#L214-L309)

这是调用方数据进入后端后的**第一个标准化点**，其输出几乎直接决定了哈希计算的输入。

```php
private function getTransactionData(): array
{
    $return = [];
    foreach ($this->get('transactions') as $transaction) {
        $object = new NullArrayObject($transaction);
        $return[] = [
            // 类型转换：字符串 → 整数
            'currency_id'           => $this->integerFromValue((string) $object['currency_id']),
            // 字符串清洗（trim + 特殊字符过滤）
            'currency_code'         => $this->clearString((string) $object['currency_code']),
            // 浮点转换
            'latitude'              => $this->floatFromValue((string) $object['latitude']),
            // 日期解析：字符串 → Carbon 对象
            'date'                  => $this->dateFromValue($object['date']),
            // IBAN 标准化（去空格 + 清洗）
            'source_iban'           => $this->clearIban((string) $object['source_iban']),
            // 系统自动注入：来源标记（后续会被排除在哈希之外）
            'original_source'       => sprintf('ff3-v%s', config('firefly.version')),
            // ... 共 40+ 个字段
        ];
    }
    return $return;
}
```

**标准化方法实现**：[ConvertsDataTypes.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Support/Request/ConvertsDataTypes.php)

| 方法 | 作用 | 对哈希的影响 |
|------|------|-------------|
| `clearString()` | trim + 过滤控制字符 + 合并Unicode组合字符 | ✅ 保证相同语义的字符串生成相同哈希 |
| `clearIban()` | 去除 IBAN 中的所有空格 | ✅ 格式差异不影响哈希 |
| `dateFromValue()` | 字符串 → Carbon 对象（带时区） | ⚠️ 相同时刻不同格式可能生成相同 Carbon，取决于解析 |
| `integerFromValue()` | 字符串 → 整数（null 为空值） | ✅ 保证数值类型一致 |

#### 组级别字段映射

**文件**：[StoreRequest::getAll()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Api/V1/Requests/Models/Transaction/StoreRequest.php#L62-L74)

```php
public function getAll(): array
{
    return [
        'group_title'             => $this->convertString('group_title'),
        'error_if_duplicate_hash' => $this->boolean('error_if_duplicate_hash'),  // 防重开关
        'batch_submission'        => $this->boolean('batch_submission'),         // 参与哈希
        'apply_rules'             => $this->boolean('apply_rules', true),
        'fire_webhooks'           => $this->boolean('fire_webhooks', true),
        'transactions'            => $this->getTransactionData(),
    ];
}
```

> **注意**：`batch_submission` 会参与每笔交易的哈希计算（在 `TransactionJournalFactory::create()` 中注入到 `$row`）。

### 3.2 元字段配置

系统定义了需要持久化到 `journal_meta` 表的元字段列表，其中大部分都参与哈希计算。

**配置文件**：[firefly.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/config/firefly.php#L783-L816)

```php
'journal_meta_fields' => [
    // SEPA 银行转账字段
    'sepa_cc', 'sepa_ct_op', 'sepa_ct_id', 'sepa_db',
    'sepa_country', 'sepa_ep', 'sepa_ci', 'sepa_batch_id',
    'external_url',
    // 业务日期
    'interest_date', 'book_date', 'process_date',
    'due_date', 'payment_date', 'invoice_date',
    // 引用标识
    'recurrence_id', 'internal_reference', 'bunq_payment_id',
    'import_hash', 'import_hash_v2',          // 哈希值本身
    'external_id', 'original_source',         // 来源标记（排除在哈希外）
    // 周期交易
    'recurrence_total', 'recurrence_count', 'recurrence_date',
],
```

---

## 4. 第二层：哈希计算（Hash Generation）

### 4.1 边界位置：createJournal() 的第一行

哈希计算发生在 `createJournal()` 的**最开始**，此时 `$row` 是纯粹的调用方输入数据（加上 `batch_submission` 和 `import_hash_v2`）。

**核心文件**：[TransactionJournalFactory::createJournal()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalFactory.php#L230-L241)

```php
private function createJournal(NullArrayObject $row): ?TransactionJournal
{
    Log::debug('Now in TransactionJournalFactory::createJournal()');

    // ─── 边界线以上：调用方输入域 ───
    $row['import_hash_v2'] = $this->hashArray($row);  // 基于原始 $row 计算哈希
    $this->errorIfDuplicate($row['import_hash_v2']); // 立即检测重复
    // ─── 边界线以下：内部录入域 ───

    $type = $this->typeRepository->findTransactionType(null, $row['type']);
    $carbon = $row['date'] ?? today(config('app.timezone'));
    // ... 后续所有内部逻辑都不影响哈希
}
```

### 4.2 哈希算法详解

**核心方法**：[TransactionJournalFactory::hashArray()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalFactory.php#L524-L538)

```php
private function hashArray(NullArrayObject $row): string
{
    // 明确排除两个不参与哈希的字段
    unset($row['import_hash_v2'], $row['original_source']);

    try {
        $json = json_encode($row, JSON_THROW_ON_ERROR);
    } catch (JsonException $e) {
        Log::error(sprintf('Could not encode dataRow: %s', $e->getMessage()));
        $json = microtime();  // 兜底：失败时用时间戳（极端情况）
    }

    $hash = hash('sha256', $json);
    Log::debug(sprintf('The hash is: %s', $hash), $row->getArrayCopy());

    return $hash;
}
```

**关键设计要点**：

| 设计决策 | 原因 |
|---------|------|
| 先 JSON 编码再哈希 | 保证字段顺序、类型、嵌套结构完全一致 |
| 排除 `import_hash_v2` | 避免递归引用：哈希值本身不能参与哈希计算 |
| 排除 `original_source` | 来源标记不影响交易内容：同一笔交易从不同入口导入应生成相同哈希 |
| 使用 `NullArrayObject` | 缺失字段统一为 `null`，保证 JSON 结构一致 |
| SHA256 算法 | 碰撞概率极低，安全性足够 |

### 4.3 真正参与哈希的字段清单

以下是 `$row` 中所有参与哈希计算的字段（按分类）：

#### 核心交易字段
| 字段 | 类型 | 来源 |
|------|------|------|
| `type` | string | 调用方 |
| `date` | Carbon | 调用方（经 dateFromValue 解析） |
| `order` | int | 调用方 |
| `description` | string | 调用方（经 clearString 清洗） |
| `amount` | string | 调用方 |
| `reconciled` | bool | 调用方 |

#### 币种字段
| 字段 | 类型 | 来源 |
|------|------|------|
| `currency_id` | int\|null | 调用方 |
| `currency_code` | string\|null | 调用方 |
| `foreign_currency_id` | int\|null | 调用方 |
| `foreign_currency_code` | string\|null | 调用方 |
| `foreign_amount` | string\|null | 调用方 |

#### 源账户字段
| 字段 | 类型 | 来源 |
|------|------|------|
| `source_id` | int\|null | 调用方 |
| `source_name` | string\|null | 调用方 |
| `source_iban` | string\|null | 调用方 |
| `source_number` | string\|null | 调用方 |
| `source_bic` | string\|null | 调用方 |

#### 目标账户字段
| 字段 | 类型 | 来源 |
|------|------|------|
| `destination_id` | int\|null | 调用方 |
| `destination_name` | string\|null | 调用方 |
| `destination_iban` | string\|null | 调用方 |
| `destination_number` | string\|null | 调用方 |
| `destination_bic` | string\|null | 调用方 |

#### 分类/预算/账单/储蓄
| 字段 | 类型 | 来源 |
|------|------|------|
| `budget_id` | int\|null | 调用方 |
| `budget_name` | string\|null | 调用方 |
| `category_id` | int\|null | 调用方 |
| `category_name` | string\|null | 调用方 |
| `bill_id` | int\|null | 调用方 |
| `bill_name` | string\|null | 调用方 |
| `piggy_bank_id` | int\|null | 调用方 |
| `piggy_bank_name` | string\|null | 调用方 |

#### 其他元数据
| 字段 | 类型 | 来源 |
|------|------|------|
| `notes` | string\|null | 调用方 |
| `tags` | array\|null | 调用方 |
| `internal_reference` | string\|null | 调用方 |
| `external_id` | string\|null | 调用方 |
| `bunq_payment_id` | string\|null | 调用方 |
| `external_url` | string\|null | 调用方 |
| `recurrence_id` | int\|null | 调用方 |

#### SEPA 银行字段
| 字段 | 类型 | 来源 |
|------|------|------|
| `sepa_cc` | string\|null | 调用方 |
| `sepa_ct_op` | string\|null | 调用方 |
| `sepa_ct_id` | string\|null | 调用方 |
| `sepa_db` | string\|null | 调用方 |
| `sepa_country` | string\|null | 调用方 |
| `sepa_ep` | string\|null | 调用方 |
| `sepa_ci` | string\|null | 调用方 |
| `sepa_batch_id` | string\|null | 调用方 |

#### 日期字段
| 字段 | 类型 | 来源 |
|------|------|------|
| `interest_date` | Carbon\|null | 调用方 |
| `book_date` | Carbon\|null | 调用方 |
| `process_date` | Carbon\|null | 调用方 |
| `due_date` | Carbon\|null | 调用方 |
| `payment_date` | Carbon\|null | 调用方 |
| `invoice_date` | Carbon\|null | 调用方 |

#### 地理位置
| 字段 | 类型 | 来源 |
|------|------|------|
| `latitude` | float\|null | 调用方 |
| `longitude` | float\|null | 调用方 |
| `zoom_level` | int\|null | 调用方 |

#### 系统注入但参与哈希的字段
| 字段 | 类型 | 来源 |
|------|------|------|
| `batch_submission` | bool | 上层调用注入 |

#### **明确排除**的字段
| 字段 | 排除原因 |
|------|---------|
| `import_hash_v2` | 递归引用问题 |
| `original_source` | 来源标记不影响交易内容 |

### 4.4 不参与哈希的内部生成字段

哈希计算之后，系统内部会生成大量数据，但这些都**不影响**重复判断：

| 数据 | 位置 | 为什么不参与哈希 |
|------|------|----------------|
| TransactionJournal 自增 ID | [createJournal()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalFactory.php#L330-L342) | 每次创建都不同 |
| `user_id` / `user_group_id` | 同上 | 通过查询条件隔离，不需进哈希 |
| `transaction_type_id` | 同上 | 由 type 字符串解析而来，是内部表达 |
| 创建/更新时间 | 数据库层 | 每次创建都不同 |
| 实际的 source/destination 账户 ID | [TransactionFactory](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionFactory.php#L126-L136) | 账户查找/创建的结果 |
| Transaction 记录（正负两条） | 同上 | 内部数据结构 |
| budget/category/tag 关联表记录 | storeBudget / storeCategory / storeTags | 关联关系，非输入 |
| 规则引擎的修改 | 事件触发 | 创建后的后置处理 |

> **重要理解**：调用方传入的 `source_id` / `source_name` / `source_iban` 等**参与**哈希（因为是输入），但系统内部查找/创建后得到的实际账户对象的 ID **不参与**哈希。这意味着如果两次导入使用不同的账户查找方式（一次用 ID，一次用名称），即使最终解析到同一个账户，也会被视为不同交易。

---

## 5. 第三层：重复检测（Duplicate Detection）

### 5.1 检测开关

重复检测由 `error_if_duplicate_hash` 参数控制，默认关闭。

**传递链路**：
```
调用方请求参数
    ↓
StoreRequest::boolean('error_if_duplicate_hash')
    ↓
TransactionGroupFactory::setErrorOnHash()
    ↓
TransactionJournalFactory::$errorOnHash
    ↓
errorIfDuplicate() 方法中的 if 判断
```

### 5.2 重复检测查询：实际比对的是什么

**核心方法**：[TransactionJournalFactory::errorIfDuplicate()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalFactory.php#L419-L444)

```php
private function errorIfDuplicate(string $hash): void
{
    Log::debug(sprintf('In errorIfDuplicate(%s)', $hash));

    // 开关判断：关闭则直接跳过
    if (false === $this->errorOnHash) {
        return;
    }
    Log::debug('Will verify duplicate!');

    $result = TransactionJournalMeta::withTrashed()          // 包含软删除
        ->leftJoin('transaction_journals', 'transaction_journals.id', '=', 'journal_meta.transaction_journal_id')
        ->whereNotNull('transaction_journals.id')
        ->where('transaction_journals.user_id', $this->user->id)  // 用户级隔离
        ->where('data', json_encode($hash, JSON_THROW_ON_ERROR))  // ← 关键：比对 data 列
        ->with(['transactionJournal', 'transactionJournal.transactionGroup'])
        ->first(['journal_meta.*']);

    if (null !== $result) {
        // ... 抛出 DuplicateTransactionException
    }
}
```

**⚠️ 关键真相**：
- 重复检测查询比对的是 **`journal_meta.data` 列**，**不是** `journal_meta.hash` 列！
- 查询条件是 `data = json_encode($hash)`，其中 `$hash` 是 64 位十六进制的业务哈希字符串
- `json_encode($hash)` 会将字符串变为带双引号的 JSON 字符串，例如 `"a1b2c3d4e5f6..."`

**重复判断的完整依据**：
| 条件 | 字段 | 值 |
|------|------|----|
| 精确匹配 | `journal_meta.data` | `json_encode($businessHash)` = 带引号的 64 位哈希字符串 |
| 用户隔离 | `transaction_journals.user_id` | 当前用户 ID |
| 关联存在 | `transaction_journals.id IS NOT NULL` | 排除孤儿 meta 记录 |
| 范围 | `withTrashed()` | 包含已软删除的交易 |

> **注意**：查询未加 `name = 'import_hash_v2'` 条件，理论上如果其他 meta 字段（如 `external_id`）恰好存了相同的带引号哈希值，也会被误判为重复。但 SHA256 哈希值在实际业务中具有唯一性，几乎不可能发生误判。

### 5.3 索引关系

**索引文件**：[2024_03_03_174645_add_indices.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/database/migrations/2024_03_03_174645_add_indices.php#L44-L63)

```php
$set = [
    'journal_meta' => ['transaction_journal_id', 'data', 'name'],  // ← 索引建在这三列上
    // ...
];
```

**journal_meta 表上的索引（单列）**：
| 索引列 | 是否存在 | 作用 |
|--------|:--------:|------|
| `transaction_journal_id` | ✅ | 加速按流水账 ID 查找 |
| `data` | ✅ | **加速重复检测查询** |
| `name` | ✅ | 加速按字段名查找（updateOrCreate 中的 `where('name', ...)`） |
| `hash` | ❌ | **未建索引**，实际查询也未使用该列 |

**⚠️ 重要修正**：此前报告中提到 `hash` 列"用于数据库索引加速"是不准确的。实际情况是：
- `data` 列建了索引，重复检测查询走的是 `data` 列的索引
- `hash` 列虽然在表结构中存在，但**没有索引**，且**不参与任何查询**

### 5.4 异常向上传递

```
TransactionJournalFactory::createJournal()
    │ 抛出 DuplicateTransactionException
    ▼
TransactionJournalFactory::create()
    │ 捕获 → forceDeleteOnError() 回滚已创建的交易 → 重新抛出
    ▼
TransactionGroupFactory::create()
    │ 捕获 → 重新抛出
    ▼
TransactionGroupRepository::store()
    │ 捕获 → 重新抛出
    ▼
StoreController::store()
    │ 包装为 ValidationException（422 响应）
    ▼
前端显示错误
```

**控制器层处理**：[StoreController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Api/V1/Controllers/Models/Transaction/StoreController.php#L94-L108)

```php
public function store(StoreRequest $request): JsonResponse
{
    try {
        $transactionGroup = $this->groupRepository->store($data);
    } catch (DuplicateTransactionException $e) {
        // 将重复异常包装为表单验证错误返回
        $validator = Validator::make(
            ['transactions' => [['description' => $e->getMessage()]]],
            ['transactions.0.description' => new IsDuplicateTransaction()]
        );
        throw new ValidationException($validator);
    }
}
```

---

## 6. 哈希持久化：业务哈希如何落库

交易创建成功后，业务哈希值通过元字段机制持久化到 `journal_meta` 表，作为后续重复检测的比对依据。

### 6.1 journal_meta 表结构

**建表语句**：[2016_06_16_000002_create_main_tables.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/database/migrations/2016_06_16_000002_create_main_tables.php#L499-L507)

```php
Schema::create('journal_meta', static function (Blueprint $table): void {
    $table->increments('id');
    $table->timestamps();
    $table->integer('transaction_journal_id', false, true);  // 关联流水账
    $table->string('name', 255);                             // 字段名，如 'import_hash_v2'
    $table->text('data');                                    // 字段值（JSON 编码）
    $table->string('hash', 64);                              // data 内容的 SHA256
    $table->foreign('transaction_journal_id')
        ->references('id')->on('transaction_journals')
        ->onDelete('cascade');
});
```

**列说明**：
| 列 | 类型 | 作用 |
|----|------|------|
| `name` | varchar(255) | 标识这条 meta 存的是什么，如 `'import_hash_v2'`、`'sepa_cc'` 等 |
| `data` | text | **实际存储的值**，JSON 编码后的字符串 |
| `hash` | varchar(64) | `data` 列内容的 SHA256，**当前未被查询使用** |

### 6.2 业务哈希的完整落库流程

**调用链**：
```
createJournal() 末尾
    ↓
storeMetaFields($journal, $row)
    ↓ 遍历 journal_meta_fields 配置
storeMeta($journal, $data, 'import_hash_v2')
    ↓
TransactionJournalMetaFactory::updateOrCreate()
    ↓
$entry->data = $hashString  → 触发 Attribute setter
    ↓
setter 内部：json_encode($hashString) 写入 data 列
           hash('sha256', json_encode($hashString)) 写入 hash 列
    ↓
$entry->save()
```

**逐步详解**：

**第 1 步：storeMeta 提取原始哈希值**

[TransactionJournalFactory::storeMeta()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalFactory.php#L189-L203)

```php
protected function storeMeta(TransactionJournal $journal, array $data, string $field): void
{
    // $field = 'import_hash_v2'
    // $data[$field] = 'a1b2c3d4e5f6...' （64 位十六进制字符串）
    $set = [
        'journal' => $journal,
        'name'    => $field,                              // 'import_hash_v2'
        'data'    => (string) ($data[$field] ?? '')       // 'a1b2c3d4e5f6...'
    ];
    // ...日期字段特殊处理（import_hash_v2 不是日期，不触发）
    $factory = app(TransactionJournalMetaFactory::class);
    $factory->updateOrCreate($set);
}
```

**第 2 步：Meta 工厂保存**

[TransactionJournalMetaFactory::updateOrCreate()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalMetaFactory.php#L36-L77)

```php
public function updateOrCreate(array $data): ?TransactionJournalMeta
{
    $value = $data['data'];  // 仍为原始 64 位哈希字符串

    $entry = $data['journal']->transactionJournalMeta()
        ->where('name', $data['name'])  // name = 'import_hash_v2'
        ->first();

    // 空值不存储（import_hash_v2 有值，跳过）
    if (null === $value && null !== $entry) { $entry->delete(); return null; }
    if ('' === (string) $value) { ... return null; }

    if (null === $entry) {
        $entry = new TransactionJournalMeta();
        $entry->transactionJournal()->associate($data['journal']);
        $entry->name = $data['name'];  // 'import_hash_v2'
    }

    // ⚠️ 关键：通过 Eloquent Attribute setter 赋值
    $entry->data = $value;  // $value = 'a1b2c3d4e5f6...'
    $entry->save();

    return $entry;
}
```

**第 3 步：Attribute setter 进行 JSON 编码和二次哈希**

[TransactionJournalMeta::data()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Models/TransactionJournalMeta.php#L57-L64)

```php
protected function data(): Attribute
{
    return Attribute::make(
        get: static fn ($value): mixed => json_decode((string) $value, false),
        set: static function ($value): array {
            // $value = 'a1b2c3d4e5f6...' （原始业务哈希字符串）
            $data = json_encode($value);
            // $data = '"a1b2c3d4e5f6..."' （带双引号的 JSON 字符串）

            return [
                'data' => $data,                                   // 写入 data 列
                'hash' => hash('sha256', $data)                    // 写入 hash 列
            ];
        }
    );
}
```

### 6.3 落库后的数据形态

假设业务哈希是 `a1b2c3d4`（简化示例），落库后的一条 `journal_meta` 记录如下：

| 列 | 值 | 说明 |
|----|----|------|
| `id` | 12345 | 自增 |
| `transaction_journal_id` | 678 | 关联的流水账 ID |
| `name` | `'import_hash_v2'` | 字段标识 |
| `data` | `"a1b2c3d4"` | **JSON 编码后的字符串**（带双引号，共 10 字符） |
| `hash` | `e1a7a2e5...` | 对 `"a1b2c3d4"` 这个 JSON 字符串的 SHA256 |
| `created_at` | 2026-06-13 ... | |
| `updated_at` | 2026-06-13 ... | |

### 6.4 存储与查询的对应关系

```
存储时：
  业务哈希 "a1b2c3d4"
    ↓ json_encode()
  data 列 = "\"a1b2c3d4\"" （带双引号）
    ↓ hash('sha256', ...)
  hash 列 = 二次哈希值（未被查询使用）

查询时（errorIfDuplicate）：
  业务哈希 "a1b2c3d4"
    ↓ json_encode()
  where('data', "\"a1b2c3d4\"")
    ↓ 精确匹配
  命中 data 列相同的记录 → 判定为重复
```

**为什么存储和查询都要 json_encode？**
- `TransactionJournalMeta` 的 `data` Attribute setter 统一对所有类型的值做 `json_encode`，保证字符串、数字、布尔、数组等任意类型都能安全存入 text 列
- 查询时必须做相同的编码，才能与数据库中存储的值匹配

### 6.5 `hash` 列的真实作用

**修正之前的理解**：`hash` 列**不用于重复检测查询**，也**没有索引**。它的实际情况是：

- `hash` 列存储的是 `data` 列内容的 SHA256
- 由 Eloquent Attribute setter 自动维护
- 在迁移文件 [2024_03_03_174645_add_indices.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/database/migrations/2024_03_03_174645_add_indices.php#L44-L63) 中**未创建索引**
- 当前代码中**没有任何查询使用 `hash` 列**

**推测的设计意图**：
`hash` 列是一个**预留给未来使用**的优化字段——因为 `data` 是 text 类型（可能存储大文本，如 notes），直接对 `data` 建索引效率不高。如果未来需要对大文本 meta 值做精确匹配查询，可以改为用 `hash` 列先过滤，再用 `data` 列确认。但对于当前的 `import_hash_v2` 场景（64 位十六进制 + 引号 = 约 66 字符），直接用 `data` 列索引已经足够高效。

### 6.6 元字段存储方法补充

**storeMetaFields**：[TransactionJournalFactory::storeMetaFields()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalFactory.php#L596-L601)

```php
private function storeMetaFields(TransactionJournal $journal, NullArrayObject $transaction): void
{
    // 遍历配置中的所有元字段（import_hash_v2 也在 journal_meta_fields 配置中）
    foreach ($this->fields as $field) {
        $this->storeMeta($journal, $transaction->getArrayCopy(), $field);
    }
}
```

---

## 7. 调用方输入 vs 内部录入：边界详解

### 7.1 边界可视化

```
调用方（前端 / API 调用者 / CSV 导入器 / 银行同步）
  │
  │  HTTP POST /api/v1/transactions
  │  { transactions: [...], error_if_duplicate_hash: true, ... }
  ▼
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  边界线 A：API 入口
  ▲ 所有调用方输入在此进入系统
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  │
  ▼
StoreRequest 字段标准化
  ├─ convertString() / clearString()   字符串清洗
  ├─ dateFromValue()                   日期解析
  ├─ integerFromValue() / float...()   类型转换
  └─ clearIban()                       IBAN 标准化
  │
  ▼
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  边界线 B：哈希计算点（最重要的边界）
  ▲ hashArray($row) 在此执行
  ▲ 此线以上的数据 = 哈希输入 = 重复判断依据
  ▼ 此线以下 = 系统内部录入逻辑，不影响哈希
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  │
  ▼
errorIfDuplicate() → 查到重复？→ 抛异常，整个流程终止
  │ 未重复
  ▼
内部录入阶段（均不影响哈希）
  ├─ 解析 transaction_type_id
  ├─ 查找/创建 source 账户（getAccount）
  ├─ 查找/创建 destination 账户
  ├─ 创建 TransactionJournal 记录
  ├─ 创建 Transaction 记录（2条）
  ├─ 关联 budget / category / tags
  ├─ 存储 notes / location
  └─ 存储所有 meta 字段（包括 import_hash_v2）
  │
  ▼
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  边界线 C：持久化完成
  ▲ 交易已落库，哈希已保存为未来比对的依据
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 7.2 边界两侧的数据对比

| 数据项 | 调用方输入侧<br>（参与哈希） | 内部录入侧<br>（不参与哈希） |
|--------|:-------------------------:|:---------------------------:|
| 交易类型 | type 字符串 ✅ | transaction_type_id ❌ |
| 币种 | currency_id / currency_code ✅ | 解析后的 Currency 对象 ❌ |
| 源账户 | source_id / source_name / source_iban / source_number / source_bic ✅ | 实际 Account 对象的 ID ❌ |
| 目标账户 | destination_* ✅ | 实际 Account 对象的 ID ❌ |
| 金额 | amount / foreign_amount 字符串 ✅ | Transaction 表的带符号数值 ❌ |
| 描述 | description ✅ | （相同，存于 journal 表） |
| 日期 | date / interest_date 等 Carbon 对象 ✅ | （相同，存于 journal 表） |
| 标签/分类/预算 | tags / category_id / budget_id 等 ✅ | 关联表记录 ❌ |
| SEPA 信息 | sepa_* ✅ | （相同，存于 meta） |
| 地理位置 | latitude / longitude / zoom_level ✅ | Location 模型对象 ❌ |
| 交易 ID | 无 ❌ | 自增 ID ❌ |
| 创建时间 | 无 ❌ | created_at ❌ |
| 用户 | 无 ❌（通过查询条件隔离） | user_id ❌ |

### 7.3 这意味着什么

**如果要保证同一笔交易被识别为重复**，调用方必须保证以下输入完全一致：
- 所有参与哈希的字段值完全相同
- 字段的类型和格式完全相同（比如日期格式、IBAN 是否带空格等 —— 但 `clearString` / `clearIban` 会帮你做部分标准化）

**以下操作不会影响重复判断**：
- 修改账户在数据库中的内部 ID（只要调用方传入的查询条件不变）
- 规则引擎对交易的后续修改（因为规则在创建之后触发）
- 给交易添加附件
- 修改用户权限

---

## 8. 关键数据结构

### 8.1 NullArrayObject - 安全数组访问

**文件**：[NullArrayObject.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Support/NullArrayObject.php)

哈希计算时使用该类包装数据，确保缺失字段返回 `null` 而非抛出异常：

```php
class NullArrayObject extends ArrayObject
{
    public function offsetGet($key): mixed
    {
        if ($this->offsetExists($key)) {
            return parent::offsetGet($key);
        }
        return null;  // 缺失字段统一为 null，保证 JSON 结构一致
    }
}
```

### 8.2 DuplicateTransactionException

**文件**：[DuplicateTransactionException.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Exceptions/DuplicateTransactionException.php)

专用异常类，用于标识交易重复：

```php
class DuplicateTransactionException extends Exception {}
```

---

## 9. 防重策略的设计智慧

### 9.1 字段标准化保证哈希一致性
- 所有字段经过类型转换（字符串、整数、浮点、日期对象）
- 缺失字段统一为 `null`（通过 NullArrayObject）
- 字符串经过清洗（trim、特殊字符处理、Zalgo 文本过滤）
- IBAN 去除空格

### 9.2 双层哈希机制
| 层级 | 位置 | 用途 |
|------|------|------|
| 业务哈希 | `import_hash_v2` 元字段的 data 列 | 对交易业务数据的 SHA256，用于重复判断 |
| 存储哈希 | `journal_meta.hash` 列 | 对 data 列 JSON 的 SHA256，用于数据库索引加速 |

### 9.3 多维度保护
- **用户隔离**：不同用户的相同交易不算重复（查询条件）
- **软删除感知**：已删除的交易也会阻止重新导入
- **开关控制**：`error_if_duplicate_hash` 允许调用方选择是否启用防重
- **回滚安全**：批量导入中任一笔重复，整批已创建的交易通过 `forceDeleteOnError()` 级联回滚

**回滚机制**：[TransactionJournalFactory::forceDeleteOnError()](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalFactory.php#L450-L460)

```php
private function forceDeleteOnError(Collection $collection): void
{
    $service = app(JournalDestroyService::class);
    foreach ($collection as $journal) {
        $service->destroy($journal);  // 级联删除：交易 + 流水账 + meta + 关联
    }
}
```

---

## 10. 涉及文件清单

| 文件 | 角色 | 边界位置 |
|------|------|---------|
| [parse-from-entries.js](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/resources/assets/v2/src/pages/transactions/shared/parse-from-entries.js) | 前端字段映射 | 调用方侧 |
| [create.js](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/resources/assets/v2/src/pages/transactions/create.js) | 前端提交入口 | 调用方侧 |
| [post.js](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/resources/assets/v2/src/api/v1/model/transaction/post.js) | API 客户端 | 调用方侧 |
| [StoreRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Api/V1/Requests/Models/Transaction/StoreRequest.php) | 后端字段映射与标准化 | 边界线 A |
| [ConvertsDataTypes.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Support/Request/ConvertsDataTypes.php) | 数据类型转换工具 | 边界线 A |
| [StoreController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Api/V1/Controllers/Models/Transaction/StoreController.php) | API 入口，异常处理 | 内部侧 |
| [TransactionGroupRepository.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Repositories/TransactionGroup/TransactionGroupRepository.php) | 仓储层 | 内部侧 |
| [TransactionGroupFactory.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionGroupFactory.php) | 组工厂，开关传递 | 内部侧 |
| [TransactionJournalFactory.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalFactory.php) | **核心**：哈希计算、重复检测、回滚 | **边界线 B** |
| [TransactionFactory.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionFactory.php) | 交易记录创建 | 内部侧 |
| [TransactionJournalMetaFactory.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Factory/TransactionJournalMetaFactory.php) | 元数据持久化 | 边界线 C |
| [TransactionJournalMeta.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Models/TransactionJournalMeta.php) | Meta 模型，自动哈希 | 边界线 C |
| [DuplicateTransactionException.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Exceptions/DuplicateTransactionException.php) | 重复异常 | 内部侧 |
| [NullArrayObject.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Support/NullArrayObject.php) | 安全数组访问 | 边界线 B（辅助） |
| [JournalServiceTrait.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Services/Internal/Support/JournalServiceTrait.php) | 账户查找、标签/预算关联 | 内部侧 |
| [firefly.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/config/firefly.php) | 元字段配置 | 配置 |
| [IsDuplicateTransaction.php](file:///d:/fz/0601-1/solo-dogfeeding/code/51-firefly-iii/app/Rules/IsDuplicateTransaction.php) | 验证规则（错误消息传递） | 内部侧 |
